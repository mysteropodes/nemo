[package]
name = "nemo-n03-headless-evaluator"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false
description = "N03 isolated feasibility proof for immutable headless evaluation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
sha2 = "0.10"

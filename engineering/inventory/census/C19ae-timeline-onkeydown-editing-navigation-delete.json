{
  "census_id": "C19ae",
  "issue": 1244,
  "title": "Timeline.js onKeyDown X/flip and bracket brush-size, Space/Alt-arm, Rig/Pen/Play Enter, Escape cascade, arrow-nudge (with Motion Position routing), PageUp/PageDown active-layer step, Alt+[ / Alt+] in/out trim, and the Delete/Backspace precedence chain plus Shift+F6 Clear Keyframe",
  "status": "source census; proposed, unrun behavioral fixtures; no runtime acceptance",
  "owner": "P03/#1005 executable census leaf following C19ad/#1243. Second half of `onKeyDown`'s own body: C06 owns the function's own signature line (9641) and the reconfigurable-shortcut dispatch (9935-9949, `if(runToolShortcut(k)){}else if(runCommandShortcut(k,event)){}`); C19ad owns the body BEFORE that dispatch (9642-9934, field guards through the Motion-only overrides, sibling PR #1271, not yet merged at authoring time); this leaf owns everything AFTER it, 9950-10216, through `onKeyDown`'s own closing brace. Line 9950 is not a fresh start — it is the CONTINUATION of a UI/UX-audit rationale comment C06's own 9945-9949 span begins and cuts off mid-sentence ('...Merged into one branch: X alone keeps its existing meaning' at 9949, '(swap stroke/fill), Shift+X/Shift+Alt+X reach the flip functions that were otherwise unreachable from any key, menu, or button.' at 9950-9951) — confirmed by direct read, recorded as a fact rather than a defect, not re-mapped. Line 10216 is `onKeyDown`'s own closing `}`, confirmed by direct read (line 10217 opens `var SPACE_TAP_MS=250;`, the lead-in of the MERGED sibling C19af-timeline-keyup-cmdhold-shape-flyout-colour-fields.json, whose own `assigned_ranges` is exactly [10217,10392] — a direct adjacency check, not an assumption). 267 assigned lines: 122 code, 145 full-line comment, 0 blank — independently re-derived by running a Python re-implementation of the validator's own classify() against `git show 59a5a38c:src/js/timeline.js`, matching the issue's own proposed union AND per-packet table exactly (42/56/87/82 lines; 10/24/44/44 code; 32/32/43/38 comment); no boundary correction needed anywhere in this scope. Cited by name/line only, not re-mapped, WITH corrections where the issue's own prose named the wrong file or the wrong existing owner (each confirmed by a direct declaration read, not trusted from the issue text): `window.SM.flipHorizontal`/`.flipVertical`/`.swapStrokeFill` (timeline.js:2027/2038/677, inside the `window.SM={...}` object literal opened at timeline.js:349 and closed at timeline.js:2695, same file, out of scope — not yet covered by any merged C19*.json at authoring time, confirmed by scanning every merged C19*.json's own `assigned_ranges` for these three lines, zero hits); `window.SM.setBrushSize`/`.setEraserSize` (timeline.js:520/789, same `window.SM={...}` literal — the issue's own text says 'tools.js', direct read shows both are timeline.js SM-object functions, corrected here; `state.eraserSize`'s own 2px floor clamp lives inside `setEraserSize` itself, `Math.max(2,parseInt(v)||24)`); `window.SM.deleteSelectedFrames` (timeline.js:1847, same literal, not yet covered); `window.SM.deleteSelStrokes` (timeline.js:1977, owned by merged C19f-timeline-content-commands.json, range 1977-2001); `window.SM.deleteLayer` (timeline.js:1027, owned by merged C19d-timeline-layer-lifecycle.json, range 862-1081); `startPlay`/`stopPlay`/`togglePlay` (timeline.js:40/169/177, NOT app.js as the issue's own text groups them with `goToFrame` — direct read, cross-checked against merged C21q-app-frame-navigation-keyframe-editing.json, whose own `owner` text independently places `startPlay`/`playStep`/`advancePlayFrame` at timeline.js:17-125; only `goToFrame` of that four-name group is actually app.js, app.js:5112, owned by that same merged C21q, range 5112-5410, called from this scope twice at 10076-10077); `finalizePen`/`_pen` (tools.js:7129/7128, correctly cited as tools.js by the issue); `_rigDraw` (rig-bridge.js:31 — the issue's own text says 'rig tool' without naming the file; `SMRig.finalizeRigBone` rig-bridge.js:508, exported rig-bridge.js:696, called from this scope at 10009); `_sel.frames` (declared timeline.js:210, owned by merged C19a-timeline-foundations.json, range 193-348 — the issue's own text says 'earlier C19 slices' without naming C19a, corrected here); `removeFrame`/`clearKeyframe` (app.js:5325/5340, owned by merged C21q, range 5112-5410 — the issue's own text groups these with `_sel.frames` under 'earlier C19 slices', but both are app.js/C21q, corrected here); `_fsSel`/`fsApplyDelete` (tools.js:57/807, read/discussed by merged C19l-timeline-fs-ghost-properties-geometry.json as an existing-owner cross-reference, matching the issue's own 'C19l' citation); `_nodeSel` (tools.js:917, read/discussed by merged C19k-timeline-selection-properties-adoption.json, per the issue's own 'C19/select-bridge' citation — a direct grep of select-bridge.js for 'nodeSel' returns ZERO hits anywhere in that file, so the 'select-bridge' half of the issue's own citation is inaccurate; corrected to tools.js/C19k here) / `nodeEditTargetPath`/`renderNodeHandles`/`fillRegenerateLinked` (tools.js:961/1133/2971) / `rebuildVectorBrushOutline` (tools.js:5672) — note that neither `nodeSelApplyMove` nor `nodeSelDelete` (both named in the issue's own non-remap text) is actually called anywhere in this scope's own 267 lines: `nodeSelApplyMove` exists (tools.js:1074, called elsewhere from `activeXformApplyMove`, timeline.js:10682, out of scope) but this scope's own subselect-Delete branch (10158-10181) removes nodes INLINE (`ntp.removeSegment`/`ntp.data.centerSegments.splice`) rather than delegating to it; a whole-src grep for `nodeSelDelete` returns ZERO hits anywhere in `src/js` — no such function exists; `stepActiveLayer` (timeline.js:9621, owned by merged C19ac-timeline-feedback-inbox-avatars-dashboard.json, range 9274-9640, correctly cited by the issue); `layerInPoint`/`layerOutPoint` (app.js:2838/2889, `layerInPointRaw`/`layerOutPointRaw` app.js:2858/2901, all owned by merged C21j-app-symbol-frame-layer-time-model.json — that artifact's own reconciliation ALREADY cites this scope's own two call sites, timeline.js:10119/10129, as 'a layer-bar drag boundary check', independently corroborating `boundaries` (e) below); `SPACE_TAP_MS`/`onKeyUp` (the MERGED sibling C19af-timeline-keyup-cmdhold-shape-flyout-colour-fields.json, timeline.js:10217-10392 — the issue's own text lists this as a not-yet-described range '10220-10272'; direct read of the merged artifact shows the actual filed range is 10217-10392, corrected here); `state.altDown` readers — the issue's own text says 'tools.js'; a direct grep of tools.js for 'altDown' returns ZERO hits anywhere in that file; the actual reader is `viewtools-bridge.js:77` (`shouldRotate`), corrected here; a SECOND, deliberately separate Alt-tracking mechanism exists in layer-inout.js (`setTimeLinkAltReveal`, whose own comment at layer-inout.js:1595 explicitly disclaims reading `state.altDown`, precisely to avoid coupling to this scope's own Alt+drag semantics) and is NOT a reader of this scope's own write; `TOOL_SHORTCUTS` (timeline.js:7457, C06, correctly cited by the issue, referenced only in the general onKeyDown-wide sense — a grep restricted to this scope's own 9950-10216 finds zero direct hits for `TOOL_SHORTCUTS`/`runToolShortcut`/`runCommandShortcut`/`_pointerOverTimeline`, confirming none of them is read from inside this scope's own body). This JSON authoring does not authorize source changes.",
  "source_sha": "59a5a38c514f5da830dd2f2df4c83c63b1b22fba",
  "observed_main_sha": "da00405b447bfc621d9963bb48173c739e2d519d",
  "source_path": "src/js/timeline.js",
  "source_blob": "cc4cfd27dc59d797c911d840dc46e2f816942123",
  "source_line_count": 11907,
  "scope_files": [
    "src/js/timeline.js"
  ],
  "intended_branch": "claude/c19ae-timeline-onkeydown-part2-census",
  "assigned_ranges": [
    {
      "start": 9950,
      "end": 10216
    }
  ],
  "coverage_note": "267 assigned lines occur once across four packets (one coverage_range per packet, no gaps, no other-owner exclusions): 122 code, 145 full-line comment, 0 blank — independently re-derived by running a Python re-implementation of the validator's own classify() against `git show 59a5a38c:src/js/timeline.js`, once per packet and once for the whole span. Result equals the issue's own proposed table exactly (122/145/0 over 267 lines) and the issue's own proposed per-packet breakdown exactly (42(10/32/0), 56(24/32/0), 87(44/43/0), 82(44/38/0)) — no boundary correction needed. Line 9949 is C06's own last dispatch-comment line (out of scope, its own sentence continues mid-thought into this scope's own 9950-9951, recorded not remapped); line 10216 (`}`) is `onKeyDown`'s own closing brace and the last line of this scope; line 10217 opens the merged sibling C19af's own `var SPACE_TAP_MS=250;` lead-in (assigned_ranges [10217,10392]).",
  "source_baselines": {
    "pinned": "59a5a38c514f5da830dd2f2df4c83c63b1b22fba:src/js/timeline.js resolves to cc4cfd27dc59d797c911d840dc46e2f816942123, 11907 lines.",
    "current": "origin/main (da00405b447bfc621d9963bb48173c739e2d519d at authoring time) has the identical timeline.js blob (git rev-parse da00405...:src/js/timeline.js == git rev-parse 59a5a38c...:src/js/timeline.js == cc4cfd27dc59d797c911d840dc46e2f816942123); this artifact branches from origin/main at da00405b447bfc621d9963bb48173c739e2d519d (the branch tip at authoring time, per `git log -1`), verified directly in the working worktree before authoring (worktree at /Users/ivg/github/nemo-worktrees/c19ae-census-20260913, working tree clean).",
    "runtime": "No browser, Tauri, export, native, GPU or application behavior was executed. All caller/reader findings were derived by `grep -n`/`awk` against the frozen blob dump (`git show 59a5a38c:src/js/timeline.js > /tmp/tl.js`) for every symbol this scope reads or dispatches to (`k`, `event.*`, `state.tool`/`appMode`/`spaceDown`/`_spaceAt`/`spaceUsedForPan`/`altDown`/`activeLayerIdx`/`currentFrame`/`playing`/`totalFrames`/`undoStack`/`brushSize`/`eraserSize`/`layers`, `_pen`, `_rigDraw`, `_sel`, `_fsSel`, `_nodeSel`, `selectedPaths`, `window._layerActiveExplicit`, `window._sceneVersion`, `canvasEl`), each cross-checked by a direct `sed -n`/`awk` read of the same range and, for every external symbol, of its own defining file via `grep -n 'function <name>'`/`<name>:function`/`<name> = function` declaration forms rather than trusting a call site's apparent file — this is what surfaced every citation correction recorded in `owner` above. Existing merged C19/C21 census siblings sharing `source_path` were scanned programmatically (their own `assigned_ranges` fields) to attribute each out-of-scope declaration line to its actual owning artifact rather than guessing from the issue's own prose.",
    "fixtures": "Every fixture is source-derived, proposed and unrun. In-source feedback quotes (feedback #803, #107, and the 2026-07-25/2026-07-29/2026-07-30/2026-07-31/2026-08-16/2026-08-27/2026-09 dated comments) are read as authorial intent, not independently re-verified execution evidence."
  },
  "writer_guidance": {
    "artifact": "The final census scope is only engineering/inventory/census/C19ae-timeline-onkeydown-editing-navigation-delete.json and the four timeline.js packets partitioning lines 9950-10216.",
    "timeline": "P30 retains the timeline.js runtime writer.",
    "function_boundary": "Four packets by construct, each a contiguous slice of `onKeyDown`'s own body at 2-space indentation, continuing the SAME if/else-if chain C06 opens at 9935 and C19ad continues at 9642: (1) the comment-continuation from C06's own cut-off sentence, the merged X/Shift+X/Shift+Alt+X flip-and-swap branch, and the merged `[`/`]` brush-or-eraser size branch; (2) Space-arm, Alt-down, the Rig/NumpadEnter/Pen/Play Enter chain, and the Escape cascade (rig-cancel, pen-cancel, selection-clear); (3) the arrow-key nudge (with its Motion-vs-canvas priority split), the plain-arrow frame-step fallback, PageUp/PageDown active-layer step, the drawing-tool Alt+bracket exemption, and the Alt+[ / Alt+] in/out-point trim pair; (4) the full Delete/Backspace precedence cascade (pen/frames/fsselect/subselect/select-strokes/select-layer/shift-removeFrame) and Shift+F6 Clear Keyframe, ending at `onKeyDown`'s own closing brace.",
    "reservations": "D01, T05/Pollen, historical Motion and all named reservations remain untouched."
  },
  "reconciliation": [
    {
      "owner": "C06.preferences.shortcuts-system (existing owner, timeline.js:9641 signature, 7457-7723 TOOL_SHORTCUTS/COMMAND_SHORTCUTS tables and dispatchers, 9935-9949 dispatch, merged, out of scope)",
      "disposition": "Retains `function onKeyDown(event){` (9641), `TOOL_SHORTCUTS` (7457)/`COMMAND_SHORTCUTS` (7508)/`runCommandShortcut` (7570)/`runToolShortcut` (7606), and the 9935-9949 dispatch in full — cited by name/line only. Two of this scope's own trailing-comment references (10203-10215) describe shortcuts that are ENTIRELY implemented inside this owner's own COMMAND_SHORTCUTS table and never reach this scope's own if/else-if chain at all: plain F6 → `cmdInsertKey` (timeline.js:7516, `insertKeyframe()`), F2 → `cmdRenameLayer` (timeline.js:7523, `startLayerRename`), '/' → `cmdResetView` (timeline.js:7522, `window.SM.resetView()`) — all three dispatched via `runCommandShortcut` at line 9939, well before this scope's own 9950-10216 is ever reached. Confirmed by a targeted grep for `k==='F2'`/`k==='\\/'`/an unconditional `k==='F6'` branch anywhere in 9950-10216: zero hits. See `boundaries` (h) and `future_proposals`."
    },
    {
      "owner": "C19ad (previous leaf, timeline.js:9642-9934, sibling PR #1271, not yet merged at authoring time)",
      "disposition": "Owns timeline.js:9642-9934 in full — the field guards, undo/save, clipboard chords, Select-All/Duplicate/Group/Lock/zoom, and the Motion-only P/A/R/S/T/U/E/M/graph-toggle/key-selection/work-area overrides that precede C06's own 9935-9949 dispatch. Confirmed disjoint by construction: this scope's own declared range begins at 9950, strictly after C06's own 9935-9949; a whole-scope grep restricted to 9950-10216 finds none of C19ad's own declared symbols (`inField`, `_hasInternalClip`, `_gLayerRows`, `_canvasClip`) referenced here."
    },
    {
      "owner": "C19af-timeline-keyup-cmdhold-shape-flyout-colour-fields.json (merged sibling, timeline.js:10217-10392)",
      "disposition": "Retains its own body in full — cited by name/line only. Owns `var SPACE_TAP_MS=250;` (10220) and `function onKeyUp(event){...}` (10221 onward, through its own `_cmdHoldEligible`/CMD_HOLD_MS machinery), which reads BACK the three `state.*` fields this scope's own Space-arm branch (9992-10000) writes (`spaceDown`, `_spaceAt`, `spaceUsedForPan`) to classify a tap vs. a hold, and which resets `state.altDown` to `false` (10234, this scope's own 10001 is the only WRITE of `true`). Confirmed adjacent, not overlapping, by direct read of both artifacts' own `assigned_ranges` ([9950,10216] here, [10217,10392] there) and by reading line 10216 (`}`, `onKeyDown`'s own closing brace) immediately followed by line 10217 (`var SPACE_TAP_MS=250;`)."
    },
    {
      "owner": "tools.js (`_pen`, `finalizePen`, `_fsSel`, `fsApplyDelete`, `_nodeSel`, `nodeEditTargetPath`, `renderNodeHandles`, `fillRegenerateLinked`, `rebuildVectorBrushOutline`, `nodeSelApplyMove`, existing owner, out of scope)",
      "disposition": "Retains its own bodies in full — cited by name/line only: `_pen` (7128), `finalizePen` (7129), `_fsSel` (57), `fsApplyDelete` (807), `_nodeSel` (917), `nodeEditTargetPath` (961), `renderNodeHandles` (1133), `fillRegenerateLinked` (2971), `rebuildVectorBrushOutline` (5672), `nodeSelApplyMove` (1074, NOT called from this scope — see `owner`). This scope's own Enter (10015), Delete-pen (10140-10150), Delete-fsselect (10152), and Delete-subselect (10158-10181) branches call these by name only; no body is reproduced here. A direct grep of tools.js for `altDown` returns zero hits — the issue's own citation of `state.altDown` readers as 'tools.js' is corrected in `owner` above."
    },
    {
      "owner": "rig-bridge.js (`_rigDraw`, `finalizeRigBone`, exported as `SMRig.finalizeRigBone`, existing owner, out of scope)",
      "disposition": "Retains its own body in full — cited by name/line only: `_rigDraw` (31), `finalizeRigBone` (508), `window.SMRig={...}` export (695-696). This scope's own two call sites are the Rig-Enter branch (10009, `SMRig.finalizeRigBone()`) and the Escape-cascade's own rig-cancel `if` (10017-10023, which manipulates `_rigDraw.path`/`.boneId`/`.ld`/`.draggingHandle` directly rather than calling into rig-bridge.js at all — see `boundaries` (f))."
    },
    {
      "owner": "app.js (`layerInPoint`/`layerInPointRaw`/`layerOutPoint`/`layerOutPointRaw`, `goToFrame`, `removeFrame`, `clearKeyframe`, existing owner, merged as C21j and C21q, out of scope)",
      "disposition": "`layerInPoint`(2838)/`layerInPointRaw`(2858)/`layerOutPoint`(2889)/`layerOutPointRaw`(2901) are owned by merged C21j-app-symbol-frame-layer-time-model.json, range app.js:2929+ per that artifact's own `owner` (its `reconciliation` for layer-inout.js ALREADY cites timeline.js:10119/10129 — this scope's own two Alt+bracket trim call sites — as 'a layer-bar drag boundary check', independently corroborating this scope's own read). `goToFrame`(5112)/`removeFrame`(5325)/`clearKeyframe`(5340) are owned by merged C21q-app-frame-navigation-keyframe-editing.json, range app.js:5112-5410. This scope's own call sites: `layerOutPoint`/`layerInPoint` at 10119/10129 (guarded by `window.layerOutPoint`/`window.layerInPoint` existence, falling back to the raw `ld.outPoint`/`ld.inPoint` field otherwise); `goToFrame` at 10076/10077; `removeFrame` at 10201; `clearKeyframe` at 10207. Retains all bodies in full, cited by name/line only."
    },
    {
      "owner": "the large `window.SM={...}` object literal (timeline.js:349-2695) and the per-property bodies this scope's own branches dispatch to by name, existing owner, out of scope, not yet covered in full by any single merged census leaf",
      "disposition": "Retains its own bodies in full — cited by name/line only: `swapStrokeFill`(677), `flipHorizontal`(2027), `flipVertical`(2038), `setBrushSize`(520), `setEraserSize`(789), `deleteSelectedFrames`(1847) — none of these six lines falls inside any merged C19*.json's own `assigned_ranges` at authoring time (confirmed by scanning every merged artifact sharing `source_path: src/js/timeline.js`), so they remain uncensused, existing-owner, out-of-scope territory rather than a specific sibling's own scope. Two further SM-object functions this scope calls ARE already censused: `deleteSelStrokes`(1977, merged C19f-timeline-content-commands.json, range 1977-2001) and `deleteLayer`(1027, merged C19d-timeline-layer-lifecycle.json, range 862-1081). This scope's own nine distinct call sites: `swapStrokeFill`(9954), `flipHorizontal`(9953)/`flipVertical`(9953), `setEraserSize`(9966)/`setBrushSize`(9967), `deleteSelectedFrames`(10151), `deleteSelStrokes`(10189), `deleteLayer`(10200)."
    },
    {
      "owner": "C19a-timeline-foundations.json (existing owner, `_sel`, merged, out of scope)",
      "disposition": "Retains its own declaration in full — cited by name/line only: `var _sel={frames:[],clipboard:null,clipOp:null};` (timeline.js:210), inside C19a's own assigned range (193-348). This scope's own sole read is `_sel.frames.length` (10151, the Delete/Backspace cascade's second precedence level) — never written here."
    },
    {
      "owner": "C19ac-timeline-feedback-inbox-avatars-dashboard.json (existing owner, `stepActiveLayer`, merged, out of scope)",
      "disposition": "Retains its own body in full — cited by name/line only: `function stepActiveLayer(dir){` (timeline.js:9621), inside C19ac's own assigned range (9274-9640). This scope's own sole call site is the PageUp/PageDown branch (10088)."
    },
    {
      "owner": "C19f-timeline-content-commands.json (existing owner, `deleteSelStrokes`, merged, out of scope)",
      "disposition": "Retains its own body in full — cited by name/line only: `deleteSelStrokes:function(){if(selectedPaths.length>0){pushUndo();...` (timeline.js:1977), inside C19f's own assigned range (1977-2001). This scope's own sole call site is the select-tool Delete branch (10189)."
    },
    {
      "owner": "C19d-timeline-layer-lifecycle.json (existing owner, `deleteLayer`, merged, out of scope)",
      "disposition": "Retains its own body in full — cited by name/line only: `deleteLayer:function(){` (timeline.js:1027), inside C19d's own assigned range (862-1081). This scope's own sole call site is the select-tool layer-delete branch (10200)."
    },
    {
      "owner": "C19l-timeline-fs-ghost-properties-geometry.json (existing owner, reads/discusses `_fsSel`, merged, out of scope)",
      "disposition": "Retains its own body in full — cited by name/line only. `_fsSel` is declared in tools.js:57 (not timeline.js); C19l's own `disposition` text ('Retains _fsSel/fsPrimarySel...') documents how a DIFFERENT timeline.js scope reads it, matching the issue's own 'C19l' citation for this symbol. This scope's own sole read is `_fsSel.length` (10152, the Delete/Backspace cascade's third precedence level, gated on `state.tool==='fsselect'`)."
    },
    {
      "owner": "C19k-timeline-selection-properties-adoption.json (existing owner, reads/discusses `_nodeSel`, merged, out of scope)",
      "disposition": "Retains its own body in full — cited by name/line only. `_nodeSel` is declared in tools.js:917 (not select-bridge.js, see `owner` for the correction); C19k's own `disposition` text ('Retains selectedPaths, activeXformBounds, Path geometry, anchors and _nodeSel...') documents how a DIFFERENT timeline.js scope reads it. This scope's own sole read is `_nodeSel.length`/`_nodeSel` (10158-10181, the Delete/Backspace cascade's fourth precedence level, gated on `state.tool==='subselect'`) — this scope also WRITES `_nodeSel=[]` once, after a successful node deletion (10174)."
    },
    {
      "owner": "viewtools-bridge.js (`shouldRotate`, the actual reader of `state.altDown`, existing owner, out of scope)",
      "disposition": "Retains its own body in full — cited by line only: `function shouldRotate() { return engineOn() && (state.tool === 'rotate' || (state.altDown && [...].indexOf(state.tool) < 0 && !state.spaceDown)); }` (viewtools-bridge.js:77). This scope's own sole write is `state.altDown=true` (10001); the reset to `false` happens in the merged sibling C19af's own `onKeyUp` (10234) and in this file's own window-blur listener (10242, also C19af's own range), neither reproduced here."
    },
    {
      "owner": "layer-inout.js (`setTimeLinkAltReveal`, existing owner, out of scope — cited for a NEGATIVE finding)",
      "disposition": "Retains its own body in full — cited by line only (1595). Its own comment explicitly states it does NOT read `state.altDown` (the field this scope's own 10001 writes), maintaining a deliberately separate Alt-tracking mechanism for its own free-anchor-dot reveal gesture so the two meanings never drift together. Recorded here only to rule out a false caller relationship a naive grep for 'altDown' would otherwise suggest."
    },
    {
      "owner": "motion.js (`SMMotion.setLayerValue`/`.getLayerValue`/`.invertVectorThroughParentChain`/`.liveRefreshVisiblePropertyFields`/`.enterComponentLayer`, existing owner, C20 lineage, out of scope)",
      "disposition": "Retain their own bodies in full — cited by name/line only: `setLayerValue`(13552, `window.SMMotion={...}` export), `getLayerValue`(13467), `invertVectorThroughParentChain`(3986), `liveRefreshVisiblePropertyFields`(6742), `enterComponentLayer`(6855). This scope's own call sites: the Motion arrow-nudge branch (10062-10072, `getLayerValue`/`invertVectorThroughParentChain`/`setLayerValue`/`liveRefreshVisiblePropertyFields`) and the NumpadEnter branch (10014, `enterComponentLayer`)."
    },
    {
      "owner": "P30/#1032",
      "disposition": "Retains the whole timeline.js runtime writer; this JSON authoring does not authorize source changes."
    },
    {
      "owner": "P03",
      "disposition": "This bounded census neither completes P03 nor certifies product behavior."
    }
  ],
  "boundaries": [
    "Line 9949 (C06's own last dispatch-comment line, out of scope) is outside coverage; line 9950 (the CONTINUATION of that same comment, mid-sentence) begins this scope. Line 10216 (`onKeyDown`'s own closing `}`) is the last line of this scope; line 10217 opens the merged sibling C19af's own `var SPACE_TAP_MS=250;` (assigned_ranges [10217,10392]), confirmed disjoint by direct read of both boundary lines and by C19af's own filed `assigned_ranges`.",
    "No lines inside 9950-10216 are excluded for another owner: every line belongs to exactly one of the four packets below, confirmed by the validator's packet-partition-equals-assigned-union check.",
    "(a) The complete ORDERED chord table for 9950-10216 — every `if`/`else if` that can consume the keydown, in source order, with its exact predicate, guard, `preventDefault`/`return` behavior, and callee(s) (owner file:line per `boundaries` (h) unless noted inline). Every branch below is reached only after C19ad's own generic field guard (9849, out of scope) has already passed and C06's own reconfigurable dispatch (9935-9949) has already failed to match — none of these branches has its own `inField` check, and none needs one. 1. X/flip (9952-9955): `k==='x'||k==='X'` — no `preventDefault`; `shiftKey`→(`altKey`→`flipVertical()` else `flipHorizontal()`); else `swapStrokeFill()`; no `return` statement (falls through the closing `}` of the `if` body, but the `else if` chain structure means no sibling branch below can also match the same keydown). 2. Bracket size (9964-9969): `(k==='['||k===']')&&!event.altKey` — no `preventDefault`; `sizeStep=(shiftKey?5:1)*(k===']'?1:-1)`; `tool==='eraser'`→`setEraserSize(eraserSize+sizeStep)` else `setBrushSize(Math.max(1,Math.min(80,brushSize+sizeStep)))`; `updateUI()`. 3. Space-arm (9992-10000): `k===' '&&!metaKey&&!ctrlKey` — `preventDefault()` (9993) UNCONDITIONAL; body only runs `if(!state.spaceDown)` (idempotent against key-repeat): arms `spaceDown=true`, `_spaceAt=now()`, `spaceUsedForPan=false`, `canvasEl.style.cursor='grab'`. 4. Alt-down (10001): `k==='Alt'` — no `preventDefault`; `state.altDown=true`. 5. Rig-Enter (10009): `k==='Enter'&&tool==='rig'&&typeof _rigDraw!=='undefined'&&_rigDraw.path` — `preventDefault()`; `SMRig.finalizeRigBone()` if present; `updateUI()`. 6. NumpadEnter-Motion (10014): `k==='Enter'&&event.code==='NumpadEnter'&&appMode==='motion'&&layers[activeLayerIdx]&&layers[activeLayerIdx].symbolId&&SMMotion&&SMMotion.enterComponentLayer` — `preventDefault()`; `SMMotion.enterComponentLayer(activeLayerIdx)`. 7. Plain Enter (10015): `k==='Enter'` — `preventDefault()`; `tool==='pen'&&_pen.path`→`finalizePen()` else `togglePlay()`. 8. Escape (10016-10034): `k==='Escape'` — see `boundaries` (f) for the full internal cascade; NO `preventDefault()` anywhere in this branch (the only multi-line branch in this scope with zero `preventDefault` calls at any nesting level). 9. Arrow-nudge (10048-10075): `selectedPaths.length&&(k==='ArrowLeft'||'ArrowRight'||'ArrowUp'||'ArrowDown')` — `preventDefault()` (10049) unconditional; see `boundaries` (d) for the Motion-vs-canvas split; `return`s inside the Motion branch, falls through to `selPropsApplyMove` otherwise (no explicit `return` needed, last statement in the `if` body). 10. Plain ArrowLeft frame-step (10076): `k==='ArrowLeft'` — no `preventDefault`; `if(playing)stopPlay();goToFrame(currentFrame-1);`. 11. Plain ArrowRight frame-step (10077): symmetric, `goToFrame(currentFrame+1)`. 12. PageUp/PageDown (10088): `k==='PageUp'||k==='PageDown'` — `preventDefault()`; `stepActiveLayer(k==='PageUp'?-1:1)`. 13. Alt+bracket drawing-tool exemption (10111-10114): `altKey&&(k==='['||k===']')&&['draw','fillbrush','pen','fill'].indexOf(tool)>=0` — empty body, no `preventDefault`, intentionally does nothing (see `boundaries` (e)). 14. Alt+[ trim-in (10115-10124): `altKey&&k==='['` — no `preventDefault`; guarded `layers[activeLayerIdx]&&!ld.symbolId`; see `boundaries` (e). 15. Alt+] trim-out (10125-10134): `altKey&&k===']'` — symmetric, no `preventDefault`. 16. Delete/Backspace cascade (10135-10202): `k==='Delete'||k==='Backspace'` — see `boundaries` (c) for the full seven-level decision table inside. 17. Shift+F6 (10207): `k==='F6'&&shiftKey` — `preventDefault()`; `clearKeyframe()`. No branch for plain F6, F2, or '/' exists anywhere in 9950-10216 — all three are dispatched entirely by C06's own COMMAND_SHORTCUTS table via `runToolShortcut`/`runCommandShortcut` at 9935-9939, confirmed by grep (zero `k==='F2'`, zero `k==='\\/'`, zero unconditional `k==='F6'` inside this scope) and by locating each in timeline.js's own COMMAND_SHORTCUTS array (F6→7516, F2→7523, '/'→7522) — see `boundaries` (h).",
    "(b) The two field guards from C19ad's own scope (9655 `inField`, 9849 generic re-check) are NOT re-checked anywhere in this scope's own 267 lines — every branch here is reached only once the generic guard at 9849 (out of scope) has already passed, so NONE of this scope's own branches can fire while a text/number/select field has focus. This is a direct structural consequence of position in the `else if` chain, not a per-branch guard this scope adds itself; no `event.target.tagName` check of any kind appears anywhere in 9950-10216 (confirmed by grep).",
    "(c) The Delete/Backspace precedence chain (10135-10202) as a decision table, in source order, naming the state each level reads and which function it dispatches to. Entry: `k==='Delete'||k==='Backspace'` (10135), no guard of its own beyond the reconfigurable-dispatch/generic-field-guard already passed above it. Level 1 — pen mid-draw (10140-10150): reads `state.tool`, `_pen.path`; `preventDefault()`; if `_pen.path.segments.length<=1` discards the WHOLE in-progress path (same effect as Escape's own pen-cancel, 10024) else `_pen.path.removeSegment(length-1)` (drops just the last anchor); either way `saveActiveLayerFrame();updateUI();`; no further level is reached if this one matches (the whole cascade is one `if`/`else if` chain). Level 2 — selected frame cells (10151): reads `_sel.frames.length`; `preventDefault()`; dispatches to `window.SM.deleteSelectedFrames()` (timeline.js:1847). Level 3 — fsselect (10152): reads `state.tool`, `_fsSel.length`; `preventDefault()`; dispatches to `fsApplyDelete()` (tools.js:807). Level 4 — subselect node deletion (10158-10181): reads `state.tool`, `_nodeSel.length`; `preventDefault()`; resolves target path via `nodeEditTargetPath()`, computes `minPts` (2 for a vector-brush centerline, 3 for a closed path, 2 for an open one), shows a toast and does nothing if deleting would go below `minPts`, otherwise deletes the selected indices (descending order) and calls `rebuildVectorBrushOutline`/`renderNodeHandles`/`fillRegenerateLinked`/`saveActiveLayerFrame`/`renderArcs`/`updateUI`/`SMEngineBridge.renderNow`. Level 5 — select-tool canvas strokes (10189): reads `state.tool`, `selectedPaths.length`, `window._layerActiveExplicit` (must be falsy); NO `preventDefault()` of its own; dispatches to `window.SM.deleteSelStrokes()` (timeline.js:1977). Level 6 — select-tool layer delete (10200): reads `state.tool`, `event.shiftKey` (must be falsy), `state.layers.length`; `preventDefault()`; dispatches to `window.SM.deleteLayer()` (timeline.js:1027) — this is the level `window._layerActiveExplicit` exists to protect from Level 5's own more specific match (per the in-source rationale at 10182-10188/10190-10199, both feedback #803's and feedback #107's fix). Level 7 — Shift fallback (10201): reads `event.shiftKey` (must be truthy); NO `preventDefault()`; dispatches to `removeFrame()` (app.js:5325). If none of the seven levels' own guard matches, the whole Delete/Backspace keydown is silently swallowed by having matched the outer `k==='Delete'||k==='Backspace'` test with no inner `if` succeeding — no default action, no `preventDefault()` call, and (per (b) above) no field-guard involvement either.",
    "(d) The arrow-nudge math (10048-10075): step is `event.shiftKey?10:1` (10050, standard Illustrator/Figma 1px/10px-with-Shift convention, distinct from the 1px/5px-with-Shift convention packet 1's bracket-size branch uses); `ndx`/`ndy` are the per-direction signed deltas (10051-10052). Undo strategy differs by branch: the Motion branch (10062-10072) calls `pushUndo()` (tweens.js:4741, aliasing `pushUndoLayers`) ONCE per keydown before writing through `SMMotion.setLayerValue` — one undo step per arrow press, not coalesced across repeats; the non-Motion fallback (10074) calls `selPropsApplyMove(ndx,ndy)` (timeline.js:3862, out of scope) with no `skipUndo` argument passed, so that function's own default undo behavior applies (not reproduced here, its body is out of scope). The apply helper for the non-Motion path is `selPropsApplyMove`, NOT `activeXformApplyMove` (the issue's own text asks 'which apply helper — activeXformApplyMove? cite' — direct read shows `activeXformApplyMove`, timeline.js:10682, is never called anywhere in 9950-10216; it is `selPropsApplyMove`, timeline.js:3862, called directly at 10074).",
    "(e) The Alt+[ / Alt+] trim semantics (10111-10134). `ld.inPoint`/`ld.outPoint` are the two fields written (never `ld.symTrimIn`/`symTrimOut`, a distinct pair app.js's own resolveSymbolFrameIdx/C21j scope reads for a different purpose, not touched here). Neither branch goes through a dedicated `SMLayerInOut` setter or an app.js setter function — both write `ld.inPoint`/`ld.outPoint` directly as plain field assignments, then call `renderTimeline();loadFrame(state.currentFrame);` and `SMEngineBridge.renderNow()` (guarded) to refresh, mirroring but not delegating to layer-inout.js's own drag-handler pattern (per that file's own in-source comment this scope's rationale, 10093-10094, explicitly references). The drawing-tool exemption list (10111) is `['draw','fillbrush','pen','fill']` — exactly four tools, checked via `.indexOf(state.tool)>=0`; Select/Subselect (and every other tool) fall through to the actual trim branches. `Component`-layer instances (`ld.symbolId` truthy) are excluded from BOTH trim branches via `!ldIn.symbolId`/`!ldOut.symbolId` guards (10117/10127) — their own in/out resolution goes through `resolveSymbolFrameIdx`/C21j's own machinery instead, out of scope. Clamp math: in-point `Math.max(0,Math.min(state.currentFrame,curOut-1))` (never reaches or passes the current out-point); out-point `Math.max(curIn+1,Math.min(state.currentFrame,state.totalFrames-1))` (never at or before the in-point) — the SAME shape as the work-area B/N clamps C19ad's own scope documents (9921-9934, out of scope), confirmed by direct comparison of both formulas.",
    "(f) The Escape cascade order (10016-10034), naming the exact conditional structure (not merely the order of effects). Line 10016 opens `else if(k==='Escape'){`. Line 10017 is an INDEPENDENT `if(state.tool==='rig'&&typeof _rigDraw!=='undefined'&&_rigDraw.path){...}` (10017-10023) — cancels an in-progress rig bone: clears the engine's rig preview if present, removes the Paper.js path, pops `state.undoStack` if non-empty, nulls all four `_rigDraw.*` fields, `updateUI()`. Line 10024 is a SEPARATE `if(state.tool==='pen'&&_pen.path){...}` — NOT chained to the rig `if` above it — that cancels an in-progress pen path (removes the preview line and the path, pops `state.undoStack`, nulls `_pen.path`/`_pen.draggingHandle`, `saveActiveLayerFrame();updateUI();`). Line 10033 is `else if(selectedPaths.length){clearSel();renderArcs();updateUI();}`, chained to the PEN `if` at 10024 (its `else`), NOT to the rig `if` at 10017. The practical effect is a three-way exclusivity (rig-cancel, OR pen-cancel, OR selection-clear) but this rests entirely on `state.tool` holding exactly one value at a time and `_pen.path`/`selectedPaths.length` being mutually exclusive in practice for a given tool — the source itself does not enforce it via a shared `else if` chain across all three (see `future_proposals`). No `event.preventDefault()` call exists anywhere inside the Escape branch (10016-10034), the only branch of this scope with internal complexity and zero `preventDefault` calls at any level.",
    "(g) Every `state.*` field read or written in 9950-10216, confirmed by a whole-span `grep -oE 'state\\.[A-Za-z_]+'` and classified by direct read of each site. READ-ONLY: `state.activeLayerIdx` (10014, 10063-10064, 10116, 10126, 10200), `state.appMode` (10014, 10062), `state.brushSize` (9967), `state.currentFrame` (10067, 10076-10077, 10120, 10130), `state.eraserSize` (9966), `state.layers` (10014, 10064, 10116, 10126, 10200), `state.playing` (10076-10077), `state.tool` (10009, 10015, 10017, 10024, 10062-implicitly via appMode not tool, 10111, 10140, 10152, 10158, 10189, 10200 — 'tool' read nine times total across the span), `state.totalFrames` (10119, 10130). WRITTEN-ONLY: `state._spaceAt` (9996), `state.altDown` (10001, always to `true` — the reset to `false` is C19af's own `onKeyUp`/blur-listener, out of scope), `state.spaceUsedForPan` (9997, always to `false` here). READ-AND-WRITTEN: `state.spaceDown` (read 9994, written 9995), `state.undoStack` (read via `.length` at 10020/10024/10144, written via `.pop()` at the same three sites). No other `state.*` field appears anywhere in this scope's own 267 lines — in particular `state.selectedStrokeIndices`/`state.waIn`/`state.waOut` (C19ad's own scope) and `state.appMode` outside the two Motion-gated sites above never appear here.",
    "(h) Every external function called from 9950-10216, owning module cited by name/line only, bodies not reproduced (superset of the per-branch citations in (a)-(f), collected here for completeness): `window.SM.flipHorizontal`(2027)/`.flipVertical`(2038)/`.swapStrokeFill`(677)/`.setBrushSize`(520)/`.setEraserSize`(789)/`.deleteSelectedFrames`(1847)/`.deleteSelStrokes`(1977, C19f)/`.deleteLayer`(1027, C19d) — all in the `window.SM={...}` literal, timeline.js:349-2695. `updateUI`/`renderArcs`/`renderTimeline`/`clearSel`/`selPropsApplyMove`(3862)/`showToast`(7447) — timeline.js, out of scope, earlier. `finalizePen`(tools.js:7129), `nodeEditTargetPath`(961)/`renderNodeHandles`(1133)/`fillRegenerateLinked`(2971)/`rebuildVectorBrushOutline`(5672)/`fsApplyDelete`(807) — tools.js. `SMRig.finalizeRigBone`(rig-bridge.js:508, exported 695-696). `SMMotion.enterComponentLayer`(motion.js:6855)/`.setLayerValue`(13552)/`.getLayerValue`(13467)/`.invertVectorThroughParentChain`(3986)/`.liveRefreshVisiblePropertyFields`(6742) — `window.SMMotion={...}` export, motion.js:13352-ish (per C19ad's own citation of the same export line). `stepActiveLayer`(timeline.js:9621, C19ac). `layerOutPoint`(app.js:2889)/`layerInPoint`(app.js:2838) — C21j, called guarded-by-existence (`window.layerOutPoint?...`/`window.layerInPoint?...`). `goToFrame`(app.js:5112)/`removeFrame`(app.js:5325)/`clearKeyframe`(app.js:5340) — C21q. `stopPlay`(timeline.js:169)/`togglePlay`(timeline.js:177) — timeline.js, out of scope, earlier (NOT app.js, see `owner`). `saveActiveLayerFrame`(app.js, out of scope, not independently re-verified in this census — cited by name only per the issue's own reservation on out-of-scope bodies). `window.SMEngineBridge.renderNow`/`.setRigPreview` — guarded by existence at every call site (10009's own `SMRig` check is separate from an `SMEngineBridge` check; 10018, 10071, 10122, 10132, 10178 all guard `if(window.SMEngineBridge)`/`if(window.SMEngineBridge&&window.SMEngineBridge.setRigPreview)`). `SM.t`(timeline.js, out of scope, the i18n lookup used once at 10165 for the toast message key `toastNotEnoughPointsLeft`). DOM/browser built-ins: `canvasEl.style.cursor` (9998), `performance.now`/`Date.now` (9996), array `.indexOf`/`.sort`/`.forEach`/`.splice` (native). No other external function is called anywhere in lines 9950-10216 — confirmed by reading every statement in the span directly.",
    "(i) Callers: `onKeyDown` is registered exactly once, at `document.addEventListener('keydown',onKeyDown);document.addEventListener('keyup',onKeyUp);` (timeline.js:10251, inside the merged sibling C19af's own assigned range) — the same single call site C19ad's own `boundaries` (i) already confirmed by a whole-src grep excluding this function's own declaration/comment lines and the three unrelated same-named local closures in fill-bridge.js:191/draw-bridge.js:1073/layer-inout.js:341. Nothing in this scope's own 267 lines is on the render or export CALL path: a targeted grep of `getEffectiveStrokes` (app.js:2949) and `buildSceneJson` (engine-bridge.js:772) for every symbol this scope declares or calls returns zero hits. This scope's own field writes (`state.altDown`, `state.spaceDown`/`_spaceAt`/`spaceUsedForPan`, `ld.inPoint`/`ld.outPoint` via the Alt+bracket trim branches, `_nodeSel=[]`) are read by OTHER, out-of-scope code for rendering/interaction purposes (`state.altDown` by viewtools-bridge.js's `shouldRotate`; `ld.inPoint`/`ld.outPoint` by `getEffectiveStrokes`'s own render gate, per C21j's own citation, an indirect-but-real dependency of the same class C19ad documented for `state.waIn`/`waOut`) — but this scope itself contains no rendering, persistence, or export logic beyond the direct `saveActiveLayerFrame()`/`renderArcs()`/`renderTimeline()`/`loadFrame()`/`updateUI()`/`SMEngineBridge.renderNow()` refresh calls each branch makes after mutating state, all of which dispatch BY NAME to other owners."
  ],
  "areas": [
    {
      "area": "x-flip-and-bracket-sizes",
      "packets": [
        {
          "id": "C19ae.onkeydown-x-flip-and-bracket-sizes",
          "files": [
            "src/js/timeline.js:9950-9991"
          ],
          "coverage_ranges": [
            [
              9950,
              9991
            ]
          ],
          "classification": {
            "code": 10,
            "comment": 32,
            "whitespace": 0
          },
          "symbols": [
            "event",
            "shiftKey",
            "altKey",
            "flipVertical",
            "flipHorizontal",
            "swapStrokeFill",
            "state",
            "tool",
            "eraserSize",
            "setEraserSize",
            "brushSize",
            "setBrushSize",
            "updateUI",
            "spaceDown"
          ],
          "signature": "else if(k==='x'||k==='X'){ ... } else if((k==='['||k===']')&&!event.altKey){ ... } // SPACE rationale precedes the next packet's own Space-arm branch",
          "responsibility": "The continuation of C06's own cut-off UI/UX-audit comment; the merged X/Shift+X/Shift+Alt+X flip-and-swap branch; the merged `[`/`]` brush-or-eraser size branch; and the full SPACE rationale comment block that precedes packet 2's own Space-arm branch. See `boundaries` (a).",
          "callers": [
            "No external caller — a contiguous slice of `onKeyDown`'s own body, registered once at timeline.js:10251 (see `boundaries` (i))."
          ],
          "behavior": [
            "Lines 9950-9951 (comment, 2 lines): the CONTINUATION of C06's own UI/UX-audit comment (9945-9949, out of scope), cut mid-sentence at 9949 ('...Merged into one branch: X alone keeps its existing meaning') and completed here ('(swap stroke/fill), Shift+X/Shift+Alt+X reach the flip functions that were otherwise unreachable from any key, menu, or button.').",
            "Lines 9952-9955 (code, 4 lines): `else if(k==='x'||k==='X'){ if(event.shiftKey){if(event.altKey)window.SM.flipVertical();else window.SM.flipHorizontal();} else window.SM.swapStrokeFill(); }` — no `event.preventDefault()` call anywhere in this branch (see `future_proposals`).",
            "Lines 9956-9963 (comment, 8 lines): `[`/`]` brush/eraser size rationale — Photoshop/Procreate/Clip Studio convention, previously entirely absent (grepped: no bracket-key handler anywhere); 1px per press, 5px with Shift; eraser gets its own `state.eraserSize` (2px floor clamp lives inside `setEraserSize` itself) rather than `state.brushSize` so this doesn't cross-affect the two; notes `setBrushSize` already restyles a live Select/Subselect selection in place, so bracket keys inherit that for free.",
            "Lines 9964-9969 (code, 6 lines): `else if((k==='['||k===']')&&!event.altKey){ var sizeStep=(event.shiftKey?5:1)*(k===']'?1:-1); if(state.tool==='eraser')window.SM.setEraserSize(state.eraserSize+sizeStep); else window.SM.setBrushSize(Math.max(1,Math.min(80,state.brushSize+sizeStep))); updateUI(); }` — the `!event.altKey` guard disambiguates this branch from packet 3's own Alt+[ / Alt+] trim chords (10111-10134), which read the SAME two keys; no `event.preventDefault()` here either.",
            "Lines 9970-9991 (comment, 22 lines): SPACE rationale (2026-07-25 fix, plus a 2026-08-27 Cmd/Ctrl+Space follow-up) — Photoshop/Procreate/Krita/Clip Studio's 'temporary Hand' convention collides head-on with After Effects/Premiere/TVPaint/Blender's 'play/pause'; resolved AE-style, by gesture (hold-and-drag pans, tap-and-release plays) rather than by picking a side; states explicitly that THIS handler only ever ARMS the pan (packet 2's own 9992-10000) and that the merged sibling C19af's own `onKeyUp` decides tap vs. hold from whether a drag actually happened and how long the key was held; documents the Cmd/Ctrl+Space exclusion (macOS steals focus to Spotlight the instant that combo is pressed, so the matching keyup for plain Space never arrives and `state.spaceDown` would stick `true` forever, with `viewtools-bridge.js`'s pan/rotate logic then treating every subsequent click as a pan) and cross-references the window-blur safety net (C19af's own body, out of scope) as a second-layer fix for any OTHER modifier combo that steals focus the same way."
          ]
        }
      ]
    },
    {
      "area": "space-alt-enter-escape",
      "packets": [
        {
          "id": "C19ae.onkeydown-space-alt-enter-escape",
          "files": [
            "src/js/timeline.js:9992-10047"
          ],
          "coverage_ranges": [
            [
              9992,
              10047
            ]
          ],
          "classification": {
            "code": 24,
            "comment": 32,
            "whitespace": 0
          },
          "symbols": [
            "event",
            "state",
            "spaceDown",
            "_spaceAt",
            "spaceUsedForPan",
            "canvasEl",
            "performance",
            "altDown",
            "tool",
            "_rigDraw",
            "SMRig",
            "finalizeRigBone",
            "updateUI",
            "code",
            "appMode",
            "layers",
            "activeLayerIdx",
            "symbolId",
            "SMMotion",
            "enterComponentLayer",
            "_pen",
            "finalizePen",
            "togglePlay",
            "undoStack",
            "SMEngineBridge",
            "setRigPreview",
            "saveActiveLayerFrame",
            "selectedPaths",
            "clearSel",
            "renderArcs"
          ],
          "signature": "else if(k===' '&&!event.metaKey&&!event.ctrlKey){...} else if(k==='Alt'){...} else if(k==='Enter'&&tool==='rig'...){...} else if(k==='Enter'&&code==='NumpadEnter'...){...} else if(k==='Enter'){...} else if(k==='Escape'){ if(rig){...} if(pen){...} else if(selection){...} }",
          "responsibility": "Space-arm (pairs with C19af's own `onKeyUp` for tap-vs-hold), the `state.altDown` write, the Rig/NumpadEnter/Pen/Play Enter chain (four mutually exclusive Enter interpretations in priority order), and the three-way Escape cascade (rig-cancel, pen-cancel, selection-clear). See `boundaries` (a)/(f)/(g).",
          "callers": [
            "No external caller — a contiguous slice of `onKeyDown`'s own body, registered once at timeline.js:10251 (see `boundaries` (i))."
          ],
          "behavior": [
            "Lines 9992-10000 (code, 9 lines): Space-arm — `else if(k===' '&&!event.metaKey&&!event.ctrlKey){ event.preventDefault(); if(!state.spaceDown){ state.spaceDown=true; state._spaceAt=(window.performance&&performance.now)?performance.now():Date.now(); state.spaceUsedForPan=false; canvasEl.style.cursor='grab'; } }` — writes exactly the three `state.*` fields (`spaceDown`, `_spaceAt`, `spaceUsedForPan`) that C19af's own `onKeyUp` (timeline.js:10225-10230, out of scope) reads back to classify a tap vs. a hold; the `if(!state.spaceDown)` guard makes this idempotent against key-repeat auto-fire so a held Space doesn't reset `_spaceAt`/`spaceUsedForPan` mid-hold; `canvasEl.style.cursor='grab'` is the only DOM-visible effect.",
            "Line 10001 (code, 1 line): `else if(k==='Alt'){state.altDown=true;}` — the ONLY write to `state.altDown` anywhere in this scope's own 267 lines; its actual reader is `viewtools-bridge.js:77` (`shouldRotate`), not tools.js (see `owner`/`boundaries` (g)).",
            "Lines 10002-10008 (comment, 7 lines): Rig Enter/Escape rationale (2026-07-29 fix, QA-confirmed 'on ne sait pas comment finir les traits de bones') — rig-bridge.js's own bone-drawing tool previously finished ONLY via double-click or clicking back near the first anchor (Pen-tool convention); the Pen tool itself already had this exact Enter/Escape pair (this scope's own 10015/10024); Rig silently never got the same pair when it was built, even though it mirrors Pen's drawing interaction everywhere else.",
            "Line 10009 (code, 1 line): `else if(k==='Enter'&&state.tool==='rig'&&typeof _rigDraw!=='undefined'&&_rigDraw.path){event.preventDefault();if(window.SMRig)SMRig.finalizeRigBone();updateUI();}` — `_rigDraw` rig-bridge.js:31; `SMRig.finalizeRigBone` rig-bridge.js:508, exported rig-bridge.js:696.",
            "Lines 10010-10013 (comment, 4 lines): NumpadEnter rationale — AE's own 'open selected precomp' key, kept DISTINCT from regular Return via `event.code` (both report `event.key==='Enter'`); regular Enter keeps its play/pen/rig meanings below untouched; Motion mode + Component layer only (2026-07-31 parity sweep).",
            "Line 10014 (code, 1 line): `else if(k==='Enter'&&event.code==='NumpadEnter'&&state.appMode==='motion'&&state.layers[state.activeLayerIdx]&&state.layers[state.activeLayerIdx].symbolId&&window.SMMotion&&SMMotion.enterComponentLayer){event.preventDefault();SMMotion.enterComponentLayer(state.activeLayerIdx);}` — `SMMotion.enterComponentLayer` motion.js:6855.",
            "Line 10015 (code, 1 line): `else if(k==='Enter'){event.preventDefault();if(state.tool==='pen'&&_pen.path)finalizePen();else togglePlay();}` — the plain-Enter fallback once NumpadEnter and Rig have both failed to match; `finalizePen` tools.js:7129, `_pen` tools.js:7128; `togglePlay` timeline.js:177 (NOT app.js — see `owner`).",
            "Lines 10016-10034 (code+comment, 19 lines): the Escape cascade — full internal structure documented in `boundaries` (f): an independent rig-cancel `if` (10017-10023), a separate pen-cancel `if` (10024) chained to an `else if(selectedPaths.length){clearSel();renderArcs();updateUI();}` selection-clear branch (10033), with the Select/Subselect-deselect rationale comment (10025-10032, 2026-07 UI/UX audit — the footer hint had long claimed Escape deselects, but no code path did it before this) sitting between the pen-cancel line and the selection-clear line. No `event.preventDefault()` call exists anywhere inside this branch.",
            "Lines 10035-10047 (comment, 13 lines): arrow-nudge rationale (2026-07 UI/UX audit) — the footer hint had long claimed '↑↓←→ Déplacer' for a selection but no code path ever moved anything (ArrowLeft/Right always scrubbed the timeline frame instead, ArrowUp/Down did nothing at all); confirms via a live drag/nudge test that `selectedPaths` is guaranteed empty while any tool other than Select/Subselect is active (timeline.js's own tool-switch handler clears it, out of scope), so gating on its length in the next packet's own arrow-nudge branch can't steal arrow keys from Brush/Pen/etc; states the step sizes (1px, 10px with Shift) applied in packet 3."
          ]
        }
      ]
    },
    {
      "area": "arrows-pageupdown-alt-brackets-trim",
      "packets": [
        {
          "id": "C19ae.onkeydown-arrows-pageupdown-alt-brackets-trim",
          "files": [
            "src/js/timeline.js:10048-10134"
          ],
          "coverage_ranges": [
            [
              10048,
              10134
            ]
          ],
          "classification": {
            "code": 44,
            "comment": 43,
            "whitespace": 0
          },
          "symbols": [
            "selectedPaths",
            "event",
            "state",
            "appMode",
            "SMMotion",
            "setLayerValue",
            "activeLayerIdx",
            "layers",
            "pushUndo",
            "getLayerValue",
            "invertVectorThroughParentChain",
            "currentFrame",
            "liveRefreshVisiblePropertyFields",
            "SMEngineBridge",
            "renderNow",
            "_sceneVersion",
            "selPropsApplyMove",
            "playing",
            "stopPlay",
            "goToFrame",
            "stepActiveLayer",
            "tool",
            "altKey",
            "layerOutPoint",
            "layerInPoint",
            "totalFrames",
            "inPoint",
            "outPoint",
            "symbolId",
            "renderTimeline",
            "loadFrame"
          ],
          "signature": "else if(selectedPaths.length&&(ArrowLeft|Right|Up|Down)){...} else if(k==='ArrowLeft'){...} else if(k==='ArrowRight'){...} else if(k==='PageUp'||k==='PageDown'){...} else if(altKey&&(k==='['||k===']')&&[drawing tools]){} else if(altKey&&k==='['){...} else if(altKey&&k===']'){...}",
          "responsibility": "The arrow-key nudge with its Motion-vs-canvas priority split, the plain-arrow frame-step fallback, PageUp/PageDown active-layer step, the drawing-tool Alt+bracket exemption, and the Alt+[ / Alt+] in/out-point trim pair. See `boundaries` (a)/(d)/(e).",
          "callers": [
            "No external caller — a contiguous slice of `onKeyDown`'s own body, registered once at timeline.js:10251 (see `boundaries` (i))."
          ],
          "behavior": [
            "Lines 10048-10052 (code, 5 lines): arrow-nudge opens — guard `selectedPaths.length&&(k==='ArrowLeft'||'ArrowRight'||'ArrowUp'||'ArrowDown')` (10048); `event.preventDefault()` (10049); `nudgeStep=event.shiftKey?10:1` (10050); `ndx`/`ndy` computed per-direction (10051-10052).",
            "Lines 10053-10061 (comment, 9 lines): Motion QA-sweep rationale (2026-09) — the canvas drag of this SAME selection writes the layer's Motion Position (select-bridge's own motion branch, out of scope), never the geometry, so a nudge previously went straight to `selPropsApplyMove` and moved the DRAWING instead (measured live: ArrowRight in Motion moved the path 11 world units and left `motionStatic.position` untouched — not keyable, not undoable as a Motion edit, and silently edited the frame's artwork); the fix reuses the drag's own route: pull the world delta back through the parent chain, write through `setLayerValue` (keys at the playhead when the stopwatch is on).",
            "Lines 10062-10073 (code, 12 lines): the Motion branch — guarded `state.appMode==='motion'&&window.SMMotion&&SMMotion.setLayerValue`; early `return` if `!state.layers[nLi]`; `pushUndo()` (tweens.js:4741, aliasing `pushUndoLayers`) ONCE per keydown; reads the current value via `SMMotion.getLayerValue(nLi,'position')`; converts the raw canvas delta through `SMMotion.invertVectorThroughParentChain(nLi,state.currentFrame,ndx,ndy)` if present, else uses the raw `[ndx,ndy]`; writes the summed value via `SMMotion.setLayerValue(nLi,'position',[...])`; bumps `window._sceneVersion`; calls `SMMotion.liveRefreshVisiblePropertyFields()` and `SMEngineBridge.renderNow()`, both guarded by existence; `return`s before ever reaching line 10074.",
            "Line 10074 (code, 1 line): `selPropsApplyMove(ndx,ndy);` (timeline.js:3862) — the non-Motion fallback, reached only when the Motion branch's own guard fails (the issue's own text asks whether `activeXformApplyMove` is the apply helper; direct read shows it is `selPropsApplyMove`, `activeXformApplyMove` (timeline.js:10682) is never called anywhere in this scope — see `boundaries` (d)).",
            "Line 10075 (code, 1 line): `}` — closes the arrow-nudge branch.",
            "Line 10076 (code, 1 line): `else if(k==='ArrowLeft'){if(state.playing)stopPlay();goToFrame(state.currentFrame-1);}` — reached only when `selectedPaths` is empty or the arrow-nudge branch's own guard otherwise failed to match (a Select-tool user with no canvas selection still scrubs frames with plain arrows); `stopPlay` timeline.js:169, `goToFrame` app.js:5112 (C21q); no `event.preventDefault()`.",
            "Line 10077 (code, 1 line): `else if(k==='ArrowRight'){if(state.playing)stopPlay();goToFrame(state.currentFrame+1);}` — symmetric.",
            "Lines 10078-10087 (comment, 10 lines): Home/End + PageUp/PageDown rationale — notes Home/End as a near-universal NLE/AE 'go to first/last frame' convention with NO binding at all (grepped: zero 'Home'/'End' handlers), but this scope does NOT implement either — no `k==='Home'`/`k==='End'` branch exists anywhere in 9950-10216 (confirmed by grep, see `future_proposals`); PageUp/PageDown step the active layer up/down the panel (2026-07-25), motivated because changing the active layer had no keyboard path at all (Alt+arrows/Ctrl+arrows/PageUp/PageDown/Alt+[ ] were all verified inert for it before this), with PageUp/PageDown being the one combination left entirely unclaimed (Alt+[ / Alt+] is the AE in/out trim just below, plain [ ] is packet 1's own brush size).",
            "Line 10088 (code, 1 line): `else if(k==='PageUp'||k==='PageDown'){event.preventDefault();stepActiveLayer(k==='PageUp'?-1:1);}` — `stepActiveLayer` timeline.js:9621 (merged C19ac).",
            "Lines 10089-10110 (comment, 22 lines): Alt+[ / Alt+] rationale — AE's 'trim layer in/out point to current time'; layer-inout.js already has the full in/out-point system via mouse drag (`layerInPoint`/`layerOutPoint`, C21j) but this scope adds the first keyboard path; mirrors the exact clamp logic layer-inout.js's own drag handler uses so this can never invert the range; plain `[ ]` stay bound to brush/eraser size (packet 1); documents a 2026-08-16 collision bug (Cyril, live feedback 'si j'avance le curseur de temps sur une autre keyframe et que je dessine cela ramène l'outpoint...') traced to Draw/Fill-brush/Pen/Fill all binding Alt+drag to their OWN gesture (brush resize, closing-stroke, tangent-break — `toolShortcuts` table, out of scope), so a user resizing brush size via Alt+drag mid-stroke then reaching for the ALSO-bound `]` key while Alt was still physically held fired this AE shortcut instead and silently trimmed the layer's out point to wherever the playhead happened to be; fixed by excluding exactly those four tools.",
            "Lines 10111-10114 (code, 4 lines): the drawing-tool exemption — `else if(event.altKey&&(k==='['||k===']')&&['draw','fillbrush','pen','fill'].indexOf(state.tool)>=0){ /* fall through to nothing */ }` — an intentionally EMPTY branch body (its own comment at 10112-10113 explains the no-op) that exists solely to intercept the combo for these four tools before the trim branches below can match it, letting the tool's own Alt+drag/brush-size handling (bound elsewhere, out of scope) be the only effect.",
            "Lines 10115-10124 (code, 10 lines): Alt+[ trim-in — `ldIn=state.layers[state.activeLayerIdx]`; guarded `ldIn&&!ldIn.symbolId` (Component-layer instances excluded, their own in/out resolves via `resolveSymbolFrameIdx`/C21j, out of scope); `pushUndo()`; `curOut=window.layerOutPoint?layerOutPoint(ldIn):(ldIn.outPoint!=null?ldIn.outPoint:state.totalFrames-1)`; `ldIn.inPoint=Math.max(0,Math.min(state.currentFrame,curOut-1))`; `renderTimeline();loadFrame(state.currentFrame);`; `SMEngineBridge.renderNow()` guarded by existence. No `event.preventDefault()` call.",
            "Lines 10125-10134 (code, 10 lines): Alt+] trim-out — symmetric, `ldOut.outPoint=Math.max(curIn+1,Math.min(state.currentFrame,state.totalFrames-1))`, same helper/undo/render pattern, same absence of `event.preventDefault()`."
          ]
        }
      ]
    },
    {
      "area": "delete-cascade-and-shift-f6",
      "packets": [
        {
          "id": "C19ae.onkeydown-delete-cascade-and-shift-f6",
          "files": [
            "src/js/timeline.js:10135-10216"
          ],
          "coverage_ranges": [
            [
              10135,
              10216
            ]
          ],
          "classification": {
            "code": 44,
            "comment": 38,
            "whitespace": 0
          },
          "symbols": [
            "event",
            "state",
            "tool",
            "_pen",
            "saveActiveLayerFrame",
            "updateUI",
            "_sel",
            "frames",
            "deleteSelectedFrames",
            "_fsSel",
            "fsApplyDelete",
            "_nodeSel",
            "nodeEditTargetPath",
            "isVectorBrush",
            "centerSegments",
            "closed",
            "showToast",
            "SM",
            "toastNotEnoughPointsLeft",
            "pushUndo",
            "removeSegment",
            "rebuildVectorBrushOutline",
            "renderNodeHandles",
            "fillRegenerateLinked",
            "userLayers",
            "activeLayerIdx",
            "renderArcs",
            "SMEngineBridge",
            "renderNow",
            "selectedPaths",
            "_layerActiveExplicit",
            "deleteSelStrokes",
            "shiftKey",
            "layers",
            "deleteLayer",
            "removeFrame",
            "clearKeyframe"
          ],
          "signature": "else if(k==='Delete'||k==='Backspace'){ if(pen){...} else if(frames){...} else if(fsselect){...} else if(subselect){...} else if(select&&paths&&!explicit){...} else if(select&&!shift&&layers){...} else if(shift){...} } else if(k==='F6'&&shiftKey){...}",
          "responsibility": "The full seven-level Delete/Backspace precedence cascade (pen mid-draw, selected frame cells, fsselect, subselect node deletion, select-tool canvas strokes, select-tool layer delete, Shift-fallback removeFrame) and Shift+F6 Clear Keyframe, ending at `onKeyDown`'s own closing brace. See `boundaries` (a)/(c).",
          "callers": [
            "No external caller — a contiguous slice of `onKeyDown`'s own body, registered once at timeline.js:10251 (see `boundaries` (i))."
          ],
          "behavior": [
            "Line 10135 (code, 1 line): `else if(k==='Delete'||k==='Backspace'){` opens the whole precedence cascade — see `boundaries` (c) for the full seven-level decision table.",
            "Lines 10136-10139 (comment, 4 lines): pen mid-draw rationale — Illustrator/Figma 'undo the last placed anchor, keep drawing from the one before it'; previously the only way out of a misplaced point was Escape, which threw the WHOLE in-progress path away.",
            "Lines 10140-10150 (code, 11 lines): pen mid-draw branch (Level 1) — guard `state.tool==='pen'&&_pen.path`; `event.preventDefault()`; if `_pen.path.segments.length<=1` discards the WHOLE path (remove preview line, remove path, pop `state.undoStack` if non-empty, null `_pen.path`/`_pen.draggingHandle` — same effect as Escape's own pen-cancel) else `_pen.path.removeSegment(_pen.path.segments.length-1)` (drop just the last anchor); either way `saveActiveLayerFrame();updateUI();`.",
            "Line 10151 (code, 1 line): Level 2 — `else if(_sel.frames.length>0){event.preventDefault();window.SM.deleteSelectedFrames();}` — `_sel` timeline.js:210 (C19a); `deleteSelectedFrames` timeline.js:1847.",
            "Line 10152 (code, 1 line): Level 3 — `else if(state.tool==='fsselect'&&_fsSel.length){event.preventDefault();fsApplyDelete();}` — `_fsSel` tools.js:57, `fsApplyDelete` tools.js:807 (both read/discussed by merged C19l).",
            "Lines 10153-10157 (comment, 5 lines): subselect-Delete rationale — removes just the selected anchor(s) and lets the curve reflow through the remaining points (Illustrator/Figma convention); previously had NO handler at all, so Delete silently did nothing while a node was selected with the subselect tool ('impossible d'enlever le point de la ligne').",
            "Lines 10158-10181 (code, 24 lines): Level 4, subselect node deletion — guard `state.tool==='subselect'&&_nodeSel.length>0` (`_nodeSel` tools.js:917); `event.preventDefault()`; resolves target path via `nodeEditTargetPath()` (tools.js:961); branches on whether the path is a vector-brush CENTERLINE (`ntp.data.isVectorBrush&&ntp.data.centerSegments`) to pick `curLen`/`minPts` (2 for a centerline, 3 for a closed non-centerline path, 2 for an open one); if deleting would leave fewer than `minPts` points, shows a toast (`showToast(SM.t('toastNotEnoughPointsLeft'))`) and mutates nothing; otherwise `pushUndo()`, deletes the selected indices in DESCENDING order (`idxsDesc`, keeping earlier indices valid while splicing) via `ntp.data.centerSegments.splice`/`ntp.removeSegment`, rebuilds the centerline outline if needed (`rebuildVectorBrushOutline`), clears `_nodeSel`, calls `renderNodeHandles()`, `fillRegenerateLinked(userLayers[state.activeLayerIdx],ntp)`, `saveActiveLayerFrame();renderArcs();updateUI();`, and `SMEngineBridge.renderNow()` guarded by existence. Does NOT call `nodeSelApplyMove`/any `nodeSelDelete` (see `owner`) — the removal logic is entirely inline.",
            "Lines 10182-10188 (comment, 7 lines): same-rule-as-Cmd+D rationale (cross-references C19ad's own Cmd+D branch, feedback #803, out of scope) — a timeline row click auto-selects the layer's whole content, so this branch previously fired for a LAYER selection too and Delete emptied the drawing instead of removing the layer; the NEXT branch (feedback #107) previously only ever saw the case where the layer happened to be empty; `window._layerActiveExplicit` tells the two apart.",
            "Line 10189 (code, 1 line): Level 5 — `else if(state.tool==='select'&&selectedPaths.length>0&&!window._layerActiveExplicit)window.SM.deleteSelStrokes();` — `deleteSelStrokes` timeline.js:1977 (merged C19f). No `event.preventDefault()` call on this line, unlike every OTHER matched branch in this same cascade (see `future_proposals`).",
            "Lines 10190-10199 (comment, 10 lines): feedback #107 rationale — Delete with nothing on canvas selected but a layer row IS ('la touche supprimer ne marche pas pour supprimer un calque dans la timeline'); every branch above only deletes strokes/nodes/frames; a plain layer-row selection with no canvas selection fell all the way through to nothing before this fix; the `!event.shiftKey` guard on the next branch exists so Shift+Delete still reaches `removeFrame()` below rather than being shadowed; matches the same trash-button/context-menu action (`window.SM.deleteLayer()`, already scoped to its own last-layer/camera-layer guards) rather than duplicating that logic here.",
            "Line 10200 (code, 1 line): Level 6 — `else if(state.tool==='select'&&!event.shiftKey&&state.layers.length){event.preventDefault();window.SM.deleteLayer();}` — `deleteLayer` timeline.js:1027 (merged C19d).",
            "Line 10201 (code, 1 line): Level 7 — `else if(event.shiftKey)removeFrame();` — the cascade's final fallback, reached only when every guard above it has failed AND `event.shiftKey` is true; `removeFrame` app.js:5325 (merged C21q). No `event.preventDefault()` call.",
            "Line 10202 (code, 1 line): `}` — closes the Delete/Backspace cascade.",
            "Lines 10203-10206 (comment, 4 lines): Shift+F6 rationale — Animate's 'Clear Keyframe'; must be checked BEFORE plain F6 because that branch (C06's own `cmdInsertKey`, timeline.js:7516, dispatched via `runCommandShortcut`, out of scope, never excluded `shiftKey`) previously caused Shift+F6 to accidentally INSERT a keyframe instead (2026-07-31 parity sweep); `clearKeyframe` was previously reachable only via the frame-cell right-click menu.",
            "Line 10207 (code, 1 line): `else if(k==='F6'&&event.shiftKey){event.preventDefault();clearKeyframe();}` — `clearKeyframe` app.js:5340 (merged C21q).",
            "Lines 10208-10215 (comment, 8 lines): trailing rationale describing two shortcuts that have NO branch of their own anywhere in this scope — F2 rename-active-layer (Windows/Finder convention; AE's own binding, plain Return, is claimed by Play/pen/rig above by deliberate design; also the ONLY rename path in Motion mode, whose row double-click is claimed by `enterComponentLayer`) and '/' AE '100% zoom/actual size' → `resetView` (deliberately NOT shift-gated because AZERTY needs Shift to type '/' at all, making a Shift+/ AE fit-to-window distinction unreachable for French layouts; fit-to-window already has Ctrl+Alt+0 from C19ad's own zoom chords). Both F2 (`cmdRenameLayer`, timeline.js:7523) and '/' (`cmdResetView`, timeline.js:7522) are C06's own COMMAND_SHORTCUTS entries, dispatched via `runCommandShortcut` at line 9939 — confirmed by grep, zero `k==='F2'`/`k==='\\/'` branches exist anywhere in 9950-10216 (see `boundaries` (h), `future_proposals`).",
            "Line 10216 (code, 1 line): `}` — `onKeyDown`'s own closing brace, the last line of this scope."
          ]
        }
      ]
    }
  ],
  "source_consumers": [
    {
      "consumer": "save",
      "disposition": "No `saveActiveLayerFrame`/`saveAllLayerFrames` call exists in most of this scope — the exceptions are the pen mid-draw Delete branch (10149) and the pen-cancel half of Escape (10024), and the subselect node-deletion branch (10177), all three of which call `saveActiveLayerFrame()` (app.js, out of scope) directly after mutating live Paper.js geometry (removing a pen anchor, cancelling a pen path, deleting selected nodes). The Alt+[ / Alt+] trim branches write `ld.inPoint`/`ld.outPoint` directly as plain field mutations (no `saveActiveLayerFrame` call of their own) and instead call `renderTimeline();loadFrame(state.currentFrame);` — persistence of those two fields, if any, is handled entirely by whichever out-of-scope save path serializes `state.layers` itself (not traced here, per the issue's own reservation on out-of-scope bodies)."
    },
    {
      "consumer": "load",
      "disposition": "`loadFrame(state.currentFrame)` is called twice, both inside the Alt+[ / Alt+] trim branches (10121, 10131), to refresh the displayed frame after mutating `ld.inPoint`/`ld.outPoint` — the only two call sites in this scope's own body that re-trigger a frame load. No project/document load function is called anywhere else in 9950-10216."
    },
    {
      "consumer": "undo/redo",
      "disposition": "This scope calls `pushUndo` (tweens.js:4741, aliasing `pushUndoLayers`) directly at three sites: the Motion arrow-nudge branch (10065), the subselect node-deletion branch (10167, only on the path that actually deletes something — the not-enough-points-left path at 10165 takes no undo snapshot because it mutates nothing), and both Alt+[ / Alt+] trim branches (10118, 10128). It manipulates `state.undoStack` directly (not via `pushUndo`) in three OTHER places: the rig-cancel half of Escape (10020, `.pop()`), the pen-cancel half of Escape (10024, `.pop()`), and the pen mid-draw Delete branch (10144, `.pop()`) — each of these pops the LAST undo snapshot to cancel out the one taken when the in-progress path/bone was first started (out of scope, elsewhere), rather than pushing a new one. The non-Motion arrow-nudge fallback (`selPropsApplyMove`, 10074) and every branch in the Delete/Backspace cascade below Level 4 (`deleteSelectedFrames`/`fsApplyDelete`/`deleteSelStrokes`/`deleteLayer`/`removeFrame`) delegate their own undo entirely to their own out-of-scope bodies, not reproduced here. `state.altDown`/`state.spaceDown`/`_spaceAt`/`spaceUsedForPan` writes have NO undo snapshot associated with them anywhere in this scope's own body — consistent with them being ephemeral input/session state, not document content."
    },
    {
      "consumer": "selection",
      "disposition": "This scope reads `selectedPaths`/`_nodeSel`/`_fsSel`/`_sel.frames` extensively to decide precedence (the Escape cascade and the entire Delete/Backspace chain) but only WRITES to selection state in two places: Escape's own `clearSel()` (10033, empties `selectedPaths` via the out-of-scope `clearSel` body) and the subselect node-deletion branch's own `_nodeSel=[]` (10174, after a successful deletion). Neither the arrow-nudge branch, the Alt+bracket trim branches, nor the flip/swap/bracket-size branches in packet 1 mutate any selection state."
    },
    {
      "consumer": "animation/render (Motion + Animation 2D)",
      "disposition": "The arrow-nudge branch (10048-10075) is the one place in this scope with a genuine Motion-vs-Animation-2D behavioral SPLIT: in Motion it writes a keyable Position value through `SMMotion.setLayerValue`; outside Motion it calls `selPropsApplyMove`, a plain geometry edit. The NumpadEnter branch (10014) is Motion-only by its own guard (`appMode==='motion'`). Every other branch in this scope (flip/swap, bracket size, Space-arm, Alt-down, Rig/Pen/Play Enter, Escape, plain-arrow frame-step, PageUp/PageDown, Alt+bracket trim, the full Delete/Backspace cascade, Shift+F6) runs identically regardless of `state.appMode` — none of them reads `state.appMode` at all (confirmed by `boundaries` (g))."
    },
    {
      "consumer": "export",
      "disposition": "None of this scope's own function/variable symbols are called from `export.js`/`rive-export.js` directly (confirmed: zero matches for every declared/read symbol in either file, mirroring the methodology C19ad's own `source_consumers` used). However, the Alt+[ / Alt+] trim branches write `ld.inPoint`/`ld.outPoint`, and the merged C21j-app-symbol-frame-layer-time-model.json artifact's own `reconciliation` documents `layerInPoint`/`layerOutPoint` (app.js:2838/2889) as inputs to `getEffectiveStrokes`'s own render/export gate (app.js:2951, `layerHasTimeRange(ld)&&(frameIdx<layerInPoint(ld)||frameIdx>layerOutPoint(ld))`) — so pressing Alt+[ / Alt+] changes which frames of a layer are visible to BOTH the on-screen render and any export that goes through `getEffectiveStrokes`, even though no code in this scope is itself called from the export path. This is the same class of indirect-but-real export dependency C19ad documented for `state.waIn`/`state.waOut`."
    },
    {
      "consumer": "native bridges (Tauri/engine)",
      "disposition": "`window.SMEngineBridge.renderNow()` is called, guarded by existence, at five sites across this scope's own body: the rig-cancel half of Escape's own `.setRigPreview(null)` call (10018, a DIFFERENT SMEngineBridge method, not `renderNow`), the Motion arrow-nudge branch (10071), both Alt+[ / Alt+] trim branches (10122, 10132), and the subselect node-deletion branch (10178). No other Tauri-specific API and no other bridge module is referenced anywhere in 9950-10216."
    }
  ],
  "proposed_behavioral_fixtures": [
    {
      "id": "fx-x-bracket-no-preventdefault",
      "proposed": "Dispatch a synthetic 'x'/'X'/Shift+'X'/Shift+Alt+'X' keydown and a synthetic '['/']' keydown (with `!altKey`) on `document`, spying `event.preventDefault`; expect it NEVER called by either branch (unlike every packet-2/3/4 branch that DOES call it) while the expected `window.SM.*` function still fires exactly once each — verifies the negative claim in `boundaries` (a) items 1-2 and `future_proposals` — not run."
    },
    {
      "id": "fx-escape-independent-ifs-not-chained",
      "proposed": "Construct a fixture where BOTH `state.tool==='rig'&&_rigDraw.path` AND `state.tool==='pen'&&_pen.path` are simultaneously true (an artificial state impossible via the UI but reachable by direct state manipulation, since `state.tool` can only really hold one value); fire Escape; expect BOTH the rig-cancel body AND the pen-cancel body to run (their `if`s are independent, not `else if`-chained) — verifies the exact non-exclusivity documented in `boundaries` (f), distinguishing it from a defect in a real-world reachable state — not run."
    },
    {
      "id": "fx-arrow-nudge-motion-vs-canvas",
      "proposed": "With a Select-tool canvas selection non-empty, fire ArrowRight in Animation 2D mode; expect `selPropsApplyMove` called, `SMMotion.setLayerValue` NOT called. Switch to Motion mode with `window.SMMotion.setLayerValue` present, same selection, fire ArrowRight; expect `pushUndo()` called once, `SMMotion.getLayerValue`/`invertVectorThroughParentChain`/`setLayerValue`/`liveRefreshVisiblePropertyFields` all called exactly once, `selPropsApplyMove` NOT called — verifies the priority split in `boundaries` (d) and the 2026-09 QA-sweep regression it fixed — not run."
    },
    {
      "id": "fx-alt-bracket-drawing-tool-exemption",
      "proposed": "With `state.tool` set to each of 'draw','fillbrush','pen','fill' in turn, fire Alt+'[' and Alt+']'; expect `ld.inPoint`/`ld.outPoint` UNCHANGED and no `renderTimeline`/`loadFrame`/`renderNow` call (the exemption branch's own empty body). Then switch to 'select'; fire the same combos; expect `ld.inPoint`/`ld.outPoint` to clamp per the exact formulas in `boundaries` (e) — verifies the four-tool exclusion list and the trim math together — not run."
    },
    {
      "id": "fx-delete-cascade-seven-level-priority",
      "proposed": "Construct seven fixtures, each satisfying exactly one cascade level's own guard from `boundaries` (c) while leaving every level ABOVE it unmatched (empty `_pen.path`, empty `_sel.frames`, wrong `state.tool`, empty `_nodeSel`, empty `selectedPaths` or `window._layerActiveExplicit` true, `event.shiftKey` true/false as needed); fire Delete; expect exactly the one predicted function called in each fixture and no other — verifies the full precedence order and that `window._layerActiveExplicit`/`event.shiftKey` correctly disambiguate Levels 5/6/7 — not run."
    },
    {
      "id": "fx-subselect-min-points-guard",
      "proposed": "Select every anchor but one on an open 2-point path (or every anchor but two on a closed 3-point path, or every anchor but one on a vector-brush centerline with 2 points) with the subselect tool; fire Delete; expect `showToast(SM.t('toastNotEnoughPointsLeft'))` called, `pushUndo`/`removeSegment`/`splice` NOT called, `_nodeSel` UNCHANGED — verifies the `minPts` guard in `boundaries` (c) Level 4 — not run."
    },
    {
      "id": "fx-shiftf6-before-plain-f6",
      "proposed": "Fire Shift+F6; expect `clearKeyframe()` called (app.js:5340), `insertKeyframe()` (C06's own plain-F6 `cmdInsertKey`, timeline.js:7516) NOT called — verifies the ordering rationale at 10203-10206 and the boundary fact that plain F6 is dispatched entirely through C06's own COMMAND_SHORTCUTS table, never reaching this scope's own `else if` chain — not run."
    }
  ],
  "future_proposals": [
    "Four branches in this scope call no `event.preventDefault()` at all where every structurally similar sibling branch does: the X/flip branch (9952-9955), the bracket-size branch (9964-9969), the Delete/Backspace cascade's own Level 5 `deleteSelStrokes` branch (10189) and Level 7 Shift-fallback `removeFrame()` branch (10201). None of these is confirmed as a defect by this census (a keydown for a printable character or Delete/Backspace rarely has a browser default action worth suppressing on a canvas-focused app), but the inconsistency itself — some branches in the SAME cascade guard with `preventDefault()` and others don't — is a direct-read fact worth a product decision, the same category of finding C19ad recorded for its own `inField`-guard partition.",
    "The Escape cascade's own rig-cancel `if` (10017) and pen-cancel `if`/selection-clear `else if` (10024/10033) are two SEPARATE conditional statements, not a single three-way `else if` chain — their mutual exclusivity in practice rests entirely on `state.tool` holding exactly one value at a time (a real invariant elsewhere in the codebase, not enforced locally here). Purely a style observation from direct reading, not a behavioral difference under any state this scope's own code can actually reach — not flagged as a defect.",
    "The Alt+[ / Alt+] trim branches (10115-10134) write `ld.inPoint`/`ld.outPoint` as plain field assignments rather than through a dedicated setter function, unlike most other document-mutating branches in this same file which route through `window.SM.*`. This mirrors layer-inout.js's own drag-handler pattern (per this scope's own in-source comment, 10093-10094) rather than diverging from it, so it is recorded as a direct-read fact, not a defect.",
    "The trailing comment at 10208-10215 documents F2 (rename) and '/' (reset view) shortcuts that have NO corresponding branch anywhere in this scope's own 267 lines — both are C06's own COMMAND_SHORTCUTS entries (`cmdRenameLayer` timeline.js:7523, `cmdResetView` timeline.js:7522), dispatched via `runCommandShortcut` at line 9939, well before this scope's own chain is ever reached. The comment is orphaned DOCUMENTATION sitting in the wrong function's own body, not dead code — both shortcuts are alive and reachable, just implemented elsewhere. Flagged for awareness only.",
    "Neither `nodeSelApplyMove` (tools.js:1074, exists but not called from this scope) nor `nodeSelDelete` (named in the issue's own non-remap prose; a whole-src grep finds zero matches anywhere in `src/js`) is used by this scope's own subselect-Delete branch, which implements node removal inline instead. Not confirmed as a defect — the inline implementation appears complete and independently tested via its own `minPts` guard — but recorded because the issue's own text anticipated a function that does not exist in the current source.",
    "Home/End ('go to first/last frame', a near-universal NLE/AE convention) is described in this scope's own comment (10078-10080) as unbound, and remains unbound: no `k==='Home'`/`k==='End'` branch exists anywhere in 9950-10216 (or, per a targeted grep, anywhere else in `onKeyDown`'s own 9642-10216 span covered jointly by C19ad and this scope). Recorded as a direct-read fact matching the comment's own claim, not independently assessed as a product gap beyond what the in-source comment already states."
  ],
  "surface_inventory_cross_check": [
    "No lint/type/build surface was run for this census (source-read only, per its own runtime baseline note).",
    "A whole-`src/js` `grep -n` was run for every external symbol this scope reads or dispatches to by name (`window.SM.*` property names, `SMMotion.*`, `SMRig.finalizeRigBone`, `stepActiveLayer`, `layerInPoint`/`layerOutPoint`, `goToFrame`/`stopPlay`/`togglePlay`/`startPlay`, `removeFrame`/`clearKeyframe`, `finalizePen`, `nodeEditTargetPath`/`renderNodeHandles`/`fillRegenerateLinked`/`rebuildVectorBrushOutline`/`fsApplyDelete`, `showToast`, `selPropsApplyMove`), each resolved to its own defining file:line by grepping for the literal `function <name>`/`<name>:function`/`<name> = function` declaration form rather than trusting the issue's own prose or a call site's apparent module — this is what surfaced every citation correction recorded in `owner` (setBrushSize/setEraserSize actually timeline.js not tools.js; startPlay/stopPlay/togglePlay actually timeline.js not app.js; state.altDown's actual reader is viewtools-bridge.js not tools.js; _nodeSel is read/discussed by C19k not select-bridge.js).",
    "Every merged `engineering/inventory/census/C*.json` sharing `source_path: src/js/timeline.js` or `src/js/app.js` was scanned programmatically (its own `assigned_ranges` field) to attribute each out-of-scope declaration line this scope calls into to its ACTUAL owning artifact, rather than accepting the issue's own prose citations at face value: `_sel` (timeline.js:210) → C19a (193-348); `stepActiveLayer` (timeline.js:9621) → C19ac (9274-9640); `deleteSelStrokes` (timeline.js:1977) → C19f (1977-2001); `deleteLayer` (timeline.js:1027) → C19d (862-1081); `layerInPoint`/`layerOutPoint`/`layerInPointRaw`/`layerOutPointRaw` (app.js:2838/2889/2858/2901) → C21j; `goToFrame`/`removeFrame`/`clearKeyframe` (app.js:5112/5325/5340) → C21q; `swapStrokeFill`/`flipHorizontal`/`flipVertical`/`setBrushSize`/`setEraserSize`/`deleteSelectedFrames` (timeline.js:677/2027/2038/520/789/1847, all inside the `window.SM={...}` literal, 349-2695) → confirmed NOT inside any merged artifact's own `assigned_ranges` at authoring time, recorded as uncensused existing-owner territory rather than attributed to a specific sibling.",
    "A SEPARATE whole-src grep restricted to `getEffectiveStrokes` (app.js:2949) and `buildSceneJson` (engine-bridge.js:772) for every symbol this scope declares or calls returned ZERO hits — directly supporting the `source_consumers`/`boundaries` (i) claim that this scope's own code never runs on the render/export call path; the two fields it writes that DO have a render/export-relevant reader (`ld.inPoint`/`ld.outPoint` → `getEffectiveStrokes`'s own render gate via `layerInPoint`/`layerOutPoint`) were located by reading merged C21j's own `reconciliation` text directly, which independently cites this scope's own two call sites (timeline.js:10119/10129) — not assumed, cross-confirmed from BOTH sides.",
    "Every packet's own `classification` (code/comment/whitespace) was independently re-derived by a Python re-implementation of the validator's own `classify()` algorithm against a direct `git show 59a5a38c:src/js/timeline.js` dump, run once per packet and once for the whole span — 122/145/0 over 267 lines, matching the issue's own proposed union exactly and matching the issue's own proposed per-packet table exactly for all four packets (42(10/32/0), 56(24/32/0), 87(44/43/0), 82(44/38/0)); no boundary correction was needed. Every `symbols` entry in every packet was additionally verified with a per-symbol `grep -oE '\\b<sym>\\b'` restricted to that packet's own coverage range, matching the validator's own `re.search(r'\\b'+re.escape(sym)+r'\\b',text)` check exactly — one initial mismatch was found and corrected (four Space-arm-only symbols, `_spaceAt`/`spaceUsedForPan`/`canvasEl`/`performance`, had been listed on packet 1 by mistake; they are packet 2's own, where the actual code lines using them live; packet 1's own comment only literally contains the word `spaceDown`, confirmed present twice).",
    "`onKeyDown`'s own single call site (timeline.js:10251, inside the merged sibling C19af's own assigned range) and the absence of any other call site anywhere in the src tree were re-confirmed by the same whole-src grep for the literal string `onKeyDown` that C19ad's own artifact already ran and recorded, rather than re-running it independently a second time — cross-checked instead by reading C19ad's own `boundaries` (i) text and confirming this scope's own registration-line citation (10251) matches it exactly.",
    "`engineering/inventory/census/*.json` siblings present at authoring time sharing `source_path: src/js/timeline.js`: C19a through C19al are merged (37 artifacts at authoring time, including C19ac/C19a/C19d/C19f/C19l/C19k, this scope's own dependencies, and C19af, this scope's own immediate successor at timeline.js:10217-10392 — all confirmed present and read directly, not assumed); C19ad (this scope's own immediate predecessor, timeline.js:9642-9934) was NOT present as a merged artifact in this directory at authoring time (its own PR #1271 was open, not yet merged) — its own copy was fetched to `/tmp/c19ad.json` from commit 661c95c0f4d295a3e239c43226b838e9e91c4f45 per the issue's own instructions and validated for overlap against this scope's own declared range by temporarily copying it into this directory, per the issue's own acceptance step 2, then removed before this artifact's own commit. The validator's own cross-sibling overlap check binds against whichever C19*.json files are ACTUALLY present in the directory at validation time."
  ],
  "validation": {
    "expected_assigned_lines": 267,
    "expected_classification": {
      "code": 122,
      "comment": 145,
      "whitespace": 0
    },
    "packet_partition": [
      [
        9950,
        9991
      ],
      [
        9992,
        10047
      ],
      [
        10048,
        10134
      ],
      [
        10135,
        10216
      ]
    ],
    "packet_classification": "Each packet's own `classification` field equals classify(coverage_ranges), independently re-derived by script against the frozen blob: 9950-9991=(10,32,0); 9992-10047=(24,32,0); 10048-10134=(44,43,0); 10135-10216=(44,38,0); sums to (122,145,0)=267 — identical to the issue's own proposed table.",
    "coverage_contract": "assigned_ranges union == packet_partition union == 267 lines, disjoint packets, unique packet IDs within this artifact and across every sibling C*.json in the same directory sharing source_path.",
    "required_checks": [
      "python3 census_check.py engineering/inventory/census/C19ae-timeline-onkeydown-editing-navigation-delete.json <candidate-sha> exits 0",
      "python3 census_check.py engineering/inventory/census/C19a-timeline-foundations.json <candidate-sha> still exits 0",
      "python3 census_check.py engineering/inventory/census/C19ac-timeline-feedback-inbox-avatars-dashboard.json <candidate-sha> still exits 0",
      "python3 census_check.py engineering/inventory/census/C19af-timeline-keyup-cmdhold-shape-flyout-colour-fields.json <candidate-sha> still exits 0",
      "python3 census_check.py engineering/inventory/census/C19ai-timeline-blend-matte-dropdowns-view-tween-buttons.json <candidate-sha> still exits 0",
      "python3 census_check.py engineering/inventory/census/C19aj-timeline-toolbar-onion-transport-layer-buttons.json <candidate-sha> still exits 0",
      "python3 census_check.py engineering/inventory/census/C19w-timeline-duplicator-panel.json <candidate-sha> still exits 0",
      "With the not-yet-merged sibling C19ad-timeline-onkeydown-chords-motion-overrides.json temporarily copied into this directory (fetched from commit 661c95c0f4d295a3e239c43226b838e9e91c4f45), the validator's own cross-sibling overlap check additionally confirms zero overlap between this artifact's own [9950,10216] and C19ad's own [9642,9934]; the copy is removed before commit, not staged."
    ]
  }
}
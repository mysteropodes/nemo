{
  "census_id": "C20m",
  "issue": 1237,
  "title": "Motion.js expression compilation and evaluation: sandbox surface, compiledFnFor, normalizeExprResult, evalExpressionFor (2348-2634)",
  "status": "source census; proposed, unrun behavioral fixtures; no runtime acceptance",
  "owner": "P03/#1005 executable census leaf on the src/js/motion.js open span [1769,2932] of remediation-scope.json rangeCoverage (frozen index at 4bbe332), which the C20k-C20n series closes in four slices: C20k 1769-2130 (expression built-ins, merged #1261), C20l 2131-2347 (comp/self views, mesh-aware self, AE compatibility aliases, issue #1236, open at authoring time), C20m 2348-2634 (this artifact, expression compilation and evaluation), C20n 2635-2932 (valueAtFrame, key writers, element holders, issue #1238, open at authoring time). This leaf is the third slice. Proposed sole artifact: this file. src/js/motion.js and all callers remain read-only.",
  "source_sha": "59a5a38c514f5da830dd2f2df4c83c63b1b22fba",
  "observed_main_sha": "81646cb3f0ba1fd0548c65ffd766f3229278896a",
  "source_path": "src/js/motion.js",
  "source_blob": "6617bac29042c22399cac202bf054c09e4fdf30e",
  "source_line_count": 13914,
  "scope_files": [
    "src/js/motion.js"
  ],
  "intended_branch": "claude/c20m-motion-expression-compile-eval-census",
  "assigned_ranges": [
    {
      "start": 2348,
      "end": 2634
    }
  ],
  "coverage_note": "Exactly 287 assigned lines occur once in two complete packets: 183 code, 104 full-line comments, 0 blank — independently re-derived by classifying the frozen blob programmatically per-packet with the SAME blank/`//`-line/`/*...*/`-block algorithm the validator's own classify() runs (a Python re-implementation against `git show 59a5a38...:src/js/motion.js`), and it matches the issue's own proposed union AND per-packet table exactly for both packets (packet 1: 2348-2490, 91/52/0; packet 2: 2491-2634, 92/52/0; cross-checked against `sed -n` reads of both packet boundaries: 2348/2490/2491/2634). No boundary correction was needed: line 2347 is C20l's own trailing blank line (before-2348 bucket); line 2348 opens this scope's own `// ---- compilation` section header; line 2490 closes this scope's own `compiledFnFor`; line 2491 opens this scope's own `normalizeExprResult` lead-in comment (the issue's own suggested internal split point, used verbatim — no finer split was needed); line 2634 is this scope's own trailing `var _timeoutToastPending = false;`; line 2635 opens C20n's own `// Public wrapper...` lead-in comment for `valueAtFrame` (confirmed by direct `sed -n` read of the frozen blob, not reproduced here beyond the boundary line itself).",
  "source_baselines": {
    "pinned": "59a5a38c514f5da830dd2f2df4c83c63b1b22fba:src/js/motion.js resolves to 6617bac29042c22399cac202bf054c09e4fdf30e, 13914 lines.",
    "current": "This worktree's HEAD and the allocated base 81646cb3f0ba1fd0548c65ffd766f3229278896a (candidate commit of the just-merged C20k PR #1261) both resolve motion.js to the identical blob 6617bac29042c22399cac202bf054c09e4fdf30e — verified directly (`git rev-parse 59a5a38...:src/js/motion.js` == `git rev-parse 81646cb:src/js/motion.js`). origin/main has since advanced to 0af70a3003a2c24fd476b169827b9e5115e77dfe with zero commits touching src/js/motion.js in between (`git log --oneline 81646cb..origin/main -- src/js/motion.js` empty; `git rev-parse origin/main:src/js/motion.js` also resolves to the identical blob).",
    "runtime": "No browser, Tauri, export, native, GPU or application behavior was executed. All caller findings were derived by `grep -rnE \"(^|[^a-zA-Z_.])<symbol>\\(\"` over `src/js/*.js` (and, for i18n-key coverage, `grep -o \"<key>:\" src/js/i18n.js`), filtered to exclude each symbol's own declaration line and any hit whose trimmed content starts with `//`, then reading each surviving hit directly with `sed -n` against this worktree's copy of motion.js — confirmed byte-identical to the frozen blob above (`git diff 59a5a38...:src/js/motion.js -- src/js/motion.js` empty in this worktree), so its line numbers are valid frozen-blob citations.",
    "fixtures": "Every behavioral fixture is source-derived, proposed and unrun."
  },
  "writer_guidance": {
    "artifact": "Only engineering/inventory/census/C20m-motion-expression-compile-eval.json is writable for this outcome.",
    "runtime": "Historical Motion ownership retains motion.js runtime writing; P30 retains timeline.js. C20m transfers no implementation authority.",
    "owners": "C20a-j/C02 (before line 1769, cited by name/line only) retain every constant and helper this span reads but does not declare: `_ectx` itself (`var _ectx = null;`, motion.js:1365 — this span's evalExpressionFor is its sole WRITER of a non-null value, via `_ectx = {...}` at 2563-2569, saved/restored through a local `prev`, never its declarer), `_exprDepth`/`EXPR_MAX_DEPTH`/`_depthTripped` (1366/1367/1370), `EXPR_BUDGET_MS`/`_exprDeadline`/`_tickCount`/`EXPR_TIMEOUT_TAG`/`_exprTick` (1393-1397), `instrumentLoops`/`_isIdentChar`/`_skipLiteral` (1424-1426ish), `_lineFromStack`/`_closestName` (1494/1521 — `_closestName` reads this span's OWN `EXPR_PUBLIC_NAMES`, declared later in the same file at 2360, a forward reference resolved because the whole IIFE's `var`/function declarations exist before any expression ever evaluates), `_formatRuntimeError` (1531, called by this span's evalExpressionFor at 2625), `ensureExprSeed` (1551), `_exprFps`/`_num`/`_vec` (1371/1591/1590), `hasExpr` (1338 — the sole gate that guarantees `holder.expressions[prop]` exists and is enabled-with-code before either `compiledFnFor` or `evalExpressionFor` in this span ever dereferences it as `ex`, at both of this span's reachable call sites), `rawValueAtFrame` (1270), `PROP_DIM` (506, read once by normalizeExprResult and once by evalExpressionFor's own error-message branch), `trackFor` (443). C20k (1769-2130, merged #1261) retains every `expr*` built-in this span's argument list passes by reference: `exprLayer` (1986), `exprWiggle`/`exprNoise` (1802/1771), `exprRandom`/`exprRandomFixed`/`exprRandomGauss`/`exprRandomGaussFixed`/`exprSeed` (1765-1768/1733), `exprClamp`/`exprRemap`/`exprRemapEase`/`exprRemapEaseIn`/`exprRemapEaseOut`/`exprDegrees`/`exprRadians` (1623-1640), `exprAdd`/`exprSub`/`exprMul`/`exprDiv`/`exprLength`/`exprNormalize`/`exprDot`/`exprCross`/`exprAngleTo` (1643-1693), `exprStepTime`/`exprToFrames`/`exprToSeconds` (1709-1721), `exprLoopAfter`/`exprLoopBefore` (1909/1915), `exprSelfControl`/`exprLayerControl` (2020/2047), `exprNumKeys` (2070, called — not passed — at 2583), `exprSelfAt` (1823, passed directly at 2586 AND indirectly via C20l's `aliasValueAtTime` at 2287). C20l (2131-2347, issue #1236, open at authoring time) retains `SELF_VIEW`/`COMP_VIEW`/`COMP_VIEW.marker` (2150ish/2156+), `exprContentBox` (2257, passed under both its own name and the `sourceRectAtTime` alias), and every `alias*`/`buildAlias*` wrapper this span's argument list passes: `aliasKey`/`aliasNearestKey` (2294/2290), `aliasPosterizeTime` (2280), `aliasSeedRandom` (2295), `aliasValueAtTime`/`aliasVelocityAtTime`/`aliasSpeedAtTime` (2287-2289), `aliasTimeToFrames`/`aliasFramesToTime` (2300/2305), `buildAliasComp`/`buildAliasLayer` (2314/2325). C20n (2635-2932, issue #1238, open at authoring time) retains `valueAtFrame` (2639, this span's ONLY reachable caller via the normal render path) and `hasExpr`'s caller-side gate at 2641 (hasExpr itself is C20a-j/C02, 1338) — cited by name/line only. Sibling-covered past 2932: `computeMotionMat`, `layerMotionAt`/`elementMotionAt`. C20f (motion-transform-expression-panel, merged) retains the expression editor panel in full, including its OWN direct call into this span's `evalExpressionFor` (motion.js:9040, a live-preview readout keyed to the playhead) and into `exprGlobals`/`SMMotion.setExprGlobals` (9054/9058/13729-13736, the project-wide preamble editor UI) — both reachable because C20f's code lives inside the SAME top-level IIFE as this span, not through the public SMMotion object. C20/C13 retain the `exprControls` declaration UI (`addExprControl`/`genControlKey`, motion.js:286-298) and the pickwhip/inline-editor commit path (`setExpressionCode` 11579, `SMMotion.exprSnapshotFor`/`applyExprCode` 13621-13639) — both call sites mutate `ex.code`/`ex.enabled`/`ex.lastError`/`ex.errorLine` and proactively `delete holder._exprCompiled[prop]`, cited here only because they interact with the SAME cache this span's `compiledFnFor` maintains (see boundaries for why that delete is defensive, not load-bearing, given `compiledFnFor`'s own code/pre identity check). `rig-widget.js` retains `layerControl` (CLAUDE.md §13). `image-mesh.js`/`image-mesh-bridge.js` retain mesh holders (CLAUDE.md §12ter) — grep-confirmed no `meshId`/mesh-holder reference anywhere in this span. `engine-bridge.js` retains `affineFromMotion`/`buildSceneJson` (the render-time consumer, reached transitively through C20n's `valueAtFrame`). `nemo-script.js` retains its own `globals` scripting property (line 549: getter reads `state.exprGlobals` directly, setter calls `SMMotion.setExprGlobals`) — an external caller of the SAME `state.exprGlobals` field this span's own `exprGlobals()` reads, cited because it is the only src/js file outside motion.js that touches expression-globals state. `src/js/i18n.js` retains the locale tables this span's seven direct `SM.t(...)` call sites read from (`exprErrorSyntaxPrefix`, `exprErrorSyntaxGlobalsSuffix`, `exprErrorTooDeep`, `exprErrorMustReturnNumberOrXY`, `exprErrorMustReturnNumber`, `exprErrorTimeout`, `exprToastTimeout`) plus the five keys this span's own `_formatRuntimeError`/`_closestName` calls (C20a-j/C02) read transitively. `timeline.js` retains `showToast` (7447, C19w) and the `exportJSON`/`importJSON` whitelist (2117/2410) that carries `l.expressions` — including this span's own `.lastError`/`.errorLine`/`.enabled` bookkeeping fields — verbatim into/out of the saved project.",
    "reservations": "D01, T05/Pollen, C04, historical Motion and every named reservation remain untouched."
  },
  "reconciliation": [
    {
      "owner": "P03/#1005 rangeCoverage",
      "disposition": "remediation-scope.json lists src/js/motion.js 1769-2932 as one open 1164-line span (frozen index at 4bbe332), which the C20k-C20n series closes in four slices: C20k 1769-2130 (merged #1261, expression built-ins), C20l 2131-2347 (issue #1236, open, comp/self views + AE aliases), C20m 2348-2634 (this artifact, expression compilation/evaluation), C20n 2635-2932 (issue #1238, open, valueAtFrame/key writers/element holders). This artifact takes only 287 of the parent span's 1164 lines and does not close it."
    },
    {
      "owner": "C20a-j / C02 (everything before line 1769)",
      "disposition": "Cited by name/line only. `_ectx` (`var _ectx = null;`, motion.js:1365) is declared and initialized to `null` well before this span; this span's `evalExpressionFor` (2542-2633) is its sole WRITER of a non-null object, assigning `_ectx = {holder, prop, frame, time, fps, seed, rngSeed, rngTimeless: false, rngCounter: 0, _resolved: undefined}` (2563-2569) around the compiled call, saved to a local `prev` beforehand (2562) and restored in a `finally` (2630) alongside `_exprDepth--` (2631) — every C20k/C20l built-in that reads `_ectx` observes exactly this shape, never a partially-constructed one, because the assignment (2563-2569) is a single object-literal statement, not incremental field-by-field mutation. `hasExpr` (motion.js:1338, `holder.expressions && holder.expressions[prop] && .enabled && .code`) is the precondition both of this span's reachable call sites rely on before `ex = holder.expressions[prop]` is dereferenced: `valueAtFrame` (C20n, 2641) checks it before calling `evalExpressionFor`, and C20f's playhead preview (motion.js:9037, `if (!expr.enabled || !expr.code) return;`) checks the equivalent condition inline before its own direct call at 9040 — neither `compiledFnFor` nor `evalExpressionFor` defensively re-checks this inside the span itself, which is a documented reliance on caller discipline, not an oversight (no code path in this span calls either function without that precondition already true, grep- and read-confirmed for both of the two reachable callers). `EXPR_MAX_DEPTH` (1367, value 8) and `_depthTripped` (1370, an array) are read/mutated only inside this span's `evalExpressionFor` (2550-2553, 2559, 2604) — this span is their sole USER even though it does not declare them. `EXPR_BUDGET_MS` (1393, value 60) and `_exprDeadline` (1394) are set only inside this span (`_exprDeadline = Date.now() + EXPR_BUDGET_MS`, 2559, gated to the OUTERMOST call via `_exprDepth === 0`) and read only by `_exprTick` (1398, pre-1769) — the wall-clock budget is therefore a value this span writes but a sibling function enforces. `_exprTick` itself (1397-1400) increments a module-level `_tickCount` and checks the deadline only every 1024th call (`(++_tickCount) & 1023`), throwing `new Error(EXPR_TIMEOUT_TAG)` when tripped; this span's compiled function receives `_exprTick` as its 63rd sandbox argument (`__tick`, 2590) and `instrumentLoops` (1426, pre-1769) is what actually inserts calls to it inside every `while`/`for`/`do` loop of the user's own source text, at compile time, inside this span's `compiledFnFor` (2402-2403: `preSrc = pre ? instrumentLoops(pre) : ''`, `codeSrc = instrumentLoops(ex.code || '')`). `_formatRuntimeError` (1531-1542) and `_closestName` (1521-1530) are called only from this span's own `evalExpressionFor` catch block (2625) for the non-timeout runtime-error path; `_closestName` itself reads THIS SPAN's `EXPR_PUBLIC_NAMES` (2360, declared later in the file than `_closestName`'s own declaration at 1521) — a legal forward reference because `var`/function declarations across the whole enclosing IIFE all exist before any expression is ever evaluated at runtime. `ensureExprSeed` (1551) supplies the `seed` field `_ectx` carries (called once, 2560). `PROP_DIM` (506) is read once by this span's own `normalizeExprResult` (2510, to pick the array/scalar shape) and once by `evalExpressionFor`'s own must-return-a-number error message (2594, to choose the 1D vs 2D/XY wording) — the SAME table C20k's `exprWiggle` reads, cited by line only there. `rawValueAtFrame` is never called directly by this span (it is C20n's `valueAtFrame`, 2640, that calls it, one line past this span's own boundary) — this span's `evalExpressionFor` receives the already-computed raw value as its 4th parameter (`rawValue`, 2542) rather than sampling it itself."
    },
    {
      "owner": "C20k (motion-expression-builtins, merged #1261, motion.js:1769-2130)",
      "disposition": "Retains its own body in full. Every `expr*` built-in this span's `compiledFnFor` names in `EXPR_PUBLIC_NAMES`/`EXPR_ARG_NAMES` and every one `evalExpressionFor`'s call to `fn(...)` (2572-2591) passes BY REFERENCE is declared here, never re-declared or wrapped by this span: `exprLayer` (1986, arg position 4 — 'layer'), `exprWiggle`/`exprNoise` (1802/1771, positions 8/9 — 'wiggle'/'noise'), `exprRandom`/`exprRandomFixed`/`exprRandomGauss`/`exprRandomGaussFixed`/`exprSeed` (1765-1768/1733, positions 10-14), `exprClamp`/`exprRemap`/`exprRemapEase`/`exprRemapEaseIn`/`exprRemapEaseOut`/`exprDegrees`/`exprRadians` (1627/1623-1626/1640/1639, positions 15-21), `exprAdd`/`exprSub`/`exprMul`/`exprDiv`/`exprLength`/`exprNormalize`/`exprDot`/`exprCross`/`exprAngleTo` (1643-1693, positions 22-30), `exprStepTime`/`exprLoopAfter`/`exprLoopBefore`/`exprToFrames`/`exprToSeconds` (1709/1909/1915/1716/1721, positions 31-35), `exprSelfControl`/`exprLayerControl` (2020/2047, positions 37-38 — 'control'/'layerControl'), `exprNumKeys` (2070) — the ONE entry in the whole 63-slot argument list that is a CALLED value (`exprNumKeys(holder, prop)`, position 43 — 'numKeys') rather than a passed function reference, a naming/shape asymmetry this span's own `evalExpressionFor` argument list creates, recorded here because it directly determines how C20k's `exprNumKeys` reaches user code, not asserted as a bug in C20k's own body — and `exprSelfAt` (1823, passed directly at position 55 — 'valueAtFrame' alias slot — and again indirectly through C20l's `aliasValueAtTime` wrapper). C20k's `exprLoopAfter`/`exprLoopBefore` are each passed TWICE in the argument list (positions 32-33 under their own names, and again at positions 39-40 in the 'compatibility aliases' block as `loopOut`/`loopIn`) — confirmed by direct read that both blocks pass the exact same function reference, not two different implementations."
    },
    {
      "owner": "C20l (issue #1236, open at authoring time, motion.js:2131-2347, comp/self views + AE compatibility aliases)",
      "disposition": "Cited by name/line only; not yet filed as an artifact, so line ranges below are direct frozen-blob reads, not cross-artifact citations. `SELF_VIEW`/`COMP_VIEW` (module-level singleton objects, declared starting ~2145/2156) are passed at positions 5-6 ('self'/'comp') and `COMP_VIEW.marker` at position 7 ('marker'). `exprContentBox` (2257-2270ish) is passed TWICE: once under its documented name at position 36 ('contentBox') and again as the AE-compatibility alias at position 60 ('sourceRectAtTime') — same duplicate-passing pattern C20k's loopAfter/loopBefore use. Every remaining alias in the 'compatibility aliases' block that is NOT a bare rename onto a C20k function is a C20l-declared wrapper: `aliasKey`/`aliasNearestKey` (2294/2290, positions 41-42), `aliasPosterizeTime` (2280, position 44), `aliasSeedRandom` (2295, position 52), `aliasValueAtTime`/`aliasVelocityAtTime`/`aliasSpeedAtTime` (2287-2289, positions 54/56-57 — each converts a seconds-based AE argument to the frame-based argument C20k's `exprSelfAt`/`exprSelfVelocity`/`exprSelfSpeed` expect, via `_num(t) * (c ? c.fps : _exprFps())`), `aliasTimeToFrames`/`aliasFramesToTime` (2300/2305, positions 58-59), `buildAliasComp`/`buildAliasLayer` (2314/2325, positions 61-62 — the ONLY two positions in the whole argument list that are conditionally `null` instead of always-a-value: `entry.needsAliasComp ? buildAliasComp() : null` / `entry.needsAliasLayer ? buildAliasLayer() : null`, gated by two booleans THIS span's own `compiledFnFor` computes at compile time via a regex test on the expression's own source text — `/\\bthisComp\\b/`/`/\\bthisLayer\\b/` against `pre + '\\n' + ex.code`, 2478-2479 — so an expression that never types `thisComp`/`thisLayer` never pays the cost of constructing either alias object). By contrast, five aliases in the SAME block are bare renames pointing at the identical C20k/pre-1769 function reference with no C20l wrapper at all: `linear`→`exprRemap`, `ease`→`exprRemapEase`, `easeIn`→`exprRemapEaseIn`, `easeOut`→`exprRemapEaseOut` (positions 45-48), `degreesToRadians`→`exprRadians`, `radiansToDegrees`→`exprDegrees` (positions 49-50), `lookAt`→`exprAngleTo` (position 51), `gaussRandom`→`exprRandomGauss` (position 53) — these AE names differ from Nemo's own vocabulary only in spelling, never in calling convention, so no C20l wrapper function exists for them at all; this asymmetry (wrapper-needed vs bare-rename) is recorded here because it is this span's own `EXPR_ARG_NAMES`/call-site pairing that exposes it, even though neither side of any individual pairing is this span's own code."
    },
    {
      "owner": "C20n (issue #1238, open at authoring time, motion.js:2635-2932, valueAtFrame/key writers/element holders)",
      "disposition": "Cited by name/line only. `valueAtFrame(ld, prop, frame)` (2639-2646) is this span's ONLY caller reachable through the normal per-frame render/animation path: it calls `rawValueAtFrame` (C20a-j/C02) unconditionally, then — only if `hasExpr(ld, prop)` (C20a-j/C02, 1338) is true — calls this span's own `evalExpressionFor(ld, prop, frame, raw)` (2642) and returns its result if not `null`, else falls through to the raw value (2645) — this IS the 'an enabled-with-error expression returns the exact rawValue' contract the issue asks to record: every error branch inside this span's `evalExpressionFor` (`return null`, four distinct sites: 2554, 2596, and both catch-block returns 2623/2628) routes back through THIS single fallback line in C20n, not through any fallback logic duplicated inside this span itself. This span never calls `hasExpr` itself (only C20n's `valueAtFrame` does, at 2641) and never calls any C20n key writer (`ensureElementHolder`/`meshHolder`/`toggleAnimated`-family — grep-confirmed absent from this span)."
    },
    {
      "owner": "engine-bridge.js (buildSceneJson) / motion.js sibling-covered render chain (computeMotionMat, layerMotionAt/elementMotionAt, past line 2932)",
      "disposition": "Confirmed reachable on the render path, not UI-only: `buildSceneJson` calls `SMMotion.layerMotionAt`/`SMMotion.elementMotionAt` which call `computeMotionMat` (motion.js:2933, sibling-covered) which calls `valueAtFrame` (2639, C20n) which calls this span's own `evalExpressionFor` (2542) for every property carrying an enabled expression — once per frame, per property, on export and packaged Tauri playback as well as the editor. This span's `evalExpressionFor` is therefore the single choke point through which every compiled user expression actually runs on that hot path; its own internal cost (recompile-cache lookup via `compiledFnFor`, `_ectx` object-literal allocation every call, the 63-argument positional `fn(...)` invocation, `normalizeExprResult`'s per-call array/scalar reshape) is paid once per property per frame regardless of which built-ins the expression's own text happens to call."
    },
    {
      "owner": "C20f (motion-transform-expression-panel, merged) / expression controls UI (`exprControls`, C20/C13, motion.js:279-298)",
      "disposition": "Retains the expression editor panel and the `addExprControl`/`genControlKey`-adjacent declaration UI in full. C20f is the ONE consumer of this span outside the render path, and it reaches this span's private functions DIRECTLY (motion.js is a single IIFE, so C20f's code shares the same closure) rather than through the public `SMMotion` object: `evalExpressionFor` is called a second time at motion.js:9040 inside `paintResult()`, a live readout of 'what the expression actually produces at the playhead' repainted on every commit/frame-change, independently of the render-path call C20n's `valueAtFrame` makes for the SAME property at the SAME frame — this span therefore runs TWICE per animated-expression property per frame when both the Motion panel is open AND the render/export path evaluates the same property, once for the panel's own display and once for the actual rendered value, with no cache sharing between the two call sites beyond `compiledFnFor`'s own code/pre-keyed cache (which IS shared, since both call sites resolve through the same `holder._exprCompiled[prop]`). `exprGlobals()` is also called directly from C20f (motion.js:9054/9058, the project-wide-preamble button's label and its `prompt()` seed value) and `SMMotion.setExprGlobals` (the public API this span's `exprGlobals()` reads `state.exprGlobals` back from) is C20f's own commit path for editing it (motion.js:13729-13736, past this span, clears every layer's AND every element's `_exprCompiled` cache — see boundaries for why this bulk clear is defensive, not required, given `compiledFnFor`'s own `pre` identity check)."
    },
    {
      "owner": "C20/C13 (`exprControls` declaration UI) and the pickwhip/inline-editor commit path (`setExpressionCode` 11579, `SMMotion.exprSnapshotFor`/`applyExprCode` 13621-13639)",
      "disposition": "Cited by name/line only, retain their own bodies in full. Both `setExpressionCode` and `applyExprCode` mutate `ex.code`, set `ex.lastError = null; ex.errorLine = -1;`, and `if (holder._exprCompiled) delete holder._exprCompiled[prop];` — the SAME three-field/cache-clear pattern this span's own `compiledFnFor` writes on a successful compile (2427/2460) and clears on a syntax failure (2469-2471). Neither writer is INSIDE this span, but both interact with the identical cache object this span's `compiledFnFor` owns the read/write contract for."
    },
    {
      "owner": "rig-widget.js (`layerControl(uid,name)`, CLAUDE.md §13)",
      "disposition": "Cited by name only, retains its own body in full. `layerControl` is the internal/rig-widget-facing counterpart of C20k's `exprLayerControl` (the sandbox-visible name at position 38 of this span's argument list) — no reference to `layerControl` appears anywhere in this span's own 2348-2634 (grep-confirmed); the cost-asymmetry measurement CLAUDE.md §13 records belongs to C20k's own reconciliation, not repeated here."
    },
    {
      "owner": "image-mesh.js / image-mesh-bridge.js (mesh holders, CLAUDE.md §12ter)",
      "disposition": "Cited by name only, retain their own bodies in full. No `meshId`/`elementMotion`/mesh-holder reference exists anywhere in this span (grep-confirmed) — `compiledFnFor`/`evalExpressionFor` operate on whatever `holder` object they are handed (a layer OR an element holder, per C20n's `ensureElementHolder`/`meshHolder`, both past this span) without any holder-shape branching of their own."
    },
    {
      "owner": "nemo-script.js",
      "disposition": "Cited by name/line only. `globals` (nemo-script.js:548-549) is a scripting-console property whose getter reads `state.exprGlobals` directly (the SAME field this span's own `exprGlobals()`, 2355, reads) and whose setter delegates to `SMMotion.setExprGlobals` (motion.js past this span) — the only src/js file outside motion.js found to touch expression-preamble state at all (grep-confirmed across 164 files)."
    },
    {
      "owner": "NemoOpacityDomain (application module, unrelated)",
      "disposition": "Cited by name only per the issue's own exclusion list; grep-confirmed this span references no `NemoOpacityDomain` symbol and no `src/js/domain/animation/*` symbol at all — unlike C20k's own noise/wiggle built-ins, this span's compilation/evaluation orchestration has no application-module dependency of its own."
    },
    {
      "owner": "src/js/i18n.js",
      "disposition": "Retains the locale tables in full — cited by name/line only. This span makes exactly SEVEN direct `SM.t(...)` calls: `exprErrorSyntaxPrefix`/`exprErrorSyntaxGlobalsSuffix` (2469-2470), `exprErrorTooDeep` (2551), `exprErrorMustReturnNumberOrXY`/`exprErrorMustReturnNumber` (2594, ternary on `PROP_DIM[prop] >= 2`), `exprErrorTimeout` (2612), `exprToastTimeout` (2619) — all seven confirmed present exactly four times each in src/js/i18n.js (`grep -o \"<key>:\" src/js/i18n.js`), once per locale block (en/fr/ja/es); no i18n gap exists for this span's own strings, same clean result C20k found for its own five error keys."
    },
    {
      "owner": "timeline.js",
      "disposition": "Retains its own body in full — cited by name/line only. `showToast` (7447, C19w) is this span's only cross-file UI call, reached inside `evalExpressionFor`'s timeout branch guarded by `if (window.showToast)` (2619) and deferred one tick via `setTimeout(...,0)` (2617-2622), debounced across the whole session by this span's own module-level `_timeoutToastPending` flag (2634) so a storm of timed-out expressions in the same frame produces at most one toast. `exportJSON` (2117) and `importJSON` (2410) carry `l.expressions` verbatim into and out of the saved project file — confirmed by direct read that the exported layer object literal includes `expressions:l.expressions` with no field-level filtering, so this span's OWN bookkeeping writes onto `holder.expressions[prop]` (`.lastError`, `.errorLine`, and the timeout branch's `.enabled = false`) ARE persisted to the project file exactly as they stand at save time — see boundaries for the full document-write inventory. `renderLayerList`/`renderTimeline` (called from this span's timeout branch, 2620) are cited by name only, retained in full."
    },
    {
      "owner": "P03",
      "disposition": "This bounded census neither completes P03 nor certifies product behavior."
    }
  ],
  "boundaries": [
    "Line 2347 closes C20l's own trailing blank line (before-2348 bucket); this artifact begins at the `// ---- compilation` section header on line 2348, immediately preceding `exprGlobals`'s declaration at 2355. Line 2490 closes this artifact's own `compiledFnFor`; line 2491 opens this artifact's own `normalizeExprResult` lead-in comment; line 2634 is this artifact's own trailing `var _timeoutToastPending = false;`; line 2635 opens C20n's own `// Public wrapper...` lead-in comment for `valueAtFrame`. All four boundary lines confirmed by direct `sed -n` read of the frozen blob.",
    "Both of the issue's own suggested packet cuts land exactly on a section header or a function's own lead-in comment in the frozen blob (2348, 2491) — re-verified with `sed -n` for both cut points before accepting the table; no correction was needed, and no finer internal split was taken (the issue permits splitting at internal lead-ins but does not require it, and neither packet has an internal `// ----` header of its own — `compiledFnFor`'s try/catch/multi-statement-fallback structure and `evalExpressionFor`'s depth/budget/timeout logic are each single functions with internal comments, not separately headed sub-sections).",
    "EXPR_PUBLIC_NAMES (motion.js:2360-2376, 38 names, VERBATIM) with the bound implementation reached at each of the 38 matching positions (1-38) of evalExpressionFor's own `fn(...)` call (2572-2591): 'time'->_ectx.time (this span's own context field, position 1), 'frame'->_ectx.frame (position 2), 'value'->rawValue (evalExpressionFor's own 4th parameter, reshaped `rawValue.length===1 ? rawValue[0] : rawValue.slice()`, position 3), 'layer'->exprLayer (C20k:1986, position 4), 'self'->SELF_VIEW (C20l, position 5), 'comp'->COMP_VIEW (C20l, position 6), 'marker'->COMP_VIEW.marker (C20l, position 7), 'wiggle'->exprWiggle (C20k:1802, position 8), 'noise'->exprNoise (C20k:1771, position 9), 'random'->exprRandom (C20a-j:1765, position 10), 'randomFixed'->exprRandomFixed (C20a-j:1766, position 11), 'randomGauss'->exprRandomGauss (C20a-j:1767, position 12), 'randomGaussFixed'->exprRandomGaussFixed (C20a-j:1768, position 13), 'seed'->exprSeed (C20a-j:1733, position 14), 'clamp'->exprClamp (C20a-j:1627, position 15), 'remap'->exprRemap (C20a-j:1623, position 16), 'remapEase'->exprRemapEase (C20a-j:1624, position 17), 'remapEaseIn'->exprRemapEaseIn (C20a-j:1625, position 18), 'remapEaseOut'->exprRemapEaseOut (C20a-j:1626, position 19), 'degrees'->exprDegrees (C20a-j:1640, position 20), 'radians'->exprRadians (C20a-j:1639, position 21), 'add'->exprAdd (C20a-j:1643, position 22), 'sub'->exprSub (C20a-j:1644, position 23), 'mul'->exprMul (C20a-j:1645, position 24), 'div'->exprDiv (C20a-j:1651, position 25), 'length'->exprLength (C20a-j:1664, position 26), 'normalize'->exprNormalize (C20a-j:1672, position 27), 'dot'->exprDot (C20a-j:1657, position 28), 'cross'->exprCross (C20a-j:1681, position 29), 'angleTo'->exprAngleTo (C20a-j:1693, position 30), 'stepTime'->exprStepTime (C20a-j:1709, position 31), 'loopAfter'->exprLoopAfter (C20k:1909, position 32), 'loopBefore'->exprLoopBefore (C20k:1915, position 33), 'toFrames'->exprToFrames (C20a-j:1716, position 34), 'toSeconds'->exprToSeconds (C20a-j:1721, position 35), 'contentBox'->exprContentBox (C20l:2257, position 36), 'control'->exprSelfControl (C20k:2020, position 37), 'layerControl'->exprLayerControl (C20k:2047, position 38). This is the documented, 'did you mean'-eligible vocabulary in full.",
    "EXPR_ARG_NAMES (motion.js:2377-2386, VERBATIM: EXPR_PUBLIC_NAMES.concat of 24 alias names plus the internal '__tick', 63 total) with the bound implementation at each of the 25 remaining positions (39-63) of the SAME `fn(...)` call: 'loopOut'->exprLoopAfter (C20k, position 39, SAME reference as position 32), 'loopIn'->exprLoopBefore (C20k, position 40, SAME reference as position 33), 'key'->aliasKey (C20l:2294, position 41), 'nearestKey'->aliasNearestKey (C20l:2290, position 42), 'numKeys'->exprNumKeys(holder,prop) (C20k:2070, position 43 — a CALLED VALUE at bind time, the one entry in the whole list that is not a passed function reference), 'posterizeTime'->aliasPosterizeTime (C20l:2280, position 44), 'linear'->exprRemap (C20a-j, position 45, bare rename, SAME reference as position 16), 'ease'->exprRemapEase (position 46, SAME as 17), 'easeIn'->exprRemapEaseIn (position 47, SAME as 18), 'easeOut'->exprRemapEaseOut (position 48, SAME as 19), 'degreesToRadians'->exprRadians (position 49, SAME as 21), 'radiansToDegrees'->exprDegrees (position 50, SAME as 20), 'lookAt'->exprAngleTo (position 51, SAME as 30), 'seedRandom'->aliasSeedRandom (C20l:2295, position 52), 'gaussRandom'->exprRandomGauss (C20a-j, position 53, SAME as 12), 'valueAtTime'->aliasValueAtTime (C20l:2287, position 54), 'valueAtFrame'->exprSelfAt (C20k:1823, position 55 — this sandbox name SHADOWS, but is never the same symbol as, the outer module-scope function also named `valueAtFrame` that C20n declares at 2639: a compiled expression's own `valueAtFrame(...)` call always reaches C20k's self-only, RAW-reading `exprSelfAt`, never C20n's per-property evaluated-value reader, because `new Function`'s closed parameter list contains no other binding for that name), 'velocityAtTime'->aliasVelocityAtTime (C20l:2288, position 56), 'speedAtTime'->aliasSpeedAtTime (C20l:2289, position 57), 'timeToFrames'->aliasTimeToFrames (C20l:2300, position 58), 'framesToTime'->aliasFramesToTime (C20l:2305, position 59), 'sourceRectAtTime'->exprContentBox (C20l:2257, position 60, SAME reference as position 36), 'thisComp'->`entry.needsAliasComp ? buildAliasComp() : null` (C20l:2314, position 61 — the ONE of two positions computed at CALL time rather than passed as a stable reference, gated by a boolean this span's own compiledFnFor computes at COMPILE time via `/\\bthisComp\\b/.test(body)`, 2478), 'thisLayer'->`entry.needsAliasLayer ? buildAliasLayer() : null` (C20l:2325, position 62, same pattern via `/\\bthisLayer\\b/.test(body)`, 2479), '__tick'->_exprTick (C20a-j:1397, position 63 — the ONLY entry in EXPR_ARG_NAMES not `.concat`-ed from a documented alias, injected into loop bodies by instrumentLoops at compile time, never typed by a user).",
    "The compile cache: keyed by `(holder, prop)` as the LOOKUP path (`holder._exprCompiled[prop]`, an object keyed by property name, one such object per holder — a layer `ld` or an element/mesh holder, whichever object `compiledFnFor`/`evalExpressionFor` are handed) and by `(ex.code, exprGlobals())` as the VALIDITY check: `compiledFnFor` (2389-2394) returns the cached entry only if BOTH `holder._exprCompiled[prop].code === ex.code` AND `.pre === pre` (the current preamble) — so editing EITHER the expression's own text OR the project-wide preamble self-invalidates the cache on the very next `compiledFnFor` call, with no explicit clear required. `holder._exprCompiled` lives directly on the holder object (never a WeakMap) — a plain property, confirmed absent from every export/import whitelist (`exportJSON`, timeline.js:2117; `importJSON`, 2410) and from `duplicateLayer`'s field list (app.js:3420/3424/3447/3649/3879, none of which name `_exprCompiled`), so a freshly-loaded or freshly-duplicated holder never carries a stale compiled function forward — it is rebuilt from `ex.code`/the current preamble on first read, purely as a byproduct of `_exprCompiled` being absent (undefined), not because any loader/duplicator explicitly clears it. Beyond the self-invalidating identity check, THREE call sites outside this span proactively `delete holder._exprCompiled[prop]` anyway on a code edit (`setExpressionCode` 11589, `SMMotion.applyExprCode` 13637, and this span's OWN evalExpressionFor timeout branch, 2614) and ONE site (`SMMotion.setExprGlobals`, 13733-13734) bulk-clears `_exprCompiled` on every layer AND every element on a preamble edit — all four are DEFENSIVE, not load-bearing: `compiledFnFor`'s own `.code`/`.pre` comparison already forces a recompile the next time it is called after either kind of edit, verified by reading every branch of the comparison at 2391-2394 against every writer of `ex.code`/`state.exprGlobals` found by grep. The one place the delete IS meaningfully protective is this span's own timeout branch (2614): it runs alongside `ex.enabled = false` (2611), which means C20n's `hasExpr` (1338) will refuse to call `evalExpressionFor` again until the user manually re-enables the expression — at which point `ex.code` is UNCHANGED, so without the delete the stale (but not actually broken) compiled entry would simply be reused; deleting it here is therefore a clean-slate choice, not a correctness requirement, since the timeout was a RUNTIME infinite loop, not a symptom of a bad compile.",
    "`compiledFnFor`'s two-attempt compile strategy, in the exact order attempted: (1) `build(preSrc, codeSrc)` (2415ish, 'attempt A') wraps the (loop-guarded, then un-guarded on a SyntaxError retry) preamble+code as a single `return (...)` expression — the ONLY form ever tried before 2026-08-16, so `value + wiggle(2, 10)` keeps compiling exactly as it always did; (2) on ANY exception from attempt A, `buildBody(preSrc, codeSrc)` ('attempt B') wraps the same text as a plain function BODY requiring the user's own explicit `return` — tried only because attempt A failed, so an existing single-expression project never falls through to this path. Each attempt independently retries once more with the UN-instrumented `pre`/`ex.code` (`preSrc !== pre || codeSrc !== ex.code`, i.e. only if `instrumentLoops` actually changed something) if the guarded version throws a `SyntaxError` — 'a working expression without the loop guard beats a broken one with it' per the source's own comment (2400-2401) — setting `guarded = false` on the compiled entry when this retry succeeds. Only if attempt B ALSO throws does `compiledFnFor` give up: `fn = null`, `ex.errorLine = -1` (no line number is ever computed for a SyntaxError, because `new Function`'s own stack points at the Function constructor's call site, not into the handed source — a deliberate choice, not a missing feature, per the source's own comment at 2461-2465), and `ex.lastError` is built from `exprErrorSyntaxPrefix` + the raw `e2.message` + (conditionally) `exprErrorSyntaxGlobalsSuffix` when a non-empty preamble was in play.",
    "The compiled entry's shape (`entry`, the object cached and returned by `compiledFnFor`, 2482-2489, VERBATIM): `code`/`pre` (the cache-validity pair above), `fn` (the compiled function, or `null` on total syntax failure), `prefixLines` (how many wrapper lines precede the user's own line 1 in the SOURCE HANDED TO `new Function` — `1 + preLines + 1` for the return-expression form (the `\"use strict\";` line plus the preamble plus the `return (` line), or `1 + preLines` for the function-body form, no `return (` line), used by `_formatRuntimeError`/`_lineFromStack` (C20a-j) to translate a reported stack line back into the textarea's own line numbering), `userLines` (line count of `ex.code` alone, via `_countLines`), `exprMode` (true for the return-expression form, false for the function-body fallback — read by `_lineFromStack`), `guarded` (whether the loop-instrumented source is the one actually running, vs. the un-instrumented retry), `needsAliasComp`/`needsAliasLayer` (two booleans, each a regex test — `/\\bthisComp\\b/`/`/\\bthisLayer\\b/` — against `(pre ? pre + '\\n' : '') + (ex.code || '')`, computed ONCE at compile time so an expression that never types `thisComp`/`thisLayer` never pays the cost of constructing either alias object on every evaluation).",
    "`normalizeExprResult`'s dim resolution: `PROP_DIM[prop]` (C02, 506) for ordinary properties, with a deliberate fallback `if (dim === undefined && prop.indexOf('vtx') === 0) dim = 2` (2511) for per-vertex properties (`vtx0`, `vtx1`, ...), which are absent from the fixed `PROP_DIM` table by design (mirroring `PROP_DEFAULT`'s own absence for the same family) — the source's own comment (2501-2510) states this fallback FIXES a prior total failure: without it `dim` was `undefined`, `result.length >= undefined` is always `false`, and every vertex expression silently returned `null` regardless of what it computed. Every component of an array result must be finite (`allFinite`, 2521-2524): a bare-number result already rejected non-finite values before this fix, but the ARRAY branch did not, so `[0/0, 100]` — valid JavaScript, no thrown error — reached the scene JSON as a literal NaN, which `JSON.stringify` turns into `null`, which the Rust engine then rejected outright ('invalid type: null, expected f64') on EVERY render; returning `null` from `normalizeExprResult` instead routes a bad value into the SAME 'must return a number' error path already used for other malformed results, keeping the one bad property from taking the whole engine down for the session — CLAUDE.md's own generalization of the Option<String>-incident family. Array results: `result.length >= dim` slices to exactly `dim` elements (2527) and finite-checks the slice; a single-element array `[n]` on a 2+-dim property broadcasts `n` to every axis (`fill(n)`, 2528-2529) — the SAME forgiving reading a bare number gets. Non-array, non-finite-number, or under-length-array results all return `null`.",
    "The full `_ectx` field-read inventory this span's OWN `evalExpressionFor` writes into `_ectx` (2564-2568) and what each field feeds: `holder`/`prop` (identity of the property being evaluated — read by essentially every C20k built-in that samples 'this' property), `frame`/`time` (`frame / fps`, the evaluation frame in both units — `time` is explicitly the EVALUATION frame's time, per C20k's own comment, never `state.currentFrame`), `fps` (this call's own `_exprFps()` snapshot, so a fps change mid-evaluation-chain cannot retroactively change an already-in-flight sample), `seed` (from `ensureExprSeed(holder)`, C20a-j, read by C20k's `exprNoise`/`_wiggleAxis`), `rngSeed`/`rngTimeless`/`rngCounter` (the seeded-draw family C20a-j's `exprRandom*` consume — `rngSeed = seed + _propSeedOffset(prop)`, `rngCounter` starts at 0 and is advanced per draw by C20a-j's own `_rand01`, never read or written by this span itself beyond the initial `_ectx` literal), `_resolved` (initialized to `undefined`, read/written only by C20l's own `_selfResolved`/`_selfLd` lazy-resolution cache — this span never reads or writes it beyond the initial object-literal field).",
    "`_ectx`'s lifetime: set once per `evalExpressionFor` call (2563), the OLD value saved to a local `prev` (2562) immediately beforehand, and restored via `_ectx = prev` in a `finally` block (2630) that runs on every exit path — normal return, the two `return null` error branches inside the `try`, and both branches of the `catch`. This save/restore pattern IS the re-entrancy mechanism: a cross-layer or cross-property reference (C20k's `exprLayer`/`_sampleOther`/`_controlValue`, reached through C20n's `valueAtFrame` calling back into THIS span's `evalExpressionFor` for a DIFFERENT holder/prop) pushes a new `_ectx` for the inner evaluation and pops back to the outer one when it returns, so nested evaluations never see each other's context, bounded by `_exprDepth`/`EXPR_MAX_DEPTH` (C20a-j) rather than by `_ectx` itself.",
    "The wall-clock/step budget, in full: `EXPR_BUDGET_MS = 60` (C20a-j, 1393) is applied ONLY at the outermost call (`_exprDepth === 0`, 2559) as `_exprDeadline = Date.now() + EXPR_BUDGET_MS`, shared by every nested cross-reference hop in the same evaluation chain rather than reset per hop — the source's own comment (2556-2558) states this is deliberate. `_exprTick` (C20a-j, 1397-1400) is the ENFORCEMENT point: it only actually calls `Date.now()` every 1024th invocation (`(++_tickCount) & 1023`), and only THEN checks the deadline, throwing `new Error(EXPR_TIMEOUT_TAG)` if exceeded — `_tickCount` is a single MODULE-LEVEL counter shared across every expression evaluation in the whole session, not reset per call or per property, so the '1024th call' boundary drifts across unrelated expressions rather than restarting at 0 for each one; this does not weaken the guarantee (the deadline check still eventually fires within a bounded number of extra calls after the true deadline) but means the exact call at which a runaway loop trips is not reproducible from a fresh `_tickCount` for a given expression in isolation. Depth is bounded separately by `EXPR_MAX_DEPTH = 8` (C20a-j, 1367): this span's own `evalExpressionFor` checks `_exprDepth >= EXPR_MAX_DEPTH` BEFORE doing any work (2550), turning a would-be stack overflow from a cross-layer reference cycle into the ordinary 'fall back to raw value' outcome via `ex.lastError = SM.t('exprErrorTooDeep')` and a `return null` — cheaper and safer than letting the recursion actually reach a JS stack limit.",
    "The error contract, three DISTINCT states this span's own `evalExpressionFor` can leave on `holder.expressions[prop]`, none of them stored anywhere except that one object: (1) DEPTH-EXCEEDED (2550-2555): `ex.lastError = exprErrorTooDeep`, `ex.errorLine = -1`, `ex` pushed onto `_depthTripped` (C20a-j) so an outer hop in the SAME outermost evaluation that later succeeds does not silently overwrite this warning at 2604 (`if (_depthTripped.indexOf(ex) < 0) { ex.lastError = null; ex.errorLine = -1; }` — a reference cycle resolves to SOMETHING once the innermost hop falls back to its raw value, so without this check the cap's own warning would be erased by the very evaluation it protected, making a cycle look healthy while producing an arbitrary number); (2) MUST-RETURN-A-NUMBER (2592-2597): triggered when `normalizeExprResult` returns `null` (non-finite, wrong shape, or an object/string/undefined result), `ex.lastError` set to `exprErrorMustReturnNumberOrXY` or `exprErrorMustReturnNumber` depending on `PROP_DIM[prop] >= 2`, `ex.errorLine = -1` (a bad RETURN VALUE has no useful source line to point at — the expression ran to completion); (3) RUNTIME EXCEPTION (2606, 2624-2628): the compiled function itself threw — split into TIMEOUT (`e.message === EXPR_TIMEOUT_TAG`, 2607-2623: `ex.enabled = false` PERMANENTLY disables the expression until manually re-enabled, `ex.lastError = exprErrorTimeout`, the compiled-cache entry is deleted, and a session-debounced toast fires via `_timeoutToastPending`) and ANY OTHER exception (2625-2628: `_formatRuntimeError` builds a message with a 'did you mean' suggestion via `_closestName` against `EXPR_PUBLIC_NAMES` when the message matches `/... is not defined/`, PLUS a real line number recovered from the stack via `_lineFromStack` using the compiled entry's own `prefixLines`/`userLines`/`exprMode`, translated back to the user's own textarea line numbering). All three states share the exact same TWO fields (`lastError`, `errorLine`) that C20j's `propHasExpressionError` (motion.js:629-630) reads to decide whether a property row shows an error indicator, and that C20f's own editor panel (motion.js:9236-9270) reads to paint the inline error text and recover a clickable line number — this span never distinguishes compile-time from runtime from timeout errors at the READ side; only the WRITE side (this span, plus `compiledFnFor`'s own syntax-error branch) knows which kind produced a given message. The fallback rule for every one of these three states is identical and lives one line past this span's own boundary, in C20n's `valueAtFrame` (2645): `return raw` — an enabled-with-error expression always shows the EXACT raw (pre-expression) value it would have shown before expressions existed, never a blank, NaN, or frozen property.",
    "Document writes reachable from this span, exhaustively: every assignment target other than a local `var`/parameter/array-literal was scanned across every non-comment line of 2348-2634. The ONLY persisted-object fields ever written are `holder.expressions[prop].lastError`, `.errorLine`, and — ONLY in the timeout branch — `.enabled` (set to `false`, 2611); `holder._exprCompiled`/`holder._exprCompiled[prop]` are also written (2487-2488, 2614) but are a RUNTIME CACHE, not persisted data (see the compile-cache finding above for the export/import/duplicate-whitelist absence proof). The module-level `_ectx`/`_exprDepth`/`_exprDeadline`/`_depthTripped`/`_timeoutToastPending` writes are all transient in-memory evaluation state, never touching `holder`/`state.layers`/any exported field. `.lastError`/`.errorLine`/`.enabled` ARE persisted: `timeline.js`'s `exportJSON` (2117) includes `expressions:l.expressions` in the exported layer object with no field-level filtering — so a project saved immediately after a timeout (which sets `.enabled = false`) or a runtime error (which sets `.lastError`/`.errorLine`) round-trips that exact state through `importJSON` (2410, `state.layers[idx].expressions=ld.expressions`) verbatim; a disabled-by-timeout expression therefore reopens still disabled, with its last error message intact, rather than silently re-arming and immediately re-tripping the same timeout on the very next frame evaluated after load. No call to `pushUndo`, `saveActiveLayerFrame` or `saveAllLayerFrames` occurs anywhere in this span (grep-verified) — this span's own writes are captured by undo only incidentally, whenever a SIBLING writer (`setExpressionCode`/`applyExprCode`, both of which call `pushUndo()` themselves before mutating `ex.code`) happens to have already pushed a history entry for the same holder for an unrelated reason.",
    "Render-path reachability, stated plainly per the issue's own request: this span IS on the render path. `computeMotionMat` (motion.js:2933, sibling-covered) -> `valueAtFrame` (2639, C20n) -> this span's own `evalExpressionFor` (2542) -> the compiled user expression (built by this span's own `compiledFnFor`, 2388) -> `normalizeExprResult` (2500, this span) on the way back out. This sequence runs once per frame per animated property that carries an enabled expression, on every render/export/scrub, not only inside the Motion timeline editor — the SAME reachability chain C20k's own boundaries documented for the built-ins this span's argument list exposes to that compiled function."
  ],
  "areas": [
    {
      "area": "expression-compilation",
      "packets": [
        {
          "id": "C20m.sandbox-surface-and-compile",
          "files": [
            "src/js/motion.js:2348-2490"
          ],
          "coverage_ranges": [
            [
              2348,
              2490
            ]
          ],
          "classification": {
            "code": 91,
            "comment": 52,
            "whitespace": 0
          },
          "symbols": [
            "exprGlobals",
            "EXPR_PUBLIC_NAMES",
            "EXPR_ARG_NAMES",
            "_countLines",
            "compiledFnFor"
          ],
          "signatures": [
            "exprGlobals() -> string",
            "_countLines(s) -> number",
            "compiledFnFor(holder, prop) -> {code, pre, fn, prefixLines, userLines, exprMode, guarded, needsAliasComp, needsAliasLayer}"
          ],
          "responsibility": "Section header (2348) + 6-line lead-in (2349-2354) states the preamble design: a project-wide preamble is plain statements evaluated in the SAME scope as the expression body, cached against BOTH the expression's own code and the preamble so editing the preamble recompiles everything rather than leaving stale compiles behind. `exprGlobals()` (2355): returns `state.exprGlobals || ''`. 4-line lead-in (2356-2359) states the sandbox's OWN design rule: `new Function` with an explicit, closed parameter list, no `window`/`document`/`state` reachable from inside a compiled expression. `EXPR_PUBLIC_NAMES` (2360-2376, 38 entries, VERBATIM — see boundaries): the documented vocabulary AND the only pool `_closestName` (C20a-j) draws its 'did you mean' suggestions from; two named entries ('control', 'layerControl', 2371/2375) carry their own inline comments explaining why they belong in the documented pool rather than the compatibility-alias block below. `EXPR_ARG_NAMES` (2377-2386): `EXPR_PUBLIC_NAMES.concat([...])` with 24 undocumented AE-compatibility alias names plus the internal `__tick` (63 total) — a 3-line comment (2378) marks the alias block, a 1-line comment (2384) marks `__tick` as the runaway-guard `instrumentLoops()` injects, never typed by a user. `_countLines(s)` (2387): `s ? s.split('\\n').length : 0`. `compiledFnFor(holder, prop)` (2388-2490): looks up `ex = holder.expressions[prop]` and the current preamble `pre = exprGlobals()`; returns the cached `holder._exprCompiled[prop]` unchanged if BOTH `.code === ex.code` and `.pre === pre` (2391-2394); otherwise instruments both preamble and code through `instrumentLoops` (C20a-j) for the loop-tick guard, then attempts TWO compile strategies in order (see boundaries): a single-expression `return (...)` wrapper first (the only form used before 2026-08-16), falling back to a multi-statement function-BODY wrapper (requiring the user's own explicit `return`) only if the first throws — each strategy independently retries with the un-instrumented source on a `SyntaxError` if instrumentation changed anything, and total failure of both strategies clears `fn` to `null` and records a syntax error with no line number (by design — `new Function`'s own stack cannot be mapped back to the handed source). On any successful compile, builds and caches a fresh `entry` object (2482-2489, full field list in boundaries) computing `needsAliasComp`/`needsAliasLayer` via a regex test against the combined preamble+code text, so an expression that never types `thisComp`/`thisLayer` never pays for constructing either alias object at evaluation time.",
          "cache_contract": "Lookup key: `holder._exprCompiled[prop]`, a plain property on whatever holder object is passed in (never a WeakMap). Validity key: `(ex.code, exprGlobals())` — BOTH must match the cached entry's `.code`/`.pre` for a cache hit; either changing invalidates on the very next call with no explicit clear required, though three external writers and this span's own timeout branch proactively delete the entry anyway (defensive, not load-bearing — full citation in boundaries). Never persisted (absent from exportJSON/importJSON/duplicateLayer field lists) and never touched by save/undo/redo directly — it exists purely to avoid re-running `new Function` on every frame for an unchanged expression.",
          "argument_coercion": "`ex.code`/`pre` are used as raw strings, never number-coerced. `instrumentLoops` operates on the TEXT (a source-to-source rewrite that inserts `__tick()` calls into loop bodies), not on any parsed value. `_countLines` coerces via `s ? ... : 0` — a falsy (empty/undefined) string yields 0, never NaN.",
          "return_shape": "`compiledFnFor` always returns an object (the cached or freshly-built `entry`) — never `null` even on total compile failure (in that case `entry.fn` alone is `null`, checked by evalExpressionFor at 2545).",
          "determinism": "Pure function of `(holder.expressions[prop].code, exprGlobals())` given a fixed cache state — the SAME code+pre pair always compiles to behaviorally identical output (module-level `new Function` compilation has no external randomness); the CACHE itself is the only stateful element, and it is read-then-conditionally-written, never read while being written.",
          "raw_vs_evaluated": "N/A — this packet never samples a track; it only compiles text into a callable function.",
          "callers": "Zero calls to `compiledFnFor(` anywhere in src/js outside this span's own `evalExpressionFor` (2544) — its ONLY caller (grep-confirmed across src/js/*.js). `exprGlobals()` has three callers: this span's own `compiledFnFor` (2390, internal) and C20f's expression-panel button label/prompt seed (motion.js:9054/9058, past this span). `EXPR_PUBLIC_NAMES` is read by C20a-j's `_closestName` (1524-1525, a forward reference resolved because the whole IIFE's declarations exist before any expression evaluates) and by this span's own `compiledFnFor` indirectly through `EXPR_ARG_NAMES` (2396, `var args = EXPR_ARG_NAMES`). `EXPR_ARG_NAMES` has exactly that one reader.",
          "external_calls": [
            "instrumentLoops(src) — motion.js:1426 (C20a-j/C02), called twice per compile attempt (preamble and code).",
            "_closestName is NOT called from this packet directly — it is called from evalExpressionFor's catch block (packet 2) and from C20a-j's own _formatRuntimeError.",
            "SM.t('exprErrorSyntaxPrefix'/'exprErrorSyntaxGlobalsSuffix') — src/js/i18n.js, on total compile failure only."
          ],
          "effects": "Writes `holder._exprCompiled[prop]` (a runtime cache, not persisted — see cache_contract) and `ex.lastError`/`ex.errorLine` (persisted bookkeeping, cleared to `null`/`-1` on any successful compile attempt, 2427/2460; set on total syntax failure, 2469-2471). No other object is mutated."
        }
      ]
    },
    {
      "area": "expression-evaluation",
      "packets": [
        {
          "id": "C20m.normalize-and-evaluate",
          "files": [
            "src/js/motion.js:2491-2634"
          ],
          "coverage_ranges": [
            [
              2491,
              2634
            ]
          ],
          "classification": {
            "code": 92,
            "comment": 52,
            "whitespace": 0
          },
          "symbols": [
            "normalizeExprResult",
            "evalExpressionFor",
            "_timeoutToastPending"
          ],
          "signatures": [
            "normalizeExprResult(result, prop) -> number | number[] | null",
            "evalExpressionFor(holder, prop, frame, rawValue) -> number | number[] | null"
          ],
          "responsibility": "9-line lead-in (2491-2499) states the design rationale: normalize against `PROP_DIM[prop]` (a table, not the literal cases 1/2) because PROP_DIM has never actually been capped at 2 ('fillColor' is a 4-channel track), so an old `dim === 2` branch would silently truncate a 4-dimension property's result. `normalizeExprResult(result, prop)` (2500-2541): resolves `dim` from `PROP_DIM[prop]`, with a `vtx*`-prefixed fallback to `dim = 2` for per-vertex properties absent from the fixed table (a documented bug-fix — see boundaries); requires every component of an array result to be FINITE (`allFinite`, rejecting `[0/0, 100]`-style NaN payloads that would otherwise reach the scene JSON as a silent `null` and crash the Rust engine); accepts an array of exactly `dim`+ elements (sliced to `dim`), a single-element array on a 2+-dim property (broadcast to every axis), or a finite bare number (also broadcast); returns `null` for anything else (wrong shape, non-finite, object/string/undefined). `evalExpressionFor(holder, prop, frame, rawValue)` (2542-2633): looks up `ex`/the compiled `entry` via `compiledFnFor` (packet 1), bails to `null` if compilation failed; enforces the `EXPR_MAX_DEPTH` cross-reference recursion cap and the shared outermost-call wall-clock budget (full mechanics in boundaries); constructs and installs a fresh `_ectx` (saved/restored around the call via a local `prev`); invokes the compiled function with all 63 sandbox arguments positionally (2572-2591, full mapping in boundaries); runs the raw result through `normalizeExprResult`, erroring ('must return a number'/'...or XY') on `null`; on success, clears the depth-tripped warning only if THIS `ex` was not itself one of the entries the depth cap flagged during the outermost evaluation; catches thrown errors, splitting the TIMEOUT tag (disable the expression, message, debounced toast) from any other runtime exception (`_formatRuntimeError`, 'did you mean' + recovered line number); restores `_ectx`/`_exprDepth` in a `finally` on every exit path. `_timeoutToastPending` (2634): a module-level boolean, the session-wide debounce flag for the timeout toast.",
          "ectx_contract": "Written ONLY here (`_ectx = {holder, prop, frame, time: frame/fps, fps, seed, rngSeed: seed + _propSeedOffset(prop), rngTimeless: false, rngCounter: 0, _resolved: undefined}`, 2563-2569); full per-field cross-citation to C20k/C20l readers in boundaries. Lifetime: save-prev/assign/restore-in-finally around the single `fn(...)` call and everything it can transitively re-enter (cross-layer/control reads back through C20n's valueAtFrame calling this same function for a different holder/prop) — this save/restore IS the re-entrancy mechanism; depth is bounded separately by `_exprDepth`/`EXPR_MAX_DEPTH`.",
          "error_contract": "Three distinct states written onto `holder.expressions[prop]`, never elsewhere: depth-exceeded (`exprErrorTooDeep`, errorLine -1, tracked in `_depthTripped` so an outer success does not erase the warning), must-return-a-number/XY (errorLine -1, no useful source line since the expression ran to completion), and runtime exception (split into permanent-disable timeout vs. formatted-with-line-number generic error). All three share the same `lastError`/`errorLine` read contract C20j's propHasExpressionError (629) and C20f's editor panel (9236-9270) both use. The universal fallback for every error state lives one line past this span, in C20n's valueAtFrame (2645): return the exact raw value. Full citations in boundaries.",
          "budget_contract": "EXPR_BUDGET_MS=60 (C20a-j) applied only at the outermost call (_exprDepth===0), shared across a whole cross-reference chain; enforced by _exprTick's every-1024th-call Date.now() check (C20a-j) via the '__tick' sandbox argument instrumentLoops wires into every while/for/do in the compiled source. Depth capped at EXPR_MAX_DEPTH=8 (C20a-j), checked before any work is done. Full mechanics in boundaries.",
          "argument_coercion": "`normalizeExprResult` performs no type coercion beyond `typeof`/`Array.isArray`/`isFinite` checks and `Array.prototype.slice` — a non-number, non-array, or non-finite result is rejected outright (`null`), never coerced via `Number(...)`. `evalExpressionFor` coerces nothing of its own; `rawValue` arrives pre-shaped from its caller (C20n's valueAtFrame) and is only reshaped (`.length===1 ? [0] : .slice()`) before being handed to the compiled function as the `value` argument.",
          "return_shape": "`normalizeExprResult`: a bare finite number for a 1-dim property, a `dim`-length array of finite numbers for a 2+-dim property, or `null` on any rejection. `evalExpressionFor`: the SAME shape `normalizeExprResult` returns on success, or `null` on any of the three error states — never throws past its own `finally` (every internal exception is caught and converted to a `null` return plus bookkeeping on `ex`).",
          "determinism": "Deterministic GIVEN a deterministic compiled function and a fixed wall-clock budget: the SAME (holder, prop, frame, rawValue) with an unchanged compiled entry and no cross-reference cycle produces the SAME normalized result on every call, EXCEPT that the timeout path is inherently wall-clock/load-dependent (whether `_exprTick` trips depends on real elapsed time, not on the inputs alone) — the one non-deterministic edge this span's own contract has.",
          "raw_vs_evaluated": "This packet is the boundary between the two: it receives an already-computed RAW value as its 4th parameter (never samples rawValueAtFrame itself) and produces an EVALUATED value that C20n's own valueAtFrame chooses to use in place of the raw one only when this packet's own result is not `null` — the exact 'enabled-with-error falls back to raw' contract, enforced entirely in C20n, one line past this span's own boundary.",
          "callers": "`normalizeExprResult`: called only from this span's own `evalExpressionFor` (2592) — zero external callers (grep-confirmed). `evalExpressionFor`: exactly two callers in all of src/js — C20n's `valueAtFrame` (2642, the render-path caller) and C20f's own live-preview `paintResult()` (motion.js:9040, reachable directly because C20f shares this span's enclosing IIFE). `_timeoutToastPending` is read/written only inside this span's own timeout branch (2615-2618) and read once more at its own declaration-adjacent reset inside the same `setTimeout` callback — zero external references.",
          "external_calls": [
            "compiledFnFor(holder, prop) — this span, packet 1 (2388).",
            "ensureExprSeed(holder) — motion.js:1551 (C20a-j/C02).",
            "_exprFps() — motion.js:1371 (C20a-j/C02).",
            "_propSeedOffset(prop) — motion.js:1557 (C20a-j/C02).",
            "PROP_DIM — motion.js:506 (C02), read for the must-return-a-number-or-XY error wording.",
            "SM.t(...) — src/js/i18n.js, seven keys (exprErrorSyntaxPrefix/exprErrorSyntaxGlobalsSuffix live in packet 1; exprErrorTooDeep/exprErrorMustReturnNumberOrXY/exprErrorMustReturnNumber/exprErrorTimeout/exprToastTimeout live in this packet).",
            "_formatRuntimeError(e, entry) — motion.js:1531 (C20a-j/C02), which itself calls _closestName (1521) and _lineFromStack (1494).",
            "showToast(...) — src/js/timeline.js:7447 (C19w), guarded by `if (window.showToast)`.",
            "renderLayerList()/renderTimeline() — timeline.js, called from the debounced timeout toast callback, wrapped in try/catch ('not mounted').",
            "The 63 sandbox arguments passed to the compiled fn(...) itself (exprLayer, SELF_VIEW, COMP_VIEW, every expr*/alias*/buildAlias* symbol) — full per-position mapping and owning census in boundaries."
          ],
          "effects": "Writes `holder.expressions[prop].lastError`/`.errorLine` (all three error states) and, only on timeout, `.enabled = false` — all three PERSISTED via exportJSON/importJSON's verbatim `expressions:l.expressions` round-trip (timeline.js:2117/2410, see boundaries). Writes/restores the module-level `_ectx`, increments/decrements `_exprDepth`, may set `_exprDeadline`/clear `_depthTripped`/push onto it, may delete `holder._exprCompiled[prop]` (runtime cache only) and set/clear `_timeoutToastPending` — all five are transient in-memory evaluation state, never persisted."
        }
      ]
    }
  ],
  "source_consumers": [
    {
      "consumer": "save",
      "disposition": "No frame-save call (`saveActiveLayerFrame`/`saveAllLayerFrames`) occurs anywhere in motion.js:2348-2634 (grep-verified). This span's only persisted-side-effect fields are `holder.expressions[prop].lastError`/`.errorLine`/`.enabled` (the last only on timeout) — plain object mutations picked up automatically by whatever LATER save call serializes the layer, not triggered by this span itself. `timeline.js`'s `exportJSON` (2117) carries `expressions:l.expressions` verbatim with no field-level filtering, so these three bookkeeping fields ARE part of the saved project exactly as they stand at save time."
    },
    {
      "consumer": "load",
      "disposition": "No parser is implemented in this span. `timeline.js`'s `importJSON` (2410, cited by name/line only) assigns `state.layers[idx].expressions=ld.expressions` verbatim, so a previously-persisted `.lastError`/`.errorLine`/`.enabled` reopens exactly as saved — a timeout-disabled expression stays disabled with its last message intact after reload, rather than silently re-arming and immediately re-tripping. `holder._exprCompiled` (this span's own compile cache) is absent from every export/import field list, so a freshly-loaded holder always recompiles on first read — a natural invalidation via the field's absence, not an explicit clear this span or the loader performs."
    },
    {
      "consumer": "undo/redo",
      "disposition": "Zero calls to `pushUndo` anywhere in this span (grep-verified) — its own error/timeout bookkeeping writes are captured by undo only incidentally, when a sibling writer (`setExpressionCode`/`applyExprCode`, motion.js past this span) has already pushed a history entry for the SAME holder for the unrelated reason of committing new expression code."
    },
    {
      "consumer": "selection",
      "disposition": "This span reads/writes no selection state (`_motionKeySel`, `setKeySel`, `isKeySelected` and similar are never referenced) — it operates entirely inside the compile/evaluate call graph, with no UI/DOM/selection surface of its own."
    },
    {
      "consumer": "animation",
      "disposition": "This span is the ORCHESTRATION layer between a compiled expression and the animation system, not a track reader/writer of its own: it never calls `rawValueAtFrame`, `toggleAnimated`, `setKeyAtCurrentFrame` or any other key writer (grep-confirmed absent). It receives an already-sampled raw value as a parameter (from C20n's `valueAtFrame`) and returns either a normalized evaluated value or `null`, leaving the actual choice between the two to its caller."
    },
    {
      "consumer": "render",
      "disposition": "Confirmed reachable on the render path, NOT UI-only (see boundaries): `buildSceneJson` (engine-bridge.js) -> `SMMotion.layerMotionAt`/`elementMotionAt` (sibling-covered) -> `computeMotionMat` -> `valueAtFrame` (C20n) -> this span's own `evalExpressionFor` -> the compiled expression (built by this span's own `compiledFnFor`) -> `normalizeExprResult` (this span) on the way out. This full round trip runs once per frame per property carrying an enabled expression, on export and packaged Tauri playback as well as inside the Motion timeline editor — the compile-cache (packet 1) is what keeps that per-frame cost to a single 63-argument function call plus a small object allocation (`_ectx`) rather than a fresh `new Function` parse every frame."
    },
    {
      "consumer": "expression-editor-UI (C20f)",
      "disposition": "C20f's live-preview readout (`paintResult()`, motion.js:9040) calls this span's own `evalExpressionFor` a SECOND time per property per frame, independently of the render-path call — both calls share the SAME compile cache (packet 1) but not any evaluation-result cache, so an expensive expression (e.g. one calling C20k's `layer(ref).control(name)`, which pays a full 5-eager-`valueAtFrame` `layerSnapshot`) is evaluated twice per frame whenever the panel is open. C20f's globals-editor button also calls this span's `exprGlobals()` directly (9054/9058) and commits through `SMMotion.setExprGlobals` (13729-13736, past this span), which bulk-clears every layer's and element's compile cache — defensive, not required, given packet 1's own identity check."
    },
    {
      "consumer": "P03",
      "disposition": "This bounded census neither completes P03 nor certifies product behavior."
    }
  ],
  "proposed_behavioral_fixtures": [
    {
      "id": "compile-cache-identity-invalidation",
      "status": "proposed, unrun",
      "matrix": "unchanged code+preamble (expect cache hit, verify via a spy/counter on new Function or a timing probe) vs. code edited only vs. preamble edited only vs. both edited, across two consecutive compiledFnFor(holder, prop) calls",
      "expected": "A cache hit returns the IDENTICAL entry object (===) on the second call; any of the three edit cases produces a NEW entry object with updated `.code`/`.pre`, with no explicit `delete holder._exprCompiled[prop]` required to force it."
    },
    {
      "id": "two-attempt-compile-fallback",
      "status": "proposed, unrun",
      "matrix": "a bare-expression body ('value + 1', compiles under attempt A) vs. a multi-statement body ('var l = layer(\"X\"); return l ? l.position : value;', fails attempt A, succeeds attempt B) vs. a body that fails both (unbalanced syntax)",
      "expected": "Bare expressions never reach attempt B (exprMode=true, prefixLines includes the `return (` line); multi-statement bodies compile only via attempt B (exprMode=false, no `return (` line in prefixLines, requires the user's own explicit return); total failure sets fn=null, errorLine=-1, and a message built from exprErrorSyntaxPrefix with no line number."
    },
    {
      "id": "instrumented-vs-uninstrumented-retry",
      "status": "proposed, unrun",
      "matrix": "an expression whose instrumentLoops output is syntactically valid (the common case) vs. a constructed pathological expression where instrumentation itself introduces a SyntaxError",
      "expected": "The common case compiles the GUARDED (tick-injected) source (entry.guarded=true) and a runaway while-loop inside it eventually throws EXPR_TIMEOUT_TAG; the pathological case falls back to the UN-instrumented source (entry.guarded=false) and a runaway loop inside IT never trips the tick guard at all (structurally unbounded), a documented trade-off rather than a bug."
    },
    {
      "id": "vertex-property-dim-fallback",
      "status": "proposed, unrun",
      "matrix": "an expression on prop='vtx3' returning a bare number, a [du,dv] array, a NaN-containing array, and an out-of-PROP_DIM-table ordinary prop with no vtx prefix and no dim fallback",
      "expected": "vtx3 with a bare number broadcasts to [n,n]; vtx3 with [du,dv] passes through unchanged; vtx3 with [NaN,1] returns null (allFinite rejection, not a silent NaN); an ordinary prop absent from PROP_DIM and not vtx-prefixed has dim=undefined, `result.length >= undefined` is always false, and normalizeExprResult returns null for every input — the documented failure mode that only vtx-prefixed props are fixed against."
    },
    {
      "id": "cross-layer-cycle-depth-cap",
      "status": "proposed, unrun",
      "matrix": "layer A's position expression reads layer B's position via layer('B'), B's reads A's via layer('A') — a 2-cycle — evaluated at increasing EXPR_MAX_DEPTH-adjacent depths, vs. a non-cyclical 8-hop chain vs. a 9-hop chain",
      "expected": "The 2-cycle trips exprErrorTooDeep once _exprDepth reaches EXPR_MAX_DEPTH (8) and _depthTripped records the tripped `ex`; a non-cyclical chain of exactly 8 hops completes without tripping the cap; a 9-hop chain trips it on the 9th; after the outermost evaluation completes successfully despite an inner trip, the tripped entry's lastError survives (is not cleared) because it remains in _depthTripped for that outermost pass."
    },
    {
      "id": "runaway-loop-timeout-and-recovery",
      "status": "proposed, unrun",
      "matrix": "an expression with `while(true){}` (uninstrumented-retry-immune, i.e. instrumentLoops succeeds) evaluated once, vs. two such expressions evaluated within the same 60ms window, vs. re-enabling the SAME timed-out expression afterward with unchanged code",
      "expected": "The single runaway expression throws EXPR_TIMEOUT_TAG within EXPR_BUDGET_MS (60ms) of the outermost call starting, sets ex.enabled=false and ex.lastError=exprErrorTimeout, deletes its own compile-cache entry, and fires exactly one debounced showToast even if evaluated on consecutive frames before the setTimeout(...,0) callback runs; re-enabling with unchanged code recompiles (cache was deleted) and will time out again on the next evaluation since the code itself is unchanged."
    },
    {
      "id": "sandbox-name-collision-valueAtFrame",
      "status": "proposed, unrun",
      "matrix": "a compiled expression whose text calls `valueAtFrame(...)` (the AE-compatibility alias name) vs. the SAME text if it could somehow reach the module-scope function of the identical name declared in C20n",
      "expected": "Inside the sandbox, `valueAtFrame(...)` always resolves to the bound argument at position 55 (exprSelfAt, C20k's self-only RAW-reading built-in) — never to C20n's own per-property EVALUATED-value reader of the same name — because `new Function`'s closed parameter list contains no other binding for that identifier; this is a name collision between two intentionally different functions, not a bug, and should be pinned by a fixture before any future refactor accidentally unifies the two."
    },
    {
      "id": "must-return-number-error-wording",
      "status": "proposed, unrun",
      "matrix": "a 1-dim property (rotation) expression returning a string, vs. a 2-dim property (position) expression returning a string, vs. either returning undefined",
      "expected": "1-dim: exprErrorMustReturnNumber; 2-dim (PROP_DIM[prop] >= 2): exprErrorMustReturnNumberOrXY; both cases set errorLine=-1 (no source line, since the value returned by a successfully-run expression is not a parse-time or stack-derivable position)."
    }
  ],
  "future_proposals": [
    {
      "api": "compileExpressionText(code, preamble, argNames) -> {fn, prefixLines, userLines, exprMode, guarded} | {fn: null, error, errorLine: -1}",
      "status": "bounded proposal, unimplemented",
      "boundary": "Separates the pure two-attempt/instrumented-retry compile strategy (testable with plain strings, no holder/cache object needed) from compiledFnFor's own cache-key bookkeeping (holder._exprCompiled[prop] identity check), so the compile algorithm itself can be fixture-tested without constructing a fake holder."
    },
    {
      "api": "planExprBudget(outermostStartMs, budgetMs) -> {deadline, tick() -> void|throws}",
      "status": "bounded proposal, unimplemented",
      "boundary": "Makes the shared-across-a-cross-reference-chain wall-clock budget an explicit, constructible object instead of three module-level mutable variables (_exprDeadline/_tickCount/EXPR_BUDGET_MS) — a fixture could then advance a fake clock deterministically instead of relying on real elapsed time, closing the one non-deterministic edge this span's own contract has (see determinism, packet 2)."
    },
    {
      "api": "classifyExprError(state) -> 'depth' | 'shape' | 'timeout' | 'runtime' | 'none'",
      "status": "bounded proposal, unimplemented",
      "boundary": "Currently the three error states this span writes are distinguishable only by re-deriving which code path produced a given lastError/errorLine pair; a discriminated classification would let C20j's propHasExpressionError and C20f's editor panel branch on WHICH kind of error occurred (e.g. offering a re-enable action only for a timeout) without string-matching lastError's own localized text."
    },
    {
      "api": "renameSandboxAlias(oldPublicName) -> collision report against outer module scope",
      "status": "bounded proposal, unimplemented",
      "boundary": "A static check that no name in EXPR_ARG_NAMES ever collides with a module-scope function of a DIFFERENT meaning (the valueAtFrame/exprSelfAt collision this artifact documents as intentional) — would turn a future accidental collision into a build-time failure instead of a silent behavioral divergence discoverable only by reading both declarations."
    }
  ],
  "surface_inventory_cross_check": {
    "packet_symbols": [
      "exprGlobals",
      "EXPR_PUBLIC_NAMES",
      "EXPR_ARG_NAMES",
      "_countLines",
      "compiledFnFor",
      "normalizeExprResult",
      "evalExpressionFor",
      "_timeoutToastPending"
    ],
    "caller_anchors": [
      "compiledFnFor: sole caller is this span's own evalExpressionFor (motion.js:2544) — zero calls anywhere else in src/js.",
      "normalizeExprResult: sole caller is this span's own evalExpressionFor (motion.js:2592) — zero calls anywhere else in src/js.",
      "evalExpressionFor: exactly two callers in all of src/js — C20n's valueAtFrame (motion.js:2642) and C20f's expression-panel paintResult() (motion.js:9040).",
      "exprGlobals: three callers — this span's own compiledFnFor (motion.js:2390) and C20f's globals-editor button label/prompt seed (motion.js:9054/9058); also exposed on the public SMMotion object (`exprGlobals: exprGlobals`, motion.js past this span) though no caller outside motion.js was found reading SMMotion.exprGlobals directly (nemo-script.js's own `globals` getter reads `state.exprGlobals` directly instead, bypassing the function).",
      "EXPR_PUBLIC_NAMES: read by C20a-j's _closestName (motion.js:1524-1525, a forward reference) and by this span's own compiledFnFor indirectly via EXPR_ARG_NAMES.concat.",
      "EXPR_ARG_NAMES: read only by this span's own compiledFnFor (motion.js:2396, `var args = EXPR_ARG_NAMES`), and passed to `Function.apply(null, args.concat(...))` in both build()/buildBody() helpers.",
      "No caller of any of the 8 symbols was found outside src/js/motion.js — this span is motion.js-internal only, confirmed by the same grep methodology as the exemplar artifacts (`grep -rnE \"(^|[^a-zA-Z_.])name\\(\" src/js`, excluding declarations and `//`-prefixed lines)."
    ],
    "helper_anchors": [
      "_ectx module var:1365 (writer: this span, evalExpressionFor:2563-2569) / _exprDepth:1366 / EXPR_MAX_DEPTH:1367 / _depthTripped:1370 / EXPR_BUDGET_MS:1393 / _exprDeadline:1394 / _tickCount/EXPR_TIMEOUT_TAG/_exprTick:1396-1400 / instrumentLoops:1426 / _lineFromStack:1494 / _closestName:1521 / _formatRuntimeError:1531 / ensureExprSeed:1551 / _exprFps:1371 / _num:1591 / _propSeedOffset:1557 / hasExpr:1338 / PROP_DIM:506 / rawValueAtFrame:1270 (all C20a-j/C02, cited by name/line only)",
      "exprLayer:1986 / exprWiggle:1802 / exprNoise:1771 / exprRandom family:1765-1768 / exprSeed:1733 / exprClamp:1627 / exprRemap family:1623-1626 / exprDegrees:1640 / exprRadians:1639 / exprAdd..exprAngleTo:1643-1693 / exprStepTime:1709 / exprLoopAfter:1909 / exprLoopBefore:1915 / exprToFrames:1716 / exprToSeconds:1721 / exprSelfControl:2020 / exprLayerControl:2047 / exprNumKeys:2070 / exprSelfAt:1823 (C20k, merged #1261, cited by name/line only)",
      "SELF_VIEW/COMP_VIEW:~2145-2260 / exprContentBox:2257 / aliasKey:2294 / aliasNearestKey:2290 / aliasPosterizeTime:2280 / aliasSeedRandom:2295 / aliasValueAtTime:2287 / aliasVelocityAtTime:2288 / aliasSpeedAtTime:2289 / aliasTimeToFrames:2300 / aliasFramesToTime:2305 / buildAliasComp:2314 / buildAliasLayer:2325 (C20l, issue #1236, open at authoring time, cited by name/line only)",
      "valueAtFrame:2639 / hasExpr's caller-side gate at 2641 (C20n, issue #1238, open at authoring time, cited by line only)",
      "computeMotionMat:2933, layerMotionAt/elementMotionAt (sibling-covered past 2932, cited by line only)",
      "SM.t: src/js/i18n.js locale tables (four blocks) for the seven exprError*/exprToastTimeout keys this span throws/toasts with",
      "showToast: src/js/timeline.js:7447 (C19w, cited by name/line only)",
      "setExpressionCode:11579 / SMMotion.exprSnapshotFor+applyExprCode:13621-13639 / SMMotion.setExprGlobals:13729-13736 (motion.js, past this span, interact with the same _exprCompiled cache — cited by name/line only)",
      "nemo-script.js:548-549 (`globals` scripting property, cited by name/line only)"
    ]
  },
  "validation": {
    "expected_assigned_lines": 287,
    "expected_classification": {
      "code": 183,
      "comment": 104,
      "whitespace": 0
    },
    "packet_partition": [
      [
        2348,
        2490
      ],
      [
        2491,
        2634
      ]
    ],
    "packet_classification": [
      {
        "range": [
          2348,
          2490
        ],
        "code": 91,
        "comment": 52,
        "whitespace": 0
      },
      {
        "range": [
          2491,
          2634
        ],
        "code": 92,
        "comment": 52,
        "whitespace": 0
      }
    ],
    "coverage_contract": "Validate assigned_ranges and flattened areas[].packets[].coverage_ranges independently with the same function; reject actual artifact omission at line 2348 and overlap at line 2634.",
    "required_checks": [
      "frozen/current motion.js blob and allocated-base ancestry",
      "two unique packet IDs, exact ranges, counts, symbols, helpers, callers and boundaries",
      "before-2348 (C20l, issue #1236) preceding boundary and C20n (issue #1238) following boundary at line 2635",
      "seven consumer dispositions and proposed, unrun fixtures",
      "EXPR_PUBLIC_NAMES/EXPR_ARG_NAMES verbatim with bound-implementation mapping per name, the compile cache's key/invalidation, the _ectx field-write inventory and lifetime, the error contract across all three states, the time/step budget constants, every document write and its exportJSON/importJSON disposition, and the render-path reachability statement, all against the actual source lines",
      "JSON, private-path, secret, placeholder and trailing-whitespace hygiene",
      "sole untracked artifact or committed-clean mode"
    ],
    "runtime_limit": "No browser/Tauri/app suite, hosted CI, GPU, runtime edit, export or product acceptance claim."
  }
}
{
 "census_id": "C20s",
 "issue": 1249,
 "title": "Census motion.js key-drag engine: marquee update/end, drag render coalescing, auto-scroll, snapping, onDragMove/onDragUp (12685-13124)",
 "status": "source census; proposed, unrun behavioral fixtures; no runtime acceptance",
 "owner": "P03/#1005 executable census leaf closing the src/js/motion.js open span [12685,13124] of remediation-scope.json rangeCoverage (frozen index at 4bbe332): the key-drag engine (marquee update/end, drag render coalescing, auto-scroll, snapping, onDragMove/onDragUp). This is the last motion.js census slice of the current P03 series. Sole artifact: engineering/inventory/census/C20s-motion-key-drag-engine.json; src/js/motion.js and all callers remain read-only. Cited by name/line only, not re-mapped: trackRowHtml/renderTracksFor (C20i, motion.js:10672-11125), renderDiscreteKeyGridRow/renderPathGroupTrackRow (C20h, motion.js:10229-10671), the panel rows (C20g/C20f), trackFor/valueAtFrame/setKeyAtFrame/toggleAnimated (C20n/C02, motion.js:443/2635-2932), layer-inout.js (SMLayerInOut.marqueeSelect, group-bar drag — CLAUDE.md §11 \"one gesture, one rectangle\"), ui.js global pointer handlers and curve editor, rig-widget.js (§13 widget wiring menu), timeline-zoom.js, pushUndo*/_scrubLiveActive (tweens.js:4741/4896/4808), showToast (timeline.js:7447, C19w), window.showContextMenu (ui.js), SM.t (i18n.js), everything before 11412 and after 12405/13124 (_keyAnchor, _motionKeySel, _layerSel, enableTimeRemap, setAppMode — sibling-covered by name; see boundaries for the two genuine unassigned gaps this leaves at 12406-12684 and 13125+). isKeySelected/setKeySel/_groupKeySelByTrack (C20o, issue #1245, motion.js:11412-11682); the ease-influence model and key selection ops (C20p, issue #1246, motion.js:11683-11924); _motionMarquee/startMarquee/applyMarqueeSelection/marqueeLayersIn/addStaggerEdges/addSpaceEdges/addMoveFill/startSkewDrag/buildKeyRows/the key-selection box (C20q, issue #1247, motion.js:11925-12174, MERGED as PR #1279 during this leaf's own authoring — see source_baselines/reconciliation); _layerStaggerBoxEl/buildLayerRows/dragLayerBars/_layerSkew/startLayerSkewDrag/onLayerSkewMove/onLayerSkewUp/updateLayerStaggerBox (C20r, issue #1248, motion.js:12175-12405, MERGED as PR #1280 during this leaf's own authoring).",
 "source_sha": "59a5a38c514f5da830dd2f2df4c83c63b1b22fba",
 "observed_main_sha": "6cfd3ff6f9bd5cbc18683d05d2514531c6ada4ae",
 "source_path": "src/js/motion.js",
 "source_blob": "6617bac29042c22399cac202bf054c09e4fdf30e",
 "source_line_count": 13914,
 "scope_files": [
  "src/js/motion.js"
 ],
 "intended_branch": "claude/c20s-motion-key-drag-engine-census",
 "assigned_ranges": [
  {
   "start": 12685,
   "end": 13124
  }
 ],
 "coverage_note": "Exactly 440 assigned lines in one contiguous range and five complete packets: 300 code, 136 full-line comments, 4 blank — independently re-derived by classifying the frozen blob programmatically per-packet with the SAME blank/`//`-line/`/*...*/`-block algorithm the validator's own classify() runs (a Python re-implementation executed directly against `git show 59a5a38...:src/js/motion.js`), and it matches the issue's own proposed union AND per-packet table exactly for all five packets: 116=62/53/1, 31=12/18/1, 39=35/4/0, 60=59/0/1, 194=132/61/1, summing to 440=300/136/4. No boundary correction was needed; the only line-number corrections are two off-by-one fixes to the issue's own prose table (the final `document.addEventListener` pair sits at 13121-13122, not 13122-13123, with a blank line at 13123 before the 13124 header — re-verified line-by-line with direct `sed -n`/`grep -n` reads against the frozen blob; this does not change any packet boundary, range, or classification, all of which were already stated correctly by the issue).",
 "source_baselines": {
  "pinned": "59a5a38c514f5da830dd2f2df4c83c63b1b22fba:src/js/motion.js resolves to 6617bac29042c22399cac202bf054c09e4fdf30e, 13914 lines.",
  "current": "This worktree's branch was created off the assigned base 33b0753 (origin/main at task start). During this leaf's own authoring, origin/main advanced to 6cfd3ff via two docs-only census PRs — #1279 (docs(C20q), engineering/inventory/census/C20q-motion-key-marquee-selection-box.json) and #1280 (docs(C20r), engineering/inventory/census/C20r-motion-layer-stagger-skew-box.json) — verified directly: `git rev-parse 33b0753:src/js/motion.js` == `git rev-parse 6cfd3ff:src/js/motion.js` == `git rev-parse 59a5a38...:src/js/motion.js` == 6617bac29042c22399cac202bf054c09e4fdf30e, and `git log --oneline 33b0753..origin/main -- src/js/motion.js` returns zero commits (the two commits in `git log --oneline 33b0753..origin/main` touch only engineering/inventory/census/*.json). This worktree's own branch was NOT rebased/merged onto 6cfd3ff (task instruction: work only in this worktree, base pinned at 33b0753); C20q's and C20r's own JSON files were fetched to /tmp and temporarily copied into engineering/inventory/census/ ONLY for the validator's overlap check (per step 4), then deleted before the sole commit — `git status --short` at commit time shows only this artifact.",
  "runtime": "No browser, Tauri, export, native, GPU or application behavior was executed. All caller findings were derived by `grep -rnE '(^|[^a-zA-Z_.])<symbol>\\(' --include=\"*.js\" src` over the recursive src/js tree, plus a second bare-identifier pass (`grep -rnE '\\b<symbol>\\b'`) for every symbol declared in this span, per C20p's own precedent that action tables can wire a function by bare reference rather than a call. The bare pass surfaced no additional call sites beyond the paren pass for any of the 17 span-local symbols, but it DID surface a load-bearing negative result: `onDragMove` and `onDragUp` have ZERO occurrences anywhere in src/js outside motion.js itself (both passes), which directly contradicts this span's own lead-in comment (12931-12933) — see boundaries and packet 5's own callers field for the full evidence chain. Every surviving hit was read directly with `sed -n`/`grep -n` against this worktree's copy of motion.js at the frozen blob."
 },
 "writer_guidance": {
  "artifact": "Only engineering/inventory/census/C20s-motion-key-drag-engine.json is writable for this outcome.",
  "runtime": "Historical Motion ownership retains motion.js runtime writing; P30 retains timeline.js. C20s transfers no implementation authority.",
  "owners": "C02/C20a-n (before line 11412, cited by line only) retain trackFor (motion.js:443), keyAt (motion.js:1189), sortKeys (motion.js:1188), propsFor (motion.js:176, not itself covered by any merged census as of this leaf's authoring — verified by the same full sweep used for the 12406-12684/13125+ gaps below). C20h (10229-10671) retains renderPathGroupTrackRow's and renderDiscreteKeyGridRow's own mousedown handlers, which are the sole writers of the group-shaped `window._motionKeyDrag` at motion.js:10438/10561/10568 — this span only ever READS that object and writes `.key.frame` on the key objects it references, never a field of the drag object itself except clearing it to null. C20i (10672-11125) retains trackRowHtml in full, including the sole writer of the connector-drag object `window._motionConnectDrag` (motion.js:10782-10788), the sole writer of the single-key-shaped `window._motionKeyDrag` (motion.js:10954), one of two writers of `window._motionSkewDrag` (motion.js:10941, the Alt+drag shortcut, mode fixed to 'bottom'), and `selectLayerFromGrid` (motion.js:11126, called from this span's own endMarquee at 12777) and `syncBarSelToLayerSel` (motion.js:6932, called from this span's own endMarquee at 12770) — neither 11126 nor 6932 falls inside any merged census range as of this leaf's authoring (verified by the same full sweep), so both are cited by name/line only as genuinely open gaps, not as C20i's own declared scope. C20o (11412-11682, issue #1245) retains isKeySelected, setKeySel (motion.js:11473, the ONLY function this span's own writers never call — selection assembly is entirely upstream of this span; this span only ever reads the resulting `_motionKeySel`/drag objects), _groupKeySelByTrack in full — this span never calls any of them directly. C20p (11683-11924, issue #1246) retains the ease-influence model and the four selection/key-set operators; this span never touches `key.curvePoints`/`key.color`/`key.hold` (grep-confirmed zero hits for any of the three field names anywhere in 12685-13124). C20q (11925-12174, issue #1247, MERGED PR #1279) retains `_motionMarquee`/startMarquee/marqueeLayersIn/addStaggerEdges/addSpaceEdges/addMoveFill/startSkewDrag/buildKeyRows/the key-selection box in full — this span's own `updateMarquee` (12701-12722) and `endMarquee` (12723-12800) call `applyMarqueeSelection` (motion.js:11936, C20q) once each cross-reference, and this span's own `onDragMove` reads `window._motionSkewDrag` objects that ONLY C20q's `startSkewDrag` (motion.js:12084) and C20i's own Alt+drag shortcut (motion.js:10941) ever construct. C20r (12175-12405, issue #1248, MERGED PR #1280) retains `onLayerSkewMove` (motion.js:12265) and `onLayerSkewUp` (motion.js:12281) in full — this span's own `onDragMove`/`onDragUp` call them as the FIRST gate of both functions (motion.js:12935/13102), short-circuiting this span's own logic entirely when a layer-level skew/stagger-box drag is in progress."
 },
 "reconciliation": [
  {
   "owner": "P03/#1005 rangeCoverage",
   "disposition": "remediation-scope.json lists src/js/motion.js 12685-13124 as one open span (frozen index at 4bbe332), closed in full by this single leaf (unlike the four-way C20o-C20r split of the prior 11412-12405 span). This artifact's own 440-line union does not abut that prior span directly: lines 12406-12684 (279 lines: enableTimeRemap, disableTimeRemap, timeRemapValue, previewKeyframeShift, clearKeyframeShiftPreview, shiftLayerMotionKeys) sit between C20r's own end (12405) and this artifact's own start (12685), and lines 13125+ (setAppMode onward) sit after this artifact's own end — both are named 'sibling-covered' by the issue but, verified by sweeping every census/*.json on origin/main at 6cfd3ff for assigned_ranges touching src/js/motion.js, NEITHER range is currently covered by any merged artifact. This is recorded as a genuine open gap in the overall rangeCoverage picture, not a defect of this leaf (this leaf's own union is disjoint from every neighbor by construction: 12684 < 12685 and 13124 < 13125)."
  },
  {
   "owner": "C20q (sibling, issue #1247, motion.js:11925-12174, MERGED PR #1279 mid-authoring)",
   "disposition": "Retains _motionMarquee/startMarquee/applyMarqueeSelection/marqueeLayersIn/addStaggerEdges/addSpaceEdges/addMoveFill/startSkewDrag/buildKeyRows/_keySelBoxEl/removeKeySelectionBox/updateKeySelectionBox in full, per its own merged artifact (engineering/inventory/census/C20q-motion-key-marquee-selection-box.json, fetched read-only for this reconciliation). This artifact's own callers into C20q's range (applyMarqueeSelection at motion.js:12710, and the window._motionSkewDrag object shape C20q's own startSkewDrag constructs at motion.js:12084) are documented by line only against the frozen blob and against C20q's own published symbol list, not re-derived."
  },
  {
   "owner": "C20r (sibling, issue #1248, motion.js:12175-12405, MERGED PR #1280 mid-authoring)",
   "disposition": "Retains _layerStaggerBoxEl/removeLayerStaggerBox/buildLayerRows/noKeysGuard/dragLayerBars/_layerSkew/startLayerSkewDrag/onLayerSkewMove/onLayerSkewUp/updateLayerStaggerBox in full, per its own merged artifact (engineering/inventory/census/C20r-motion-layer-stagger-skew-box.json, fetched read-only for this reconciliation). This artifact's own onDragMove/onDragUp call onLayerSkewMove(e)/onLayerSkewUp() as the very first statement of each (motion.js:12935/13102) and return immediately if either returns truthy — documented by line only against C20r's own published symbol list, not re-derived."
  }
 ],
 "boundaries": [
  "Line 12680 (`return touched;`, confirmed by direct `sed -n`/`grep -n` read against the frozen blob) closes shiftLayerMotionKeys (declared 12616, sibling-covered per the issue's own naming) and precedes this scope. Lines 12681-12684 are staggerSelectedLayers' OWN lead-in comment ('Right-click alternative to dragging the handle...') yet are EXCLUDED from this artifact's assigned range — assigned_ranges starts at 12685, the function keyword itself, per the issue's own explicit instruction ('starts at staggerSelectedLayers (12685, immediately after its sibling-covered lead-in comment 12681-12684)'). Unlike C20p's own 'misplaced lead-in' finding (a comment describing the WRONG construct), this comment correctly describes staggerSelectedLayers; it is simply boundary-excluded, landing in the genuinely open 12406-12684 gap recorded in reconciliation above. Both re-verified with direct `sed -n` reads; no correction to the issue's own 440-line union was made.",
  "Line 13124 (`// ---- mode switching ----`, confirmed by direct read) closes this scope; line 13125 opens `function setAppMode(mode) {`, sibling-covered and, per the same origin/main sweep, not yet covered by any merged census artifact — the 13124 header line is included in this artifact per the issue's own explicit instruction ('that one line is included so no line of the open span is orphaned — record it as the header of a sibling-covered construct').",
  "The five packet cuts (12685/12801/12832/12871/12931) all land on a construct's own lead-in comment or declaration seam in the frozen blob, matching the issue's own suggested table exactly — re-verified with `sed -n`/`grep -n` for all five cut points; the sub-construct declaration lines within each packet (12701, 12723, 12806-12807, 12814, 12832, 12836-12838, 12841, 12845, 12871, 12875, 12905, 12913, 12917, 12934, 13101) were independently re-verified the same way against the issue's own table and match exactly, with two corrected line numbers for the closing `document.addEventListener` pair (13121-13122, not the issue's stated 13122-13123 — see coverage_note) that do not change any packet range or classification.",
  "MATERIAL FINDING, not a boundary of this artifact's own scope but a documentation-accuracy issue WITHIN it: packet 5's own lead-in comment (12931-12933, 'Drag-to-retime a keyframe (mousemove/up delegated from ui.js's global pointer handlers via SMMotion.onDragMove/onDragUp, same pattern as the span-end/keyframe drag handlers already in timeline.js)') describes a delegation mechanism that does NOT match the actual code it introduces. Evidence: (1) `grep -rn 'onDragMove\\|onDragUp' src/js/ui.js` returns zero hits; (2) `grep -rn '\\bonDragMove\\b'`/`'\\bonDragUp\\b'` over the ENTIRE src/js tree returns hits ONLY inside motion.js itself (comments, the two declarations at 12934/13101, the self-referential auto-scroll re-entrant call at 12867, and the two `document.addEventListener` registrations at 13121-13122); (3) `window.SMMotion` (the export object literal, motion.js:13352-13914) has no `onDragMove`/`onDragUp` key (grep-confirmed against the entire object body). The two functions are wired directly by `document.addEventListener('mousemove', onDragMove)`/`('mouseup', onDragUp)` INSIDE this very span (13121-13122), with zero involvement from ui.js and zero export on SMMotion. A DIFFERENT, actually-exported-and-delegated function with a similar name, `SMMotion.onDrag` (singular, no Move/Up suffix, declared motion.js:6507, exported motion.js:13777, called from tools.js:7632 and select-bridge.js:1800 — the CANVAS gizmo drag for `_motionDrag`, an entirely separate module var from `_motionKeyDrag`/`_motionSkewDrag`/`_motionConnectDrag`), is the one the comment's own 'same pattern' framing likely intends, but it is outside this span and outside this leaf's own scope. Recorded as a discrepancy between comment and code, not corrected (moving/editing comment text is out of scope for a read-only census, per C20p's own precedent for its own misplaced lead-in)."
 ],
 "areas": [
  {
   "area": "key-drag-engine",
   "packets": [
    {
     "id": "C20s.stagger-step-and-marquee-update-end",
     "files": [
      "src/js/motion.js:12685-12800"
     ],
     "coverage_ranges": [
      [
       12685,
       12800
      ]
     ],
     "classification": {
      "code": 62,
      "comment": 53,
      "whitespace": 1
     },
     "symbols": [
      "staggerSelectedLayers",
      "updateMarquee",
      "endMarquee"
     ],
     "signature": "staggerSelectedLayers(step:number) -> undefined (no-op if fewer than 2 layers selected, no return value either way); updateMarquee(e:MouseEvent) -> undefined (no-op if no marquee in progress); endMarquee() -> undefined (no-op if no marquee in progress).",
     "responsibility": "staggerSelectedLayers(step) (12685-12700): right-click typed-step ALTERNATIVE to dragging the layer-stagger-box handle (C20r's own startLayerSkewDrag/onLayerSkewMove, motion.js:12239-12295, cited by line only), applied instantly rather than live-dragged, but sharing the SAME rank-0-anchor semantics so both routes agree. `order=_layerSel.slice().sort(ascending by layer index)`; `holders=order.map(li=>state.layers[li]).filter(Boolean)`; no-ops if fewer than 2 holders. `pushUndo()` (12689, tweens.js:4741 — the ONLY pushUndo call anywhere in this artifact's 440 lines, grep-confirmed) BEFORE the loop. For every holder EXCEPT rank 0 (the first, by sorted layer index — the anchor that never moves): `propsFor(h)` (motion.js:176, not itself covered by any merged census as of this leaf's authoring) enumerates every animatable property name on that holder; for each, `trackFor(h,prop)` (motion.js:443, C02) resolves the track, skipping props with none; every key on that track is shifted by `step*rank` frames (12696) and CLAMPED to `[0, state.totalFrames-1]` inline (`Math.max(0,Math.min(...))`) — the SAME per-key clamp shape used throughout this artifact's own drag branches, but applied unconditionally to every key on every affected track rather than gated by a collision check: two keys on the SAME track that land on the same frame after this shift are NOT deduplicated or merged, they simply overlap (later ones win whatever `keyAt`/render logic reads first — this function does not itself check for or prevent that, unlike every onDragMove branch below, all of which explicitly refuse a colliding move). `sortKeys(track)` (motion.js:1188, C02) re-sorts EVERY affected track once, inside the per-property loop (once per holder-prop pair, not once globally). After all holders: `renderLayerList(); renderTimeline();` (timeline.js:6335/4026) unconditionally, then `window.SMEngineBridge.renderNow()` if the global exists — unconditional, not gated on whether anything actually changed (unlike setSelectedEaseInfluence in C20p, which gates its own renders on a nonzero-change counter). updateMarquee(e) (12701-12722): no-ops if `!_motionMarquee` (C20q's own module var, motion.js:11925, `{startX,startY,rectEl,moved,layer[,layers]}`). Computes `dx=e.clientX-_motionMarquee.startX`, `dy=e.clientY-_motionMarquee.startY`; sets `.moved=true` once either axis exceeds a 3px threshold (12706, the same 3px threshold used throughout this codebase's own drag-vs-click disambiguation, e.g. C20q's own connector-drag comment). Repositions the visible rectangle element (`_motionMarquee.rectEl`, a DOM node C20q's own startMarquee/mousedown handler created — cited by line only) via inline `left/top/width/height` styles from the min/max corner math (12707-12709) on EVERY move regardless of the 3px threshold (the rectangle tracks the raw cursor from the first pixel; only the SELECTION APPLICATION below is gated on `.moved`). Once `.moved` is true: calls `applyMarqueeSelection(x0,y0,x0+w,y0+h)` (motion.js:11936, C20q, cross-reference by line only — the KEY-GRID selection sweep) UNCONDITIONALLY on every subsequent move (not just once), then, if `window.SMLayerInOut && SMLayerInOut.marqueeSelect` exists, calls `SMLayerInOut.marqueeSelect(x0,y0,x0+w,y0+h)` (layer-inout.js, exported as `marqueeSelect: applyMarqueeSelection` at layer-inout.js:1640 — a DIFFERENT, layer-inout-local `applyMarqueeSelection` than this file's own C20q symbol of the same name) — the FORWARD half of the 'one gesture, one rectangle' round trip documented inline (12711-12719, CLAUDE.md §11): this call is what makes a single marquee drag on the key grid ALSO sweep the in/out bars, which the pre-2026-07-25 code never did (the comment cites a specific regression test: 'a drag across three bars' left halves left getBarSelection() empty'). The REVERSE half (layer-inout.js's own updateMarquee calling `SMMotion.marqueeSelect`, which resolves to THIS file's C20q `applyMarqueeSelection` — layer-inout.js:494, grep-confirmed, cited by line only) lives entirely in layer-inout.js, sibling-covered, and is a DIFFERENT call than this function's own forward call (two complementary marquee engines, each forwarding into the other's sibling equivalent, not one function calling itself in both directions). endMarquee() (12723-12800): no-ops if `!_motionMarquee`. Captures `moved`, `downLayer` (the layer index a plain click landed on, if any — set by C20q's own startMarquee, cited by line only), `sweptLayers` (a copy of `_motionMarquee.layers`, populated by C20q's own marqueeLayersIn during the drag, cited by line only) BEFORE removing the rectangle DOM node and nulling `_motionMarquee` (12727-12728) — so the remaining logic operates on locals, safe from the module var already being cleared. #856 guard (12742-12750): reads `SMLayerInOut.getBarSelection()` (layer-inout.js, cited by line only) and filters OUT of `sweptLayers` any layer whose bar selection entry has `part==='in'||part==='out'` (an edge-only catch must not ALSO select the whole layer — documented regression: a left-half sweep of three bars used to select three whole layers, which then made the NEXT drag move both edges of each bar instead of just the caught points). If `moved && sweptLayers.length`: compares the swept set against the current `_layerSel` (timeline.js:5423, sibling-covered global, bare-identifier — no local `var _layerSel` declaration exists in motion.js, confirmed by `grep -n 'var.*_layerSel' src/js/motion.js` finding none; it resolves through the scope chain to timeline.js's own top-level `var _layerSel=[]`); if different, calls `SM.setActiveLayer` (timeline.js:1534, sets the FIRST swept layer active) BEFORE assigning the full `_layerSel` array (12758-12759, ORDER-DEPENDENT per the inline comment: setActiveLayer collapses `_layerSel` to a single layer internally, so assigning the multi-list first would be silently overwritten), sets `_layerSelAnchor` (timeline.js:5432, same bare-identifier global-fallthrough) to the first swept layer, and — ONLY if the bar selection is currently empty (`!sweptBars.length`, 12769-12770, a SECOND #856 guard: do not resync bars from the just-set layer selection when THIS SAME gesture already established a distinct in/out-only bar selection, or the sync would erase the edge distinction the lasso just drew) — calls `syncBarSelToLayerSel()` (motion.js:6932, not itself covered by any merged census as of this leaf's authoring, cited by name/line only). Then `renderLayerList(); renderTimeline();` and a gated `SMEngineBridge.renderNow()` (12771-12772). Independent of the moved-with-swept-layers branch: `if (!moved && downLayer!=null)` (12777) — a plain click landing ON a layer's own grid row SELECTS that layer via `selectLayerFromGrid` (motion.js:11126, not itself covered by any merged census as of this leaf's authoring, cited by name/line only) and returns immediately (no further clearing logic runs for this case). `if (!moved)` alone (a plain click on EMPTY grid space, downLayer null) (12784-12797): clears the key selection (`setKeySel([])`, motion.js:11473, C20o), clears the bar selection (`SMLayerInOut.clearSelection()`, layer-inout.js, cited by line only) and, if `_layerSel.length>0`, clears the LAYER selection too (`_layerSel=[]`) — three independent selections (keys, bars, layers) all owned by this ONE gesture's 'click empty = deselect' convention, matching the canvas's own marquee tools elsewhere in the app (cited by name only). `renderTimeline()` always runs in this branch; `renderLayerList()` only if a layer selection was actually dropped (12797, gated — the one asymmetric render gate in this whole packet).",
     "return_shape": "All three functions return undefined; every effect is via DOM mutation, module-var reassignment, or calls into other packets/siblings.",
     "render_path": "None of these 3 symbols is called from the render path (grep-confirmed zero hits in engine-bridge.js/export.js). staggerSelectedLayers' own `k.frame` writes and endMarquee's own selection-state writes are both document/UI-state mutations that a LATER render (via the `renderNow`/`renderTimeline` calls this packet itself makes) will pick up — not something these functions read from the render path themselves.",
     "callers": "staggerSelectedLayers has exactly ONE caller anywhere in src/js: a context-menu action at motion.js:7473 (`ctxStaggerSelectedLayersEllipsis`, prompting for a frame offset via `prompt(SM.t('promptStaggerOffsetFrames'),'2')`) — that call site sits at line 7473, inside a gap not covered by any merged census as of this leaf's authoring (C20b's own two ranges are 6774-6831 and 6954-7031); not exported on window.SMMotion (grep-confirmed against the entire export object body, motion.js:13352-13914). updateMarquee/endMarquee have NO callers anywhere in src/js by name except this SAME artifact's own packet 5: updateMarquee is called from onDragMove at motion.js:12981 (unconditionally, once the connect-drag branch has been ruled out) and endMarquee is called from onDragUp at motion.js:13104 (unconditionally, as the second statement after the layer-skew gate) — both are CROSS-PACKET internal calls within this artifact, not external callers. layer-inout.js has its OWN LOCAL functions also named `updateMarquee` (layer-inout.js:479, called from layer-inout.js:801) and `endMarquee` (layer-inout.js:497, called from layer-inout.js:975) — grep-confirmed to be textually distinct declarations in a different module scope, NOT calls into this packet's own symbols (there is no cross-file call by either name; the actual cross-file coupling runs through `SMLayerInOut.marqueeSelect`/`SMMotion.marqueeSelect`, documented in this packet's own responsibility field above).",
     "external_calls": [
      "propsFor — motion.js:176 (not yet censused; staggerSelectedLayers' property enumeration)",
      "trackFor — motion.js:443 (C02; staggerSelectedLayers' track lookup)",
      "sortKeys — motion.js:1188 (C02; staggerSelectedLayers' post-shift re-sort, once per holder-prop pair)",
      "pushUndo — tweens.js:4741 (staggerSelectedLayers' sole checkpoint, before the shift loop)",
      "renderLayerList — timeline.js:6335 (staggerSelectedLayers unconditionally; endMarquee gated in one branch, unconditional in the other)",
      "renderTimeline — timeline.js:4026 (staggerSelectedLayers unconditionally; endMarquee in both its branches)",
      "window.SMEngineBridge.renderNow — engine-bridge.js (name only; staggerSelectedLayers unconditionally when the global exists; endMarquee only in the moved-with-swept-layers branch)",
      "applyMarqueeSelection — motion.js:11936 (C20q; updateMarquee's key-grid sweep, called on every move once .moved is true)",
      "window.SMLayerInOut.marqueeSelect — layer-inout.js (= applyMarqueeSelection, layer-inout.js:1640; updateMarquee's forward hand-off to the bar sweep)",
      "window.SMLayerInOut.getBarSelection — layer-inout.js (endMarquee's #856 edge-only filter and its second sync guard)",
      "SM.setActiveLayer — timeline.js:1534 (endMarquee, only when the swept set differs from the current _layerSel, called BEFORE _layerSel is reassigned)",
      "syncBarSelToLayerSel — motion.js:6932 (not yet censused; endMarquee, only when the bar selection is currently empty)",
      "selectLayerFromGrid — motion.js:11126 (not yet censused; endMarquee's plain-click-on-a-row branch)",
      "setKeySel — motion.js:11473 (C20o; endMarquee's plain-click-on-empty-space branch, called with [])",
      "window.SMLayerInOut.clearSelection — layer-inout.js (= clearBarSel, layer-inout.js:1641; endMarquee's plain-click-on-empty-space branch)",
      "_layerSel / _layerSelAnchor — timeline.js:5423/5432 (sibling-covered top-level globals, read/written by endMarquee via bare-identifier scope fallthrough, no local declaration in motion.js)",
      "_motionMarquee — motion.js:11925 (C20q module var; read by updateMarquee/endMarquee, nulled by endMarquee)"
     ],
     "effects": "Document writes: `k.frame` for every key on every affected property track of every non-anchor selected layer (staggerSelectedLayers, motion.js:12696) — real, persisted animation-track content, checkpointed by the packet's own unconditional pre-loop pushUndo(). Selection-only writes (transient, NOT persisted, NOT covered by any pushUndo in this packet): `_layerSel`/`_layerSelAnchor` (endMarquee, both branches), the key selection via `setKeySel([])` and the bar selection via `SMLayerInOut.clearSelection()` (endMarquee's empty-click branch). endMarquee itself never calls pushUndo — it only ever reshapes selection state, never document/key content, matching this artifact's general rule (see packet 5's own write-discipline note) that the checkpoint for any CONTENT-mutating drag is taken once, at the originating mousedown, never inside this span."
    },
    {
     "id": "C20s.drag-timeline-render-coalescing",
     "files": [
      "src/js/motion.js:12801-12831"
     ],
     "coverage_ranges": [
      [
       12801,
       12831
      ]
     ],
     "classification": {
      "code": 12,
      "comment": 18,
      "whitespace": 1
     },
     "symbols": [
      "_motionDragTimelineRaf",
      "requestMotionDragTimelineRender",
      "flushMotionDragTimelineRender"
     ],
     "signature": "_motionDragTimelineRaf:number (rAF handle or 0, module-scoped, motion.js:12806); requestMotionDragTimelineRender() -> undefined (idempotent while a frame is already pending); flushMotionDragTimelineRender() -> undefined (always renders, per the 2026-08 fix documented inline).",
     "responsibility": "Lead-in (12801-12805): key drags can emit well above display refresh rate; the collision/retiming logic updates DATA synchronously (see packet 5) but a full Motion-grid rebuild per mousemove only paints intermediate states the screen can never show — CLAUDE.md §5bis's rAF-coalescing pattern, applied here to `renderTimeline()` specifically. requestMotionDragTimelineRender() (12807-12813): no-ops if `_motionDragTimelineRaf` is already truthy (a frame is already scheduled); otherwise `requestAnimationFrame` schedules a callback that resets the flag to 0 and calls `renderTimeline()` (timeline.js:4026) — so no matter how many times this is called within one frame's worth of mousemove events, at most ONE `renderTimeline()` actually runs before the next paint. flushMotionDragTimelineRender() (12814-12831): a 2026-08 fix, documented at length inline (12815-12828) — this used to early-return when `_motionDragTimelineRaf` was 0 (no PENDING frame), which is the COMMON case for any drag lasting more than one frame's worth of movement, since the last mid-drag rAF callback already fires and resets the flag to 0 well before mouseup; the old code therefore did NOTHING on release, leaving the grid stuck on whatever it looked like at the last mid-drag paint — rendered while `window._motionKeyDrag` was still truthy, which suppresses the ease boxes (C20p's own `isPrimarySelectedKey`/`buildKeyEaseBox` gate, motion.js:10833, cited by line only: `if (isPrimarySelectedKey(...) && !window._motionKeyDrag && !window._motionSkewDrag)`). The fix: cancel any still-pending rAF (`cancelAnimationFrame`, only if one exists) and call `renderTimeline()` UNCONDITIONALLY — the function's own name and its own original comment ('flush the latest state on pointer release') always meant to render regardless of pending-frame state; only the early-return guard was wrong.",
     "return_shape": "Both functions return undefined; their entire effect is scheduling/cancelling an rAF callback and/or calling renderTimeline().",
     "render_path": "Neither symbol is on the render path itself (grep-confirmed zero hits in engine-bridge.js/export.js). renderTimeline() (timeline.js:4026) is a TIMELINE UI rebuild, not the scene-render path (buildSceneJson/getEffectiveStrokes) — this packet coalesces UI paint work triggered by a document mutation happening elsewhere (packet 5's own key.frame writes), it does not itself read or write render-path data.",
     "callers": "requestMotionDragTimelineRender: called from 4 sites, all inside this SAME artifact's own packet 5 (cross-packet internal, not external): the skew/space/liquify branch (motion.js:13043), the connect-drag branch (motion.js:12977), the group key-drag branch (motion.js:13081), and the single key-drag branch (motion.js:13098) — no caller anywhere else in src/js (grep-confirmed, parens and bare-identifier passes both empty outside motion.js, and both hits inside motion.js are these same 4 plus the declaration). flushMotionDragTimelineRender: called from 3 sites, all inside this SAME artifact's own packet 5 onDragUp (cross-packet internal): the skew-drag cleanup branch (motion.js:13105), the connect-drag cleanup branch (motion.js:13106), and the key-drag finalize path (motion.js:13111) — no caller anywhere else in src/js (grep-confirmed, both passes).",
     "external_calls": [
      "renderTimeline — timeline.js:4026 (both functions' own eventual effect)"
     ],
     "effects": "No document writes. The only state touched is the module-scoped `_motionDragTimelineRaf` handle (set/reset by requestMotionDragTimelineRender, read/cleared by flushMotionDragTimelineRender) and the DOM the coalesced renderTimeline() call rebuilds. No pushUndo call in this packet (grep-confirmed) — purely a render-scheduling utility, no content mutation of its own."
    },
    {
     "id": "C20s.drag-auto-scroll",
     "files": [
      "src/js/motion.js:12832-12870"
     ],
     "coverage_ranges": [
      [
       12832,
       12870
      ]
     ],
     "classification": {
      "code": 35,
      "comment": 4,
      "whitespace": 0
     },
     "symbols": [
      "motionDragScrollLeft",
      "_motionAutoScrollRaf",
      "_motionAutoScrollEvent",
      "motionDragIsActive",
      "stopMotionAutoScroll",
      "autoScrollMotionDrag"
     ],
     "signature": "motionDragScrollLeft() -> number (0 if #fg-wrap is absent); _motionAutoScrollRaf:number (rAF handle or 0); _motionAutoScrollEvent:object|null (a plain snapshot of pointer/modifier state, or null); motionDragIsActive() -> boolean; stopMotionAutoScroll() -> undefined; autoScrollMotionDrag(e:MouseEvent) -> undefined.",
     "responsibility": "motionDragScrollLeft() (12832-12835): reads `document.getElementById('fg-wrap').scrollLeft`, or 0 if the element is missing — the single chokepoint every deltaFrames computation in this artifact (packet 4's motionDragDeltaFrames, packet 5's skew branch) uses to account for horizontal timeline scroll happening MID-drag. motionDragIsActive() (12838-12840): `!!(window._motionKeyDrag || window._motionConnectDrag || window._motionSkewDrag)` — the THREE key-level drag globals this artifact's own onDragMove/onDragUp dispatch on (packet 5); deliberately does NOT include `_motionMarquee` (C20q) or any layer-level skew state (C20r's own `_layerSkew`, cited by line only) — auto-scroll's own continuation gate (below) only cares about the three KEY-drag globals. stopMotionAutoScroll() (12841-12844): cancels any pending `_motionAutoScrollRaf` and resets both module vars to 0/null — the single teardown path for the whole auto-scroll subsystem. autoScrollMotionDrag(e) (12845-12870): reads `#fg-wrap`'s bounding rect; no-ops if missing. Computes a `step` toward whichever edge the pointer is within a 34px threshold of (`edge=34`), scaled by `Math.ceil(distance-past-edge / 3)` (12849-12850) — the FURTHER past the edge, the LARGER the step (unbounded acceleration, not a fixed speed); `step` stays 0 (no scroll) if the pointer is within the non-edge middle. If `step` is 0, calls `stopMotionAutoScroll()` and returns (auto-scroll ends the instant the pointer re-enters the middle). Otherwise clamps the new `scrollLeft` to `[0, scrollWidth-clientWidth]` (12853); if the clamped value equals the value BEFORE the clamp (already at a scroll extreme), calls `stopMotionAutoScroll()` and returns (12854, no infinite RAF loop pinned against an edge). Otherwise snapshots the triggering event's pointer position and ALL FOUR modifier keys into `_motionAutoScrollEvent` (12855-12858, a plain object — the LIVE MouseEvent itself is not retained, only these named fields) and, if no rAF is already pending, schedules `tickMotionAutoScroll` (12859-12868, a named function expression): on each tick, resets the rAF handle to 0; if `!motionDragIsActive() || !_motionAutoScrollEvent`, stops and returns (the drag ended or was cleared between ticks — this is the ONLY place `motionDragIsActive()` is called in this artifact); otherwise re-invokes `onDragMove(_motionAutoScrollEvent)` (packet 5, motion.js:12867) with the SNAPSHOT event — re-entering the FULL onDragMove dispatch (including its own `onLayerSkewMove(e)` first-gate, C20r, cited by line only) as if the pointer had actually moved, so scrolling continues to retime the dragged key(s) even with a stationary physical pointer, and (via onDragMove's own call into this SAME function, since every branch that moves something also calls `autoScrollMotionDrag`/`motionDragDeltaFrames`) re-schedules its own next tick as long as the edge condition still holds.",
     "return_shape": "motionDragScrollLeft: number. motionDragIsActive: boolean. The other four: undefined, effect via module vars, DOM scrollLeft, and the rAF-driven re-entrant onDragMove call.",
     "render_path": "None of these 6 symbols is called from the render path (grep-confirmed zero hits in engine-bridge.js/export.js). autoScrollMotionDrag's own re-entrant `onDragMove` call DOES reach packet 5's document-mutating branches (key.frame writes) exactly as a real mousemove would — the auto-scroll tick is not a passive UI-only effect, it is a full drag-continuation tick.",
     "callers": "motionDragScrollLeft: called from packet 4's motionDragDeltaFrames (motion.js:12873) and from 7 sites outside this artifact, all inside C20h/C20i/C20q's own mousedown handlers that construct the `window._motionKeyDrag`/`_motionSkewDrag`/`_motionConnectDrag` objects' own `startScrollLeft` field (motion.js:10439,10561,10568,10785,10941,10944,10954, and C20q's own startSkewDrag at motion.js:12084) — every one of those is a `startScrollLeft: motionDragScrollLeft()` field initializer, not a control-flow call; grep-confirmed no other caller anywhere in src/js. motionDragIsActive: called exactly once, from this SAME packet's own tickMotionAutoScroll closure (motion.js:12862, cross-packet-internal within this same packet, not external) — grep-confirmed zero callers anywhere else in src/js. stopMotionAutoScroll: called from this SAME packet's own autoScrollMotionDrag (twice, motion.js:12851/12854, plus once more inside tickMotionAutoScroll at 12862) and from packet 5's own onDragUp (motion.js:13103) — all cross-packet-internal to this artifact; grep-confirmed zero callers anywhere else in src/js. autoScrollMotionDrag: called from packet 4's own motionDragDeltaFrames (motion.js:12872, unconditionally, the FIRST statement of that function) and directly from packet 5's own skew/space/liquify branch (motion.js:12990) — both cross-packet-internal; grep-confirmed zero callers anywhere else in src/js. _motionDragTimelineRaf-style module vars (_motionAutoScrollRaf/_motionAutoScrollEvent) are read/written only within this packet.",
     "external_calls": [
      "onDragMove — motion.js:12934 (this SAME artifact's own packet 5; tickMotionAutoScroll's re-entrant continuation call)"
     ],
     "effects": "No document writes directly. DOM side effect: `wrap.scrollLeft` (motion.js:12853, a UI scroll position, not persisted animation data). No pushUndo call anywhere in this packet (grep-confirmed) — the only content mutation this subsystem can trigger is indirect, via its own re-entrant onDragMove call reaching packet 5's key.frame writes, which are themselves already covered by the mousedown-time checkpoint documented in packet 5's own write-discipline note."
    },
    {
     "id": "C20s.drag-snap",
     "files": [
      "src/js/motion.js:12871-12930"
     ],
     "coverage_ranges": [
      [
       12871,
       12930
      ]
     ],
     "classification": {
      "code": 59,
      "comment": 0,
      "whitespace": 1
     },
     "symbols": [
      "motionDragDeltaFrames",
      "collectMotionSnapCandidates",
      "showMotionSnapGuide",
      "clearMotionSnapGuide",
      "snapMotionFrame"
     ],
     "signature": "motionDragDeltaFrames(e:MouseEvent, drag:{startX,startScrollLeft}) -> number (signed frame delta, rounded); collectMotionSnapCandidates(excludedKeys:Array|falsy) -> Array<{frame:number,label:string}> (deduplicated by frame, one label per frame — the FIRST source to claim a frame wins, later sources for the same frame are silently dropped); showMotionSnapGuide(frame:number, label:string) -> undefined; clearMotionSnapGuide() -> undefined; snapMotionFrame(frame:number, excludedKeys:Array|falsy, e:MouseEvent|falsy) -> number (the snapped frame, or the input frame unchanged if no candidate is within tolerance or snapping is disabled/overridden).",
     "responsibility": "motionDragDeltaFrames(e,drag) (12871-12874): calls `autoScrollMotionDrag(e)` (packet 3) UNCONDITIONALLY as its first statement, then returns `Math.round((e.clientX - drag.startX + motionDragScrollLeft() - (drag.startScrollLeft||0)) / FC)` — the delta in FRAMES between the current pointer position (corrected for any horizontal scroll that has happened since mousedown, via the difference between the CURRENT `motionDragScrollLeft()` and the SNAPSHOT `drag.startScrollLeft` taken at mousedown) and the drag's own start X, divided by `FC` (app.js:76, the GLOBAL frame-cell pixel width, read as a bare identifier — not module-scoped in motion.js, overwritten at runtime by timeline-zoom.js's own init()). This is the ONLY place in this artifact that both triggers auto-scroll AND computes a delta in one call — used by packet 5's connect-drag and key-drag (group+single) branches; the skew/space/liquify branch computes its own inline `total` instead (see packet 5) and calls `autoScrollMotionDrag` separately. collectMotionSnapCandidates(excludedKeys) (12875-12904): builds a `byFrame` map (a plain `Object.create(null)`, so frame numbers can safely be used as string keys with no prototype collision) via an inner `add(frame, label)` helper that rounds the frame, discards it if out of `[0, state.totalFrames)`, and SKIPS if that frame is ALREADY claimed (`byFrame[frame]` truthy) — first-writer-wins, no merge of labels. Candidate sources, in this exact order (each subsequent source can only win a frame the prior ones left unclaimed): (1) `state.currentFrame` labeled 'Tête de lecture' (playhead); (2) `state.waIn`/`state.waOut` labeled 'Début zone'/'Fin zone' (work-area bounds); (3) every entry of `state.markers` labeled by its own `.name` or the fallback 'Repère' (global markers); (4) for EVERY layer in `state.layers`: every key of every property track via `addHolder(ld)` (iterates `Object.keys(ld.motion||{})`) labeled 'Clé', excluding any key present in the caller's own `excludedKeys` array (so a key being dragged never snaps to its OWN un-moved position); the layer's own `timeRemap` track via `addTrack(ld.timeRemap)` directly (same 'Clé' label, same exclusion); every entry of the layer's OWN `.markers` labeled by its own `.name` or 'Repère calque' (per-layer markers); every key of every element's motion track via `Object.keys(ld.elementMotion||{})` (per-shape Motion-element keys, same 'Clé' label/exclusion); every key of every effect's per-property track via `(ld.effects||[]).forEach` -> `Object.keys((eff&&eff.keys)||{})` (effect parameter keyframes, same 'Clé' label/exclusion); (5) if `state.bpmShow && state.bpm>0 && state.fps>0`: a full BPM grid, one candidate every `state.fps*60/state.bpm` frames from `state.bpmOffset||0` to `state.totalFrames`, ALL labeled 'Temps BPM' — since the grid is generated AFTER every other source in `add()`'s first-wins order, a BPM tick that coincides with an existing key/marker/playhead/work-area frame is silently absorbed by whichever of THOSE labels got there first, never overwriting it with 'Temps BPM'. Returns `Object.keys(byFrame).map(f => {frame:Number(f), label:byFrame[f]})` — an unordered array (object key iteration order for numeric-looking string keys is ascending in every JS engine, but this function does not itself guarantee or re-sort it). showMotionSnapGuide(frame,label) (12905-12912): lazily creates a `#motion-snap-guide` div appended to `#fg-wrap` if one does not already exist, then repositions it via inline `style.left = frame*FC + FC/2` (centered on the snapped frame's own cell) and sets `dataset.label = (label||'Aligné') + ' · ' + (frame+1)` (1-based display, CSS presumably renders `data-label` as content). clearMotionSnapGuide() (12913-12916): removes the `#motion-snap-guide` element if present — a pure DOM teardown, no state variable to reset. snapMotionFrame(frame, excludedKeys, e) (12917-12930): ALWAYS calls `clearMotionSnapGuide()` FIRST (12918, unconditionally, before deciding whether a new guide should replace it — so a frame that snaps to nothing leaves no stale guide behind). Returns `frame` UNCHANGED immediately if `!_motionSnapEnabled` (motion.js:618, sibling module var, a persisted `localStorage['nemo-motion-snap']` toggle exposed via a checkbox UI at motion.js:7009-7021, cited by line only) OR if `e` is present and either `e.metaKey || e.ctrlKey` is true — the MODIFIER OVERRIDE, held-key disables snapping for that one call regardless of the persisted setting. Otherwise computes `threshold = Math.max(1, Math.ceil(8 / Math.max(1, FC)))` — an 8-PIXEL tolerance converted to a FRAME count via the current zoom (`FC`), floored at 1 frame minimum (so snapping still works even when zoomed out far enough that 8px is sub-frame) and with `FC` itself floored at 1 to avoid a divide-by-zero/negative threshold if `FC` were ever 0 or negative. Iterates EVERY candidate from `collectMotionSnapCandidates(excludedKeys)` (a FRESH full rescan on every single call — no caching across a drag's own many ticks, see future_proposals), tracking the CLOSEST one within `threshold` (`dist<=threshold && (!best||dist<best.dist)`, strict less-than for ties so the FIRST-encountered of two equidistant candidates wins, not the last). If no candidate qualifies, returns the input `frame` unchanged (no guide shown, since clearMotionSnapGuide already ran). Otherwise calls `showMotionSnapGuide(best.frame, best.label)` and returns `best.frame`.",
     "return_shape": "motionDragDeltaFrames/snapMotionFrame: number (a frame value/delta). collectMotionSnapCandidates: Array<{frame:number,label:string}>. showMotionSnapGuide/clearMotionSnapGuide: undefined, DOM-only side effects.",
     "render_path": "None of these 5 symbols is called from the render path (grep-confirmed zero hits in engine-bridge.js/export.js). collectMotionSnapCandidates READS render-adjacent document state (every key's `.frame` across every layer/element/effect) but never mutates it and is not itself part of scene construction — it is a UI-affordance query, run fresh on every snap check.",
     "callers": "motionDragDeltaFrames: called from packet 5's connect-drag branch (motion.js:12950) and key-drag branch (motion.js:13055) — both cross-packet-internal to this artifact; grep-confirmed zero callers anywhere else in src/js (the skew/space/liquify branch does NOT call it, computing its own inline `total` instead — see packet 5). collectMotionSnapCandidates: called exactly once, from this SAME packet's own snapMotionFrame (motion.js:12923, cross-packet-internal within this packet); grep-confirmed zero callers anywhere else in src/js. showMotionSnapGuide: called exactly once, from this SAME packet's own snapMotionFrame (motion.js:12929); grep-confirmed zero callers anywhere else in src/js. clearMotionSnapGuide: called from this SAME packet's own snapMotionFrame (motion.js:12918, unconditionally, every call) and from packet 5's own onDragMove (group key-drag's snap-collision fallback, motion.js:13075; single key-drag's snap-collision fallback, motion.js:13093) and onDragUp (all three cleanup branches, motion.js:13105/13106/13110) — all cross-packet-internal to this artifact; grep-confirmed zero callers anywhere else in src/js. snapMotionFrame: called from packet 5's connect-drag branch (twice: motion.js:12958 retime sub-branch, motion.js:12971 non-retime sub-branch) and key-drag branch (twice: motion.js:13064 group sub-branch, motion.js:13087 single-key sub-branch) — 4 call sites total, ALL cross-packet-internal to this artifact and ALL inside packet 5; grep-confirmed zero callers anywhere else in src/js. NOTABLE ABSENCE, grep-confirmed: the skew/space/liquify branch (packet 5, motion.js:12982-13051) never calls snapMotionFrame at all — the three key-selection-box gestures (skew/space/liquify, C20q's own startSkewDrag) bypass the entire magnetic-snap system; only plain key drags (group or single) and the connector/segment drag snap to other keys/markers/playhead/BPM.",
     "external_calls": [
      "autoScrollMotionDrag — motion.js:12845 (this SAME artifact's own packet 3; motionDragDeltaFrames' unconditional first statement)",
      "motionDragScrollLeft — motion.js:12832 (this SAME artifact's own packet 3; motionDragDeltaFrames' delta correction)",
      "FC — app.js:76 (global var, not module-scoped; motionDragDeltaFrames' pixel-to-frame divisor, snapMotionFrame's pixel-tolerance-to-frame conversion, showMotionSnapGuide's cell-position math)",
      "_motionSnapEnabled — motion.js:618 (sibling module var; snapMotionFrame's persisted on/off gate)",
      "state.currentFrame/waIn/waOut/markers/layers/totalFrames/bpmShow/bpm/bpmOffset/fps — plain state fields, no owning function (collectMotionSnapCandidates' candidate sources)"
     ],
     "effects": "No document writes anywhere in this packet (grep-confirmed: none of these 5 symbols writes any `key.*`/`ld.*`/`state.*` field). The only mutation is DOM: creating/repositioning/removing the `#motion-snap-guide` element (showMotionSnapGuide/clearMotionSnapGuide). No pushUndo call in this packet — purely a read-only query plus a UI affordance, consistent with the rest of this artifact's write discipline (see packet 5)."
    },
    {
     "id": "C20s.on-drag-move-up",
     "files": [
      "src/js/motion.js:12931-13124"
     ],
     "coverage_ranges": [
      [
       12931,
       13124
      ]
     ],
     "classification": {
      "code": 132,
      "comment": 61,
      "whitespace": 1
     },
     "symbols": [
      "onDragMove",
      "onDragUp"
     ],
     "signature": "onDragMove(e:MouseEvent) -> undefined (every branch either mutates+renders+returns, or falls through to the next branch; the function itself never returns a value); onDragUp() -> undefined (same shape).",
     "responsibility": "Lead-in (12931-12933) — SEE BOUNDARIES for a material finding: this comment claims 'mousemove/up delegated from ui.js's global pointer handlers via SMMotion.onDragMove/onDragUp', which does not match the actual wiring below (direct `document.addEventListener`, no ui.js involvement, no SMMotion export). onDragMove(e) (12934-13100) dispatch order, precisely, with each gate's exact line: (1) `if (onLayerSkewMove(e)) return;` (12935, C20r, motion.js:12265 — the LAYER-level stagger/skew-box drag, checked FIRST; if it handled the event, nothing below this line runs at all for this tick); (2) `var cd = window._motionConnectDrag; if (cd) { ...; return; }` (12936-12979 — the connector/segment drag, C20i's own sole writer at motion.js:10782-10788); (3) `updateMarquee(e);` (12981, packet 1, ALWAYS invoked at this point regardless of whether a marquee is actually active — self-gated internally on `!_motionMarquee`, so this is a cheap no-op unless a marquee mousedown, C20q's own startMarquee, is genuinely in progress); (4) `var sk = window._motionSkewDrag; if (sk) { ...; return; }` (12982-13052 — the key-selection-box skew/space/liquify drag, written by EITHER C20q's own startSkewDrag, motion.js:12084, OR C20i's own Alt+drag shortcut, motion.js:10941); (5) `var d = window._motionKeyDrag; if (!d) return;` then the group/single key-drag body (13054-13099 — written by 4 sites: C20h's renderPathGroupTrackRow/renderDiscreteKeyGridRow, motion.js:10438/10561/10568, group-shaped; C20i's own trackRowHtml, motion.js:10944 group-shaped or motion.js:10954 single-shaped). Structurally, at most ONE of `_motionConnectDrag`/`_motionSkewDrag`/`_motionKeyDrag`/ `_motionMarquee` is ever set at a given time, since each is created by a mutually-exclusive mousedown handler outside this span that does not co-set the others (cited by line only in C20h/C20i/C20q) — this span itself does not enforce that exclusivity, it structurally relies on it. CONNECT-DRAG BRANCH (12936-12979): re-derives `track=trackFor(cd.ld,cd.prop)` and the pair's current indices (`ia`,`ib`) FRESH on every move rather than caching them at mousedown — the inline comment (12938-12944) explains why: `sortKeys()` below can shuffle indices if a drag ever lands exactly on a neighbor's frame (itself blocked, but cheap enough to just always re-derive). Computes `prevKey`/`nextKey` (the neighbor OUTSIDE the dragged pair on each side) and `deltaFrames = motionDragDeltaFrames(e,cd)` (packet 4). If `cd.retime` (Alt was held at mousedown): keeps `cd.a` PLANTED, only `cd.b` moves — clamps to `[cd.startAFrame+1, min(totalFrames-1, nextKey ? nextKey.frame-1 : totalFrames-1)]` (12955-12956), snaps via `snapMotionFrame(nb,[cd.a,cd.b],e)` then RE-CLAMPS the snapped result into the same bounds (12958, so a snap target can never escape the collective clamp even though snapMotionFrame itself does not know about it), no-ops if unchanged (12959), else writes `cd.b.frame=nb` and `sortKeys(track)` (12960). Else (both endpoints move together, duration constant): computes `loD`/`hiD` as the INTERSECTION of what each endpoint can individually tolerate against its OWN outside neighbor (12965-12969), clamps `deltaFrames` into `[loD,hiD]` (12970), snaps `cd.startAFrame+d` via snapMotionFrame (12971) then re-derives `d` from the snapped result re-clamped into `[loD,hiD]` (12972, same 'snap then re-clamp' pattern as the retime branch), no-ops if BOTH endpoints are already at their target (12973), else writes `cd.a.frame`+`cd.b.frame` together (12974) and `sortKeys(track)` once (12975). Either sub-branch ends with `requestMotionDragTimelineRender()` (packet 2) and a gated `SMEngineBridge.renderNow()` (12977-12978), then `return` — this branch NEVER falls through to updateMarquee/skew/key-drag. MARQUEE (12981): see packet 1's own responsibility. SKEW/SPACE/LIQUIFY BRANCH (12982-13052): calls `autoScrollMotionDrag(e)` directly (12990, NOT via motionDragDeltaFrames — this branch never calls motionDragDeltaFrames or snapMotionFrame, grep-confirmed in packet 4's own callers field: the magnetic snap system does not apply to these three gestures at all). Computes `total = (e.clientX - sk.startX + motionDragScrollLeft() - (sk.startScrollLeft||0)) / FC` — an UNROUNDED float distance in frames (distinct from motionDragDeltaFrames' own rounded-integer delta), because each row/key applies its OWN fractional factor to this SAME total before rounding, so fractional middle rows land exactly on `round(fraction*total)` with zero cumulative drift and dragging back to the start restores every key to its exact original frame (12984-12989, inline comment preceding the whole `if(sk)` block). For each row (`sk.rows`, an array of arrays of `{track,key,orig}` from C20q's own buildKeyRows/startSkewDrag, cited by line only), computes `rowF` (13000-13001) — 1 for `mode==='move'` or a single row; `(n-1-r)/(n-1)` for `mode==='top'`; `r/(n-1)` for `mode==='bottom'` (the SKEW gestures, described by their own inline comment at 12997-12999: one edge anchored, the opposite edge moves the full distance, in-between rows interpolate linearly by row index — the diagonal). For each key within a row, `f` starts as `rowF` (13011) then is OVERRIDDEN per-key for two other gesture families (described by their own inline comment at 13003-13010): if `mode==='liquify'` (13012-13020), `f=Math.exp(-((en.orig-sk.grabFrame)/radius)^2)` — a Gaussian falloff centered on the mousedown grab point, `radius=Math.max(2, span*0.35)` scaled to the selection's own frame span (`sk.fMax-sk.fMin`) so a 10-frame and a 200-frame selection feel the same, floored at 2 to avoid a near-zero-radius blowup on a near-single-frame selection; if `mode==='left'||'right'` (13021-13024), `f` is a TIME-based (not row-based) fraction of the key's own position between the selection's own `fMin`/fMax` (`0` if the whole selection is on one frame — nothing to spread), independent of which track/row the key lives in (the SPACE gestures: spread/compress along the timeline with the opposite edge anchored). For every key: `nf = en.orig + Math.round(total*f)` (13026); rejects the WHOLE gesture (`return false` from the `.every`, propagating to `ok3=false`) if `nf` is out of `[0,totalFrames)`, OR if `keyAt(en.track,nf)` finds an UNDRAGGED occupant (`existing!==en.key && dragged.indexOf(existing)<0` — a key ALSO being dragged this same tick at that target is NOT a collision, since it will move away too); otherwise stages `{key,track,nf}` into `plan`. If `!ok3`: `return` — the ENTIRE skew/space/liquify gesture is abandoned for this tick, no partial application of the rows/keys that WOULD have been valid (all-or-nothing, same philosophy as the group key-drag below, but here there is no snap-then-fallback-to-raw retry since this branch never snaps at all). If `ok3`: commits every planned `p.key.frame=p.nf` (13038-13041), collects each DISTINCT track touched and calls `sortKeys` on each ONCE (not once per key), then `requestMotionDragTimelineRender()` and a gated `SMEngineBridge.renderNow()` (13043/13051, with an inline 2026-07 comment explaining the renderNow call itself: without it, the interpolated value AT THE CURRENT PLAYHEAD only updated on mouseup, since nothing else re-rendered the stage mid-drag — every sibling drag, canvas gizmo and bar drag, already re-rendered on every move; this was the one gap), then `return`. KEY-DRAG BRANCH (13054-13099): `var d = window._motionKeyDrag; if (!d) return;` — if NONE of the four globals were set, onDragMove does nothing at all for this tick. `deltaFrames = motionDragDeltaFrames(e,d)` (13055, packet 4, which ALSO triggers auto-scroll as a side effect — shared with the connect-drag branch). GROUP sub-branch (`d.group`, 13056-13084): `movedKeys = d.keys.map(s=>s.key)`; snaps `deltaFrames` via the FIRST key's own `origFrame` (`d.keys[0].origFrame+deltaFrames`) then re-derives `deltaFrames` as the difference back from that SAME first key's `origFrame` (13064) — every OTHER key in the group is checked against this ONE shared, snapped delta, not independently snapped. `groupCanMove(by)` (13065-13071, a closure) — for EVERY selected key: target in-bounds AND (`!existing || existing===s.key || movedKeys.indexOf(existing)>=0`) — the exact same 3-way collision-tolerance rule as the skew branch (no occupant, self, or a co-dragged key are all fine). `ok=groupCanMove(deltaFrames)`; if blocked AND the snapped delta differs from the RAW (unsnapped) delta AND the raw delta WOULD work (`groupCanMove(rawDeltaFrames)`), falls back to the raw delta (13073-13076, clearing the snap guide since the snap target is being abandoned) — 'a magnetic target that would create a collision must not create a dead zone' extended to the whole-group case. If STILL not `ok`: `return` — the entire group move is abandoned for this tick, no partial move (same all-or-nothing philosophy, inline comment 13057-13060 makes the intent explicit: 'keeps the group's relative spacing intact rather than silently dropping just the colliding member'). If `ok` but nothing would actually CHANGE (`d.keys.some(s=>s.key.frame!==s.origFrame+deltaFrames)` false): `return` (no-op tick, no render triggered). Otherwise writes EVERY selected key's `.frame = s.origFrame+deltaFrames` and calls `sortKeys(trackFor(s.holder,s.prop))` PER KEY (13080, not deduplicated by track the way the skew branch dedupes — a group spanning many keys on the SAME track re-sorts that track once per key in the group, not once total; a minor asymmetry with the skew branch's own `touched` dedup array), then `requestMotionDragTimelineRender()` + gated `renderNow()` (13081-13082) and `return`. SINGLE-KEY sub-branch (falls through when `!d.group`, 13085-13099): clamps `nf` to `[0,totalFrames-1]` from `d.startFrame+deltaFrames` (13085); snaps via `snapMotionFrame(nf,[d.key],e)` (13087, NO re-clamp after snapping here, unlike the connect-drag branch — a snap candidate could theoretically sit outside `[0,totalFrames-1]` only if collectMotionSnapCandidates' own bounds check, packet 4, already excludes such candidates, which it does: `frame<0||frame>=state.totalFrames` is rejected inside `add()`, so this is safe by construction, not by an explicit second clamp); no-ops if unchanged from the key's CURRENT frame (not its start frame — 13088). If the snapped target is occupied by ANY other key (`keyAt(track,nf)`, no dragged-key exemption needed since there is only ONE key in this sub-branch): clears the snap guide, falls back to the UNSNAPPED `nf` (13090-13093), then abandons the whole tick if EITHER the fallback also equals the current frame (no-op) OR the fallback is ALSO occupied (13094) — 'a magnetic target occupied by another key must not create a dead zone around it: keep the nearest free raw frame instead' (inline comment 13089-13091), the single-key mirror of the group branch's own snap-then-raw-fallback rule. Otherwise writes `d.key.frame=nf` and `sortKeys(track)`, then `requestMotionDragTimelineRender()` + gated `renderNow()` (13098-13099) — no explicit `return` needed, this is the end of the function body. onDragUp() (13101-13120): `if (onLayerSkewUp()) return;` (13102, C20r, motion.js:12281 — the SAME first-gate pattern as onDragMove, mirrored). `stopMotionAutoScroll()` (13103, packet 3, ALWAYS — regardless of which drag variant is ending, any pending auto-scroll rAF is torn down unconditionally). `endMarquee()` (13104, packet 1, ALWAYS — self-gated internally on `!_motionMarquee`, same no-op-if-inactive pattern as updateMarquee in onDragMove). Skew cleanup (13105): if `window._motionSkewDrag`, null it, `clearMotionSnapGuide()` (a no-op call, since this branch never set a guide — defensive symmetry, not a real cleanup need), `flushMotionDragTimelineRender()` (packet 2, the FORCED final render), gated `renderNow()`, `return`. Connect-drag cleanup (13106): same shape, PLUS `document.body.style.cursor=''` (resetting the `e-resize`/`ew-resize` cursor the mousedown handler set, C20i cited by line only), then `return`. Key-drag finalize (13107-13119, reached only if NEITHER skew nor connect-drag was active): `if (!window._motionKeyDrag) return;` — a plain mouseup with no drag of any of the three kinds in progress does NOTHING beyond the three unconditional calls above. Otherwise: captures `dragged = window._motionKeyDrag.keys || []` (empty array for the single-key shape, which has no `.keys` field) BEFORE nulling the global (13108-13109). `clearMotionSnapGuide()` + `flushMotionDragTimelineRender()` (13110-13111). #774 SECOND-HALF FIX, inline comment at 13112-13117, code at 13118: `if (dragged.some(d => d.prop==='timeRemap') && window.loadFrame) loadFrame(state.currentFrame)` — retiming a Time Remap key changes which SOURCE frame the playhead exposes, exactly as retyping its value would, and a plain `renderNow()` cannot show that on its own; `loadFrame` (app.js:4746) is the ONLY thing that re-resolves a video's frame sync or a Component's content. This check only fires for the GROUP shape (the only shape whose `.keys` array can carry a `timeRemap`-prop entry; the single-key shape's own object never has a `.prop==='timeRemap'` field to match since it only carries `d.prop` directly, not inside a `.keys` array — so a single-key Time Remap drag would NOT trigger this reload path; PATH-DEPENDENT ON HOW THE MOUSEDOWN HANDLER SHAPED THE DRAG OBJECT, worth flagging as a possible gap, see future_proposals). Finally, an unconditional-looking `window.SMEngineBridge.renderNow()` at 13119 — still gated the same way as every other call in this artifact (`if (window.SMEngineBridge) window.SMEngineBridge.renderNow();`), just with no early `return` after it since it is the last statement of the function. Registrations (13121-13122): `document.addEventListener('mousemove', onDragMove)` and `document.addEventListener('mouseup', onDragUp)` — UNCONDITIONAL, run once at module-load time (this file is one IIFE), for the LIFETIME of the page; there is no corresponding `removeEventListener` anywhere in this file (grep-confirmed) — both handlers are permanently live and rely entirely on their own internal `if(!d) return`-style early guards to be no-ops outside an active drag.",
     "return_shape": "Both functions return undefined; all effects are via key.frame writes, module-var clears, DOM guide/cursor/scroll changes, and calls into packets 1-4/siblings.",
     "render_path": "NEITHER onDragMove nor onDragUp is called from the render path (buildSceneJson/getEffectiveStrokes chain) — grep-confirmed zero hits in engine-bridge.js/export.js for either name. This is the clearest instance of this artifact's own general rule: these two functions MUTATE the document (`key.frame`, across 4 distinct write sites: connect-drag's `cd.a.frame`/`cd.b.frame`, skew's `p.key.frame`, group key-drag's `s.key.frame`, single key-drag's `d.key.frame` — ALL AND ONLY `.frame`, grep-confirmed zero writes to `.curvePoints`/`.color`/`.hold`/`.v` anywhere in this packet) that the render path READS on every subsequent frame via `rawValueAtFrame`'s own interpolation (sibling-covered, cited by line only) — mutating the document and rendering it are different operations, and this packet's own `renderNow()`/`renderTimeline()` calls are the former TRIGGERING the latter, not this packet itself being part of the render construction.",
     "callers": "onDragMove: registered directly via `document.addEventListener('mousemove', onDragMove)` at motion.js:13121 (inside this SAME packet) and called re-entrantly from packet 3's own `tickMotionAutoScroll` closure (motion.js:12867, cross-packet-internal). Both the parenthesized-call grep (`grep -rnE '(^|[^a-zA-Z_.])onDragMove\\(' --include=\"*.js\" src`) and the bare-identifier grep (`grep -rnE '\\bonDragMove\\b' --include=\"*.js\" src`) return hits ONLY inside motion.js — zero in ui.js, zero anywhere else in src/js. onDragUp: same shape — registered via `document.addEventListener('mouseup', onDragUp)` at motion.js:13122, with NO other caller anywhere (no re-entrant call analogous to onDragMove's auto-scroll case, since a 'mouseup' cannot be synthesized by a still-running rAF loop the way a continued 'mousemove' effect can). CONTRADICTS the span's own lead-in comment's claim of ui.js/SMMotion delegation — see boundaries for the full evidence chain (window.SMMotion, motion.js:13352-13914, has no onDragMove/onDragUp key; the actual SMMotion-exported, ui.js/tools.js/select-bridge.js-delegated drag function is the unrelated, singular `onDrag`, motion.js:6507/13777, a different module var (`_motionDrag`) and a different gesture family — the canvas gizmo, not the timeline key diamond).",
     "external_calls": [
      "onLayerSkewMove — motion.js:12265 (C20r; onDragMove's first gate, motion.js:12935)",
      "onLayerSkewUp — motion.js:12281 (C20r; onDragUp's first gate, motion.js:13102)",
      "trackFor — motion.js:443 (C02; connect-drag's/single-key-drag's track lookup)",
      "sortKeys — motion.js:1188 (C02; every branch's post-write re-sort)",
      "keyAt — motion.js:1189 (C02; skew's/group's/single-key's collision checks)",
      "motionDragDeltaFrames — motion.js:12871 (this SAME artifact's own packet 4; connect-drag and key-drag branches)",
      "autoScrollMotionDrag — motion.js:12845 (this SAME artifact's own packet 3; skew branch's own direct call)",
      "motionDragScrollLeft — motion.js:12832 (this SAME artifact's own packet 3; skew branch's own inline `total` computation)",
      "snapMotionFrame — motion.js:12917 (this SAME artifact's own packet 4; connect-drag and group/single key-drag branches — NOT the skew branch)",
      "clearMotionSnapGuide — motion.js:12913 (this SAME artifact's own packet 4; collision fallbacks and every onDragUp cleanup branch)",
      "updateMarquee — motion.js:12701 (this SAME artifact's own packet 1; onDragMove's unconditional marquee-update call)",
      "endMarquee — motion.js:12723 (this SAME artifact's own packet 1; onDragUp's unconditional marquee-end call)",
      "requestMotionDragTimelineRender — motion.js:12807 (this SAME artifact's own packet 2; every write-committing branch of onDragMove)",
      "flushMotionDragTimelineRender — motion.js:12814 (this SAME artifact's own packet 2; every onDragUp cleanup/finalize branch)",
      "stopMotionAutoScroll — motion.js:12841 (this SAME artifact's own packet 3; onDragUp's unconditional first cleanup step)",
      "window.SMEngineBridge.renderNow — engine-bridge.js (name only; every write-committing branch of onDragMove and every branch of onDragUp, each individually gated on the global's existence)",
      "window.loadFrame — app.js:4746 (onDragUp's #774 Time Remap second-half fix, only when a dragged group key has prop==='timeRemap')",
      "FC — app.js:76 (global var; not referenced directly in this packet's own code, only transitively via motionDragDeltaFrames/motionDragScrollLeft in packets 3-4, and directly in the skew branch's own `total` computation, motion.js:12990/12991)",
      "state.totalFrames — plain state field (clamp bound in the connect-drag and group/single key-drag branches)"
     ],
     "effects": "Document writes: `key.frame` ONLY, at exactly 4 write sites (cd.a.frame/cd.b.frame in the connect-drag branch; p.key.frame in the skew/space/liquify branch; s.key.frame in the group key-drag branch; d.key.frame in the single key-drag branch) — grep-confirmed to be the ONLY field this whole 440-line artifact's own onDragMove/onDragUp packet writes on a key object; `sortKeys(track)` reorders the owning track's own `.keys` array in place after every write (never deferred to onDragUp). Undo: ZERO pushUndo calls anywhere in onDragMove or onDragUp (grep-confirmed against this packet's own full body) — the checkpoint for every one of the 4 write-capable drag globals is taken ONCE, at the ORIGINATING MOUSEDOWN, entirely outside this span: `window._motionConnectDrag` (pushUndo at motion.js:10779, C20i), `window._motionSkewDrag` (pushUndo at motion.js:10936 for the C20i Alt+drag shortcut, or motion.js:12073 for C20q's own startSkewDrag), `window._motionKeyDrag` group-shaped (pushUndo at motion.js:10428 C20h/10554 C20h/10580 C20h/10936 C20i — 4 distinct mousedown sites), `window._motionKeyDrag` single-shaped (pushUndo at motion.js:10936, the SAME site as the group branch, since trackRowHtml's own mousedown handler calls pushUndo() once before branching into either shape). This satisfies CLAUDE.md §5quinquies's own contract cited in the issue: no `saveActiveLayerFrame`/`saveAllLayerFrames` call anywhere in this span either (grep-confirmed) — a key drag's data mutation happens synchronously in memory on the SAME live objects a later save will serialize, with the checkpoint already taken before the first pixel of movement. onDragUp's own role is exclusively: clear the drag globals to null, force one final render (flushMotionDragTimelineRender + renderNow, superseding whatever the last coalesced rAF paint showed), clear the snap guide, and — ONLY for a group drag whose `.keys` include a timeRemap-prop entry — force a `loadFrame` to re-resolve Component/video content. Keys are re-sorted DURING every move (synchronously inside onDragMove, immediately after each successful write), never deferred to onDragUp, and no dedupe pass runs at onDragUp either — a collision is always refused AT MOVE TIME (via the keyAt/groupCanMove checks documented above), never cleaned up after the fact. No showToast/SM.t call anywhere in this whole 440-line artifact (grep-confirmed) — unlike C20p's own selection operations, this drag engine gives NO toast feedback for any gesture, successful or refused."
    }
   ]
  }
 ],
 "source_consumers": [
  {
   "consumer": "save",
   "disposition": "No frame-save call (saveActiveLayerFrame/saveAllLayerFrames) occurs anywhere in motion.js:12685-13124 (grep-verified). This span's own writers (staggerSelectedLayers' `k.frame` shift; onDragMove's 4 `.frame` write sites across connect-drag/skew/group-key-drag/single-key-drag) all mutate the LIVE track/key objects directly — the same objects a later saveActiveLayerFrame/saveAllLayerFrames call (app.js, cited by name only) will serialize as part of `ld.motion`/`ld.elementMotion`. This span never itself triggers that serialization; onDragUp's own `loadFrame` call (app.js:4746, Time Remap second-half fix) re-resolves DISPLAY state, not persistence."
  },
  {
   "consumer": "load",
   "disposition": "No parser is implemented in this span. timeline.js's importJSON (cited by name only, P30) owns restoring `motion`/`elementMotion`, whose track.keys entries carry the `frame` field this span's writers move. `key.frame` is an ordinary JSON-serializable number (no live object reference, no special-cased load-time relink needed — unlike e.g. `data.brushCompanions` per CLAUDE.md §1)."
  },
  {
   "consumer": "undo/redo",
   "disposition": "ZERO pushUndo calls anywhere in onDragMove/onDragUp/the drag-engine helper packets (grep-confirmed across the full 440 lines) — the sole pushUndo call in this whole artifact is staggerSelectedLayers' own unconditional pre-loop checkpoint (motion.js:12689). Every key-drag gesture's own checkpoint is instead taken at the ORIGINATING MOUSEDOWN, entirely outside this span, by whichever sibling handler sets `window._motionConnectDrag`/`_motionSkewDrag`/`_motionKeyDrag` (C20h motion.js:10428/10554/10580, C20i motion.js:10779/10936, C20q motion.js:12073) — see packet 5's own effects field for the complete site list. This satisfies the CLAUDE.md §5quinquies contract the issue itself cites: no `saveActiveLayerFrame` during move, and (as an even stronger form here) no undo-stack push during move either — the checkpoint already exists before the drag's first pixel."
  },
  {
   "consumer": "selection",
   "disposition": "This span READS but does not itself ASSEMBLE selection: `_motionKeySel`/`_keyAnchor` (sibling module vars) are never read directly by this span's own symbols (grep-confirmed) — instead, this span reads the ALREADY-BUILT drag-state globals (`window._motionKeyDrag.keys`, `window._motionSkewDrag.rows`) that C20h/C20i/C20q's own mousedown handlers populated FROM `_motionKeySel` at drag-start time. This span DOES write `_layerSel`/`_layerSelAnchor` (endMarquee, packet 1) and the key selection via `setKeySel([])` (endMarquee's empty-click branch) — both transient, non-persisted selection state, not covered by any pushUndo in this artifact."
  },
  {
   "consumer": "animation",
   "disposition": "This span is the RETIMING surface for existing keys — it moves `key.frame` values (individually, in a group, skewed/spaced/liquified across a selection, or via a typed stagger step) but never creates or deletes a key, a track, or a Motion property (contrast with C20p's own subdivideKeys, which DOES create keys). It sits entirely downstream of key/track creation (C02/C20n) and selection assembly (C20o), and upstream of nothing but the render/save paths that read the moved frames afterward."
  },
  {
   "consumer": "render",
   "disposition": "Indirect only, through data: `key.frame` (every write site in this artifact) is read by rawValueAtFrame's own interpolation and by every consumer that resolves 'which key applies at this frame' (sibling-covered, cited by line only) on every subsequent rendered/exported frame. No symbol in this artifact is called directly from computeMotionMat/buildSceneJson/export.js (grep-confirmed). The `renderTimeline()`/`renderLayerList()`/`SMEngineBridge.renderNow()` calls this artifact makes throughout are UI-repaint triggers consuming the mutation this artifact just made, not the render-path construction itself — this artifact mutates the document the render path reads, which is a different thing (per the issue's own explicit framing)."
  }
 ],
 "proposed_behavioral_fixtures": [
  {
   "id": "group-key-drag-all-or-nothing-with-snap-fallback",
   "status": "proposed, unrun",
   "matrix": "A 4-key selection spanning 2 tracks. Drag such that the SNAPPED delta would collide an unselected key on track A, but the RAW (unsnapped) delta would not. Then drag such that BOTH the snapped AND raw deltas collide.",
   "expected": "Case 1: `groupCanMove(snappedDelta)` is false, `groupCanMove(rawDelta)` is true -> the move commits at the RAW delta (snap guide cleared, motion.js:13075), ALL 4 keys move by the same raw delta, track A's own key never overlaps the unselected occupant. Case 2: both fail -> onDragMove returns with ZERO keys moved this tick (`_motionKeySel` keys' `.frame` unchanged from before this tick), no partial move of the 3 non-colliding keys."
  },
  {
   "id": "single-key-drag-magnetic-dead-zone-avoidance",
   "status": "proposed, unrun",
   "matrix": "One key on a track with another key already at frame 20. Drag the first key such that the snap system pulls it toward frame 20 (a marker or the playhead sits there). Then drag it such that BOTH the snapped frame 20 AND the nearest raw frame are occupied (place a THIRD key immediately adjacent).",
   "expected": "Case 1: `keyAt(track,20)` finds the existing key -> falls back to the raw (unsnapped) frame (motion.js:13090-13093), the guide is cleared, the dragged key lands on the raw frame, NOT on 20. Case 2: the raw frame is also occupied -> onDragMove returns with the dragged key's `.frame` unchanged for that tick (motion.js:13094)."
  },
  {
   "id": "skew-space-liquify-never-snap",
   "status": "proposed, unrun",
   "matrix": "A 3-row key-selection-box drag (mode='top') positioned such that the resulting per-row target frames would coincide with a marker or the playhead if snapping applied. Compare the final `key.frame` values against what snapMotionFrame would have returned for the same raw targets.",
   "expected": "Every resulting `key.frame` equals `en.orig + Math.round(total*rowF)` EXACTLY (no snap adjustment applied to any of the 3 rows), and `#motion-snap-guide` is never created during this gesture (confirming motionDragMove's skew/space/liquify branch never calls snapMotionFrame, per packet 4's own callers field)."
  },
  {
   "id": "connect-drag-retime-vs-move-clamp-asymmetry",
   "status": "proposed, unrun",
   "matrix": "A 3-key track (i=0,1,2). Drag the connector between keys 0 and 1 (a) plainly (duration-constant move) toward key 2, and (b) with Alt held (retime) toward key 2, in both cases far enough that the drag would overshoot key 2's own frame if unclamped.",
   "expected": "(a) BOTH key 0 and key 1 move together by the SAME delta, clamped so key 1 never reaches or passes key 2's frame (hiD bound, motion.js:12966-12969) — key 0 stays the same distance before key 1 throughout. (b) ONLY key 1 moves; key 0 stays exactly at `cd.startAFrame`; key 1 is clamped to `[cd.startAFrame+1, key2.frame-1]` (motion.js:12955-12956) — it can shrink the segment to 1 frame but never invert or cross key 0."
  },
  {
   "id": "timeline-render-coalescing-single-paint-per-frame",
   "status": "proposed, unrun",
   "matrix": "Simulate 20 synthetic mousemove events dispatched within a single animation-frame budget (no actual rAF tick between them) during a single-key drag, then let one real rAF tick elapse, then dispatch mouseup. Instrument renderTimeline() with a call counter.",
   "expected": "Exactly ONE renderTimeline() call fires from the 20 coalesced moves (requestMotionDragTimelineRender's own idempotent-while-pending gate, motion.js:12808), reflecting only the LAST of the 20 `key.frame` values, not each intermediate one. Exactly one MORE renderTimeline() call fires at mouseup (flushMotionDragTimelineRender's own unconditional call, motion.js:12830), even though by that point `_motionDragTimelineRaf` is already 0 (the earlier rAF already fired) — confirming the 2026-08 fix's own contract (a pending-frame check would have skipped this call)."
  },
  {
   "id": "auto-scroll-edge-acceleration-and-teardown",
   "status": "proposed, unrun",
   "matrix": "During a group key-drag, move the pointer to sit exactly at the 34px edge threshold, then progressively further past it (10px, 30px, 60px past), then back into the middle, then release the drag entirely while still past the edge.",
   "expected": "`step` magnitude increases with distance past the edge (`Math.ceil(distance/3)`, motion.js:12849-12850) — 10px past yields a smaller step than 60px past. Moving back into the middle stops the rAF loop immediately (motion.js:12851) with no further scroll. Releasing the drag while still past the edge: onDragUp's own unconditional `stopMotionAutoScroll()` (motion.js:13103) tears down the rAF handle even if a tick was about to fire, and the NEXT tick (if one was already in-flight) finds `motionDragIsActive()` false and self-terminates (motion.js:12862) rather than calling onDragMove on a cleared drag object."
  },
  {
   "id": "stagger-selected-layers-anchor-and-clamp",
   "status": "proposed, unrun",
   "matrix": "3 selected layers (indices 5, 2, 8 — NOT in index order). Call staggerSelectedLayers(3) and then staggerSelectedLayers(-1000) (a step large enough to drive every shifted key below 0).",
   "expected": "Layers are processed in ASCENDING index order (2, 5, 8) regardless of selection order — layer 2 is rank 0 (never shifted), layer 5 is rank 1 (shifted by 3*1=3), layer 8 is rank 2 (shifted by 3*2=6). With step=-1000: every key on ranks 1+ clamps to frame 0 (motion.js:12696's own inline `Math.max(0,...)`), and if two keys on the SAME track both clamp to 0 they are NOT merged or deduplicated — `track.keys` after `sortKeys` contains both, at the same frame."
  },
  {
   "id": "end-marquee-three-independent-selections",
   "status": "proposed, unrun",
   "matrix": "Populate a non-empty layer selection, key selection, and bar (in/out) selection. Then (a) drag a marquee that sweeps 2 whole layer rows and moves >3px, (b) click once on empty grid space with no drag, (c) click once directly on a layer's own grid row with no drag.",
   "expected": "(a) `_layerSel` becomes exactly the 2 swept layers (in ascending order via `SM.setActiveLayer` then the full array assign, motion.js:12756-12759); key/bar selections are UNCHANGED by this branch (endMarquee's moved-with-swept-layers branch never calls setKeySel/clearSelection). (b) ALL THREE selections clear: key selection via `setKeySel([])`, bar selection via `SMLayerInOut.clearSelection()`, and layer selection via `_layerSel=[]` (motion.js:12785-12794) — three independent state stores, one gesture. (c) ONLY `selectLayerFromGrid(downLayer)` runs (motion.js:12777) and the function returns immediately — key/bar selections are left exactly as they were before the click (selectLayerFromGrid's own effect on them, if any, is sibling-covered, cited by line only, not re-derived here)."
  }
 ],
 "future_proposals": [
  {
   "api": "dragOutcome(dragObj, deltaFrames) -> {applied:boolean, appliedDelta:number, reason:'ok'|'snap-fallback-raw'|'abandoned-collision'|'abandoned-noop'}",
   "status": "bounded proposal, unimplemented",
   "boundary": "Every write-committing branch of onDragMove (connect-drag, skew/space/liquify, group key-drag, single key-drag) currently signals its own outcome ONLY via whether `key.frame` actually changed and whether a render was triggered — there is no explicit return value distinguishing 'moved exactly as requested' from 'fell back to the raw/unsnapped delta because the snapped target collided' from 'abandoned this tick entirely because even the fallback collided'. A future UI affordance (e.g. a cursor change or a toast on a fully-abandoned tick, which today gives NO feedback at all — see packet 5's own effects field) would need this distinction surfaced rather than re-deriving it by diffing `key.frame` before/after."
  },
  {
   "api": "snapSession(excludedKeys) -> {frame(rawFrame, e) -> number, dispose()}",
   "status": "bounded proposal, unimplemented",
   "boundary": "collectMotionSnapCandidates performs a full rescan of every layer/element/effect/marker/BPM-tick on EVERY call to snapMotionFrame, i.e. on every single mousemove tick of every connect-drag or key-drag gesture (packet 4's own callers field: 4 call sites, all inside onDragMove). None of that candidate set changes DURING a single gesture except for the dragged key(s)' own frame(s), which are already excluded via `excludedKeys`. A session object built once at drag-start (mirroring `window._motionKeyDrag`'s own start-of-drag snapshot pattern) could reuse one candidate list for the whole gesture instead of rebuilding it on every tick."
  },
  {
   "api": "extendSnapToBoxGestures(sk, plannedFrames) -> number[] (per-key snapped frames for skew/space/liquify)",
   "status": "bounded proposal, unimplemented",
   "boundary": "The three key-selection-box gestures (skew/space/liquify) never call snapMotionFrame at all (grep-confirmed, packet 4's own callers field) — a user skewing a block of keys toward a marker or the playhead gets no magnetic assistance, unlike a plain single/group key drag. Extending the snap contract to a PER-ROW or PER-KEY aggregate tolerance (rather than the single-delta contract snapMotionFrame currently exposes) is a bounded but real gap between these two drag families."
  },
  {
   "api": "onDragUp's Time Remap reload check widened to the single-key drag shape",
   "status": "bounded proposal, unimplemented",
   "boundary": "The #774 second-half fix (motion.js:13118) only inspects `dragged.some(d => d.prop==='timeRemap')`, which is only ever non-empty for the GROUP-shaped `window._motionKeyDrag` (its `.keys` array carries a `.prop` per entry); the single-key shape's own object exposes `d.prop` directly, not inside a `.keys` array, so a lone Time Remap key dragged via the single-key path (trackRowHtml's own unselected-key mousedown, motion.js:10954) would NOT trigger the `loadFrame` reload this fix exists for — a path-dependent gap worth verifying against real Time Remap tracks specifically (this artifact's own reading of the code is that both shapes CAN carry a timeRemap prop; only the check's own shape-sensitivity is flagged here, not asserted as a confirmed live bug — no runtime reproduction was attempted, per this leaf's own no-runtime-acceptance scope)."
  }
 ],
 "surface_inventory_cross_check": {
  "packet_symbols": [
   "staggerSelectedLayers",
   "updateMarquee",
   "endMarquee",
   "_motionDragTimelineRaf",
   "requestMotionDragTimelineRender",
   "flushMotionDragTimelineRender",
   "motionDragScrollLeft",
   "_motionAutoScrollRaf",
   "_motionAutoScrollEvent",
   "motionDragIsActive",
   "stopMotionAutoScroll",
   "autoScrollMotionDrag",
   "motionDragDeltaFrames",
   "collectMotionSnapCandidates",
   "showMotionSnapGuide",
   "clearMotionSnapGuide",
   "snapMotionFrame",
   "onDragMove",
   "onDragUp"
  ],
  "caller_anchors": [
   "onDragMove/onDragUp: the MATERIAL finding of this leaf — zero occurrences anywhere in src/js outside motion.js (both a parenthesized-call grep and a bare-identifier grep), wired exclusively by this SAME span's own `document.addEventListener` calls (motion.js:13121-13122), directly contradicting their own lead-in comment's claim of ui.js/SMMotion delegation (see boundaries). The actually-exported, actually-delegated function with a similar name is the unrelated singular `onDrag` (motion.js:6507/13777, canvas gizmo, `_motionDrag` module var, called from tools.js:7632 and select-bridge.js:1800) — outside this artifact's own scope entirely.",
   "staggerSelectedLayers: exactly ONE caller anywhere in src/js, a context-menu action at motion.js:7473, itself in a gap not covered by any merged census as of this leaf's authoring — not exported on window.SMMotion.",
   "updateMarquee/endMarquee: zero callers by name anywhere except this SAME artifact's own onDragMove/onDragUp (packet 5) — layer-inout.js's OWN, textually-distinct, same-named local functions (layer-inout.js:479/497) are a coincidental naming convention shared across the 'one gesture, one rectangle' pattern (CLAUDE.md §11), not a cross-file call.",
   "requestMotionDragTimelineRender/flushMotionDragTimelineRender/motionDragScrollLeft/motionDragIsActive/stopMotionAutoScroll/autoScrollMotionDrag/motionDragDeltaFrames/collectMotionSnapCandidates/showMotionSnapGuide/clearMotionSnapGuide/snapMotionFrame: every one of these 11 symbols has ALL of its callers inside this SAME artifact (cross-packet-internal to C20s), except motionDragScrollLeft, whose OTHER 7 call sites are `startScrollLeft: motionDragScrollLeft()` field initializers inside C20h/C20i/C20q's own mousedown handlers that construct the drag-state objects this artifact later reads — the single external caller pattern in an otherwise fully self-contained packet cluster."
  ],
  "helper_anchors": "propsFor:176, trackFor:443, sortKeys:1188, keyAt:1189 (C02, none re-derived). isKeySelected/setKeySel/_groupKeySelByTrack:11412/11473/11640 (C20o, never called directly by this span). _motionMarquee/startMarquee/applyMarqueeSelection/marqueeLayersIn/addStaggerEdges/addSpaceEdges/addMoveFill/startSkewDrag/buildKeyRows/the key-selection box:11925-12174 (C20q, MERGED PR #1279 mid-authoring). _layerStaggerBoxEl/buildLayerRows/dragLayerBars/_layerSkew/startLayerSkewDrag/onLayerSkewMove:12265/onLayerSkewUp:12281/updateLayerStaggerBox:12175-12405 (C20r, MERGED PR #1280 mid-authoring). selectLayerFromGrid:11126, syncBarSelToLayerSel:6932 — neither covered by any merged census as of this leaf's authoring (verified by the full origin/main@6cfd3ff sweep). _layerSel:timeline.js:5423, _layerSelAnchor:timeline.js:5432 (top-level globals, bare-identifier scope fallthrough, no local declaration in motion.js). SM.setActiveLayer:timeline.js:1534. SMLayerInOut.marqueeSelect/getBarSelection/clearSelection: layer-inout.js (=applyMarqueeSelection/anon/clearBarSel, layer-inout.js:1640/1654/1641). pushUndo:tweens.js:4741. renderTimeline:timeline.js:4026, renderLayerList:timeline.js:6335. window.loadFrame:app.js:4746. FC:app.js:76 (global var, not module-scoped). state.currentFrame/waIn/waOut/markers/layers/totalFrames/bpmShow/bpm/bpmOffset/fps: plain state fields.",
  "render_path_summary": "No symbol in this artifact is on the render path directly (zero calls from engine-bridge.js/export.js, grep-confirmed). The single field this artifact writes anywhere, `key.frame` (grep-confirmed: zero writes to any other key field across all 440 lines), IS render-path input, read later by rawValueAtFrame's own interpolation and every consumer that resolves the applicable key for a given frame (sibling-covered, cited by line only). This artifact's own `renderNow`/`renderTimeline` calls trigger a REPAINT of the mutation just made; they are not themselves part of scene construction — mutating the document and rendering it are different operations, and every symbol here does only the former, then asks something else to do the latter."
 },
 "validation": {
  "expected_assigned_lines": 440,
  "expected_classification": {
   "code": 300,
   "comment": 136,
   "whitespace": 4
  },
  "packet_partition": [
   [
    12685,
    12800
   ],
   [
    12801,
    12831
   ],
   [
    12832,
    12870
   ],
   [
    12871,
    12930
   ],
   [
    12931,
    13124
   ]
  ],
  "packet_classification": [
   {
    "range": [
     12685,
     12800
    ],
    "code": 62,
    "comment": 53,
    "whitespace": 1
   },
   {
    "range": [
     12801,
     12831
    ],
    "code": 12,
    "comment": 18,
    "whitespace": 1
   },
   {
    "range": [
     12832,
     12870
    ],
    "code": 35,
    "comment": 4,
    "whitespace": 0
   },
   {
    "range": [
     12871,
     12930
    ],
    "code": 59,
    "comment": 0,
    "whitespace": 1
   },
   {
    "range": [
     12931,
     13124
    ],
    "code": 132,
    "comment": 61,
    "whitespace": 1
   }
  ],
  "coverage_contract": "Validate assigned_ranges and flattened areas[].packets[].coverage_ranges independently with the same function; reject actual artifact omission at line 12685 and overlap at line 13124.",
  "required_checks": [
   "frozen/current motion.js blob and allocated-base ancestry",
   "five unique packet IDs, exact ranges, counts, symbols, helpers, callers and boundaries",
   "before-12685 (genuinely open gap 12406-12684, sibling-named but uncensused) and after-13124 (genuinely open gap 13125+, same) boundaries, plus the two MERGED sibling boundaries at 12174/12175 (C20q) and 12405/12406 (C20r) reconciled by name",
   "five consumer dispositions and proposed, unrun fixtures",
   "the complete window._motionKeyDrag (group and single shapes)/_motionConnectDrag/_motionSkewDrag object shapes, every field this span reads vs writes, cross-cited to their writers in C20h/C20i/C20q by line only",
   "the dispatch order inside onDragMove (onLayerSkewMove -> _motionConnectDrag -> updateMarquee (always) -> _motionSkewDrag -> _motionKeyDrag) and onDragUp (onLayerSkewUp -> stopMotionAutoScroll (always) -> endMarquee (always) -> skew cleanup -> connect cleanup -> key-drag finalize), with the exact gating global for each branch",
   "the key-drag semantics as pure input->output per mode (move: uniform delta; skew: per-row factor; space: per-column/time factor; liquify: Gaussian falloff from the grab point) with the exact factor computation lines",
   "the collision rule per branch (all-or-nothing refusal for group/skew-space-liquify; snap-then-raw-fallback for single-key and connect-drag; never a merge or swap) and the frame-0/totalFrames clamp per branch",
   "whether keys are re-sorted during (yes, synchronously, every branch) or after the drag, and that no dedupe pass runs at onDragUp",
   "the write discipline: key.frame is the ONLY field mutated live; zero pushUndo calls in this whole span; every drag global's own checkpoint is taken once at its originating mousedown outside this span (with exact citations); the CLAUDE.md §5quinquies no-saveActiveLayerFrame-during-move contract",
   "the requestMotionDragTimelineRender/flush coalescing contract (idempotent-while-pending vs unconditional-on-flush) and the renderNow/loadFrame follow-ups, including the Time Remap #774 second-half fix and its group-vs-single-shape asymmetry",
   "the auto-scroll loop (34px edge threshold, distance/3 acceleration, rAF lifetime, the three independent stop conditions) and the snap contract (collectMotionSnapCandidates' 5 ordered, first-wins sources, 8px-to-frame tolerance, the metaKey/ctrlKey override, _motionSnapEnabled, and that skew/space/liquify never call it)",
   "endMarquee's effects on _layerSel/_layerSelAnchor, the key selection and the bar selection (three independent stores, one gesture) and staggerSelectedLayers' exact writes (k.frame only, one pushUndo, no dedupe)",
   "every DOCUMENT write reachable from the span (key.frame only) with undo disposition",
   "every external function called from the span (complete, with owning census/module) and the complete caller list of every function declared in 12685-13124 across src/js (parens AND bare-identifier grep passes), including the material onDragMove/onDragUp non-delegation finding",
   "confirmation that none of these symbols run on the render path (buildSceneJson/getEffectiveStrokes) — they mutate the document the render path reads, a different thing",
   "JSON, private-path, secret, placeholder and trailing-whitespace hygiene",
   "sole untracked artifact or committed-clean mode",
   "no overlap with any merged C20* sibling artifact in the same census directory (including the now-merged C20q/C20r)"
  ],
  "runtime_limit": "No browser/Tauri/app suite, hosted CI, GPU, runtime edit, export or product acceptance claim."
 }
}

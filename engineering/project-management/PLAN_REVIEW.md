# Plan review and kickoff decisions

The published remediation plan covers the requested foundation program. The task manifest preserves its complete scope: seven phase goals, 23 R packages, six migration families below R18, and seven BZ packets. R18 is an epic, bringing the total to eight epics and 35 tasks.

The dependency graph distinguishes phase completion gates from tasks that can start earlier in parallel. Shared image/color contracts in R17 precede the R16 OpenFX consumer proof; the graph contains no circular dependency. R18 families carry their own prerequisites, so native/plugin work does not unnecessarily block independent document or animation characterization. R19 can start measurement from R03/R12 evidence while its final performance gate follows the migrations.

R04 remains conditional on reproducing native packaging problems on the selected base. R18 children require all applicable document consumers and public interfaces to move together. Browser and packaged desktop evidence remain separate through R21/R22. Historical tests, old labels, branch work or source-complete claims do not satisfy current acceptance.

The board adds P3, Program, typed dates, planning windows, validation, risk, size, estimates, review ownership, references and exception expiry to the published minimum schema. It uses native assignees, labels, milestone, issue hierarchy, dependencies and linked PRs. All 43 remediation items now have forecast dates, based on the user-selected September 4 start. Human assignments and committed deadlines still require owner decisions. Schedule basis makes that distinction explicit.

Before kickoff, complete R00/BZ0: name the human integration owner and backup, collect active branches/scopes/unpushed work, choose the fresh base and supported validation surfaces, and confirm initial writer/reviewer assignments. No task becomes Ready solely because it appears on the board. Confirm the proposal's 24-hour foundation and 7–14-day program windows against actual capacity and acceptance evidence.

Older proposal language about manually assigning managed agents and maintaining per-path grants is superseded by the active dedicated Nemo workspace contract. Authenticated enrolled collaborators and their managed agents have automatic Nemo repository access. That access preserves branch protections, separate writer worktrees, ownership reconciliation, typed A2A lifecycle and explicit release authority. Live BZ acceptance stays with the release-owning session.

The issue audit retains explicit maintainer closure/defer decisions for #470, #520, #524, #537 and #547. Their known residual limitations remain visible in the triage ledger as deferred scope; this preparation does not silently reopen those decisions or turn them into new feature commitments.

## Reviewed timeline

The [dated schedule](TIMELINE.md) preserves the H24 first foundation increment, H240 working target and H240–H336 acceptance buffer. The native effect proof continues after H24: R17 finishes at H32, then R16 at H52. Full subsystem migration runs through H176, recovery through H192, distribution validation through H208, and R22 acceptance targets H240. F6 includes the buffer through H336.

Every concrete task starts after its declared predecessors finish. Epic spans roll up their children; phase blocked-by edges are completion gates, so independent preparation can overlap earlier phases. R18 document work starts at H20 after its own contracts land while gesture work continues until H36. R19 measurement starts early and continues through migration. These overlaps preserve the task graph and the kickoff plan’s parallel-work model.

The forecast assumes three full lead teams, disjoint writers, prompt reviews and available native/GPU machines. Broad migrations and cross-machine acceptance carry low confidence until owners confirm capacity. Exact elapsed-hour offsets are planning estimates, not staffing or completion claims.
